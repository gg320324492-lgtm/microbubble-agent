# W68 第 14 批派工纪要候选：Prompt Template v6

> 版本：v6（2026-07-24）
> 适用：主指挥、并行 agent、文档/调研/迁移派工
> 原则：先验证、再派工；先串链、再合并；先反馈、再升级；合并顺序可见；派工前提错误必沉淀。

## §1 升级背景：v5 → v6 增量

### §1.1 v5 已经解决了什么

v5 把 v4 的"门禁 + 反馈循环 + 合并顺序表"全部沉淀进模板：

| 能力 | v5 段位 |
|---|---|
| Alembic 双头门禁 | 段 3 |
| PowerShell 5.1 binding 门禁 | 段 4 |
| plans 真验证门禁 | 段 3 |
| 经验反馈循环 | 段 5（必填"有效段 / 多余段 / 偏离段"三类）|
| 主指挥合并顺序表 | 段 6（必含 alembic 串单链位置）|

v5 在 W68 第 14 批 15 agents 派工时落地（commit `93dbd2cc7`）。A-2 memory 锚点范式第 170 守恒。

### §1.2 v5 在 W68 第 14 批实战中暴露的 2 类缺口

W68 第 14 批 15 agents 派工试用 v5 后，agent 完工回传出现 2 类问题：

**缺口 1：段 5 反馈"只填了"，但填得"不深"。** 多数 agent 回传仅写"无 fail"或"全部有效"，不指明段 1–4 中**具体哪一句 / 哪条约束**真正帮到任务，也**不主动建议**模板如何升级。主指挥拿不到足够颗粒度的信号去推 v6。

**缺口 2：缺"派工前提错误复盘"段。** 主指挥拍板派工时，往往隐含若干前提（如：B-1/B-2/B-3 alembic 接续关系、PS 5.1 binding 规避、plans 真验证纪律、web 必须 `npm run build`、baseline 守恒）。这些前提若事后被证伪，agent 已经在错误前提下跑完，回填已是事后诸葛亮。主指挥需要"派工前提错误 24h 内回填"机制，让下一批派工不再重复同样错误。

### §1.3 v6 增量

v6 保留 v5 的所有门禁与"段 5 反馈 + 段 6 合并顺序"两段，仅升级段 5 + 新增段 7：

| 升级 | 段位 | 内容 |
|---|---|---|
| 段 5 反馈循环升级 | 段 5 | 必填 6 项（有效段 / 多余段 / 新增段 7 候选 / 旧段升级建议 / 派工前提错误 / 锚点范式变化）|
| 新增段 7 | 段 7 | 派工前提错误复盘（24h 内回填本批 5 大派工前提错误实例）|

v6 默认应用从 W68 第 15 批开始；W71 默认应用 v6；W72 调研任务必须用 v6。

## §2 v6 完整 7 段模板

以下模板可直接复制到派工消息；尖括号内容必须替换。

### 段 1：角色、范围与不变量

```text
你是 Agent <编号>：<标题>。目标是 <一句话目标>。
范围：<文件/目录>；不范围：<明确排除项>。
硬规则：0 production code（如适用）；不得 merge；不得覆盖他人改动；
输出必须包含证据路径、测试结果、commit hash 和阻塞项。
当前分支：<worktree 分支名>；基线 commit：<hash>；
依赖 agent：<agent-id>（说明是否等待其 commit）。
```

### 段 2：交付物与操作边界

```text
交付物：
1. <文件一>：<内容和大致规模>
2. <文件二>：<内容和大致规模>
禁止：<代码/配置/数据库/部署等不应修改的对象>。
```

若需要脚本，列出运行时版本（Windows PowerShell 5.1 或 PowerShell 7）和调用约定；若需要 migration，列出 revision 命名范围及明确的 `down_revision` 上游。

### 段 3：任务描述、前置验证与风险门禁

```text
开始前：
- git status；确认基线没有未授权修改。
- plans 任务：grep -rn "<keyword>" C:/Users/pc/.claude/plans/，
  不以 status 自报替代事实；真未实施项写 backlog docs，完成项标
  COMPLETED + 真 commit。
- alembic 任务：git fetch origin main && cd main && alembic heads；
  检查 revision 唯一、down_revision 指向最新 head；发现双头立即报主指挥。
- 其他任务：<领域特定检查>。
```

迁移 agent 不得自动替换有争议的 down_revision；必须给出冲突矩阵。合并后由主指挥 rebase 重命名（如 070→075/074/076）、串单链并再跑 heads。

### 段 4：完成定义、测试与 PS 5.1 约束

```text
完成定义：
- 所有交付文件存在且内容可审计；
- <测试/grep/history 验证>通过；
- 报告证据路径和命令输出摘要；
- 未完成项写 BLOCKED，并说明下一步。
PowerShell 5.1：使用 --mode <value>（空格），[string]$Mode，
仅用 $Mode -eq 'session' 判断 session；附两种模式实跑证据。
```

完成定义不能只写"代码已写"或"测试通过"。测试需说明是单元、集成、静态检查还是人工检查，以及是否受环境限制。对于 docs-only 任务，至少做文件计数、关键词检查、git diff --check。web 改动必须 `npm run build`（唯一合法命令，参见 CLAUDE.md PWA manifest 410 铁律）。

### 段 5：经验反馈循环（v6 升级：6 项必填）

```text
回传反馈（必填，6 项缺一不可）：
1. 段 1–4 哪些段 / 句子有效？（具体指明段号 + 句子/短语 + 帮到了什么）
2. 哪些段多余 / 偏离 / 重复？（具体句子 + 偏离原因）
3. 新增段 7 候选：本次任务是否暴露了"应纳入模板"的新段？（如：审计脚本段 / 跨 PR 部署 checklist 段 / hook 段）
4. 旧段升级建议：段 1–4 哪一句应被改写？（原句 + 改写后 + 一句话理由）
5. 派工前提错误：本批派工蕴含哪些前提？哪个前提事后被证伪？（详见段 7）
6. 锚点范式变化：本批是否推进了锚点范式数字？推进多少？为什么？

不填任一项视为完工未达标，主指挥合并时不视为有效交付。
```

主指挥汇总 N agents 反馈后升级 v7。新铁律按"反馈必须 6 项齐全 + 至少 1 条派工前提错误实例 + 0 条负面偏离才进入 v7 候选"规则筛选。

### 段 6：主指挥合并顺序表（沿用 v5，派工必读）

```text
主指挥合并链（本 agent 必须遵守）：

| 步骤 | agent-id | 任务标题 | commit 范围 | 前置依赖 | 串单链位置 |
|---|---|---|---|---|---|
| Step 1 | <id> | <标题> | docs/.../file.md | 基线 | — |
| Step 2 | <id> | <标题> | alembic/versions/070_*.py | Step 1 | base=068 down_revision=068 |
| Step 3 | <id> | <标题> | memory/.../v5-2026-07-24.md | Step 2 | docs ref Step 1+2 |
| ... | ... | ... | ... | ... | ... |

合并触发条件：
- 主指挥在所有 agent 回传 commit hash 后按 Step 顺序 rebase / cherry-pick / merge。
- alembic 串单链中途若发现双头，停止合并并报主指挥拍板（v4 §1.1）。
- agent 不主动 merge 或 push（除非派工明文授权）。

agent 完工后必须确认 commit hash 出现在顺序表的正确行；如发现顺序与派工不一致，立报主指挥。
```

合并顺序表让 agent 提前知道：(1) 谁在它前面，谁在它后面；(2) 是否需要等 alembic 上游；(3) 是否需要被 docs 下游引用；(4) 主指挥合并时如何串链。

### 段 7：派工前提错误复盘（v6 新增，24h 内必填）

```text
派工前提错误（24h 内必填，至少 5 类各 1 例）：

| 类别 | 派工时假设 | 实际验证结果 | 修正方式 | 沉淀位置 |
|---|---|---|---|---|
| alembic 串单链 | B-1 down_revision 接 077 | 实际 merge 后 revision 重命名 070→074 | 主指挥改 063 down_revision | memory/w68-alembic-...md |
| PS 5.1 binding | 使用 --mode session | 实际传入 '--mode "session"' 被解析为多 token | 改 [string]$Mode + 严格 -eq | docs/派工 v4 §4 |
| plans 真验证 | plan Status 段标 completed | 实际未实施 (commit 4b215220 refactor 意外删除) | grep plans + git show + 审计单证 | memory/verified-plans-w68.md |
| web `npm run build` | 只需 vite build | 实际需 postbuild 自动 3 件事 + 健全性自检 + force-add dist | 禁止 vite build 直跑，postbuild 唯一合法 | docs/pwa-manifest-410.md |
| baseline 守恒 | 71 PASS + 7 SKIP | 实际增加新 PASS 或 SKIP 即视为回归 | 守恒 = 0 PASS 增 + 0 SKIP 增 | scripts/ci_qa_bench_baseline.sh |

沉淀规则：
- 每类前提错误必须有真实案例引用（commit hash / file path / commit message）；
- 沉淀位置统一在 memory/w68-<batch>-<route>-<topic>-<date>.md，禁止散落；
- 主指挥在 grand closure 时汇总本批所有派工前提错误，更新 CLAUDE.md 永久锚点节；
- 24h 内未填视为派工流程违规，主指挥应在下批派工前提检查清单中加严。
```

派工前提错误不是"派工失败"，而是"派工时主指挥拍板的隐含前提事后被证伪"。沉淀是为了让下一批派工不再重复同样错误。5 大类前提覆盖了本项目 13 批派工中最常踩的派工坑（alembic 串单链冲突 / PowerShell 参数解析陷阱 / plans 自报偏差 / PWA build 命令 / baseline 守恒红线）。

## §3 v5 → v6 diff 详表

### 3.1 段级 diff

| 段位 | v5 | v6 增量 |
|---|---|---|
| 段 1 角色/范围/不变量 | 不变 | 不变 |
| 段 2 交付物/边界 | 不变 | 不变 |
| 段 3 前置验证/门禁 | 不变 | 不变 |
| 段 4 完成定义/PS 5.1 | 不变 | 新增"web 改动必须 `npm run build`"约束 |
| 段 5 反馈循环 | 3 项必填（有效段 / 多余段 / 偏离段）| 升级为 6 项必填 + 新增"派工前提错误"+"锚点范式变化" |
| 段 6 合并顺序表 | 不变 | 不变 |
| 段 7 派工前提错误 | 无 | **新增**：24h 内必填 + 5 大类前提表格 + 沉淀规则 |

### 3.2 新增能力 diff

| 维度 | v5 | v6 增量 |
|---|---|---|
| 段数 | 6 段 | +段 7 → 7 段 |
| 模板长度 | ~70 行 | +段 5 升级 ~ 12 行 +段 7 新增 ~ 15 行 → ~97 行 |
| 反馈颗粒度 | 3 项必填 | 升级 6 项必填（每项需具体句子/短语）|
| 派工前提沉淀 | 无 | 24h 内 5 大类各 1 例 + 沉淀位置统一 |
| 锚点范式显式追踪 | 无 | 段 5 第 6 项显式询问"锚点范式变化多少 + 为什么" |
| web build 命令约束 | 无 | 段 4 显式"必须 `npm run build`，禁止 vite build 直跑" |
| 关门强度 | 段 1–6 必填 | +段 5 必填 6 项 +段 7 必填 5 类 + 24h 限时 |

### 3.3 v6 兼容性矩阵

| 版本 | 兼容 v6? | 升级路径 |
|---|---|---|
| v1（基本角色/范围/交付物）| 否（无反馈/合并表/前提）| 整模板替换为 v6 |
| v2（+ 测试/证据/分支边界）| 否（缺段 5/6/7）| 整模板替换为 v6 |
| v3（5 段雏形）| 否（缺反馈/合并表/前提）| 整模板替换为 v6 |
| v4（5 段完整 + 3 大门禁）| 部分（缺段 6/7）| 追加段 6 + 段 7，段 5 升级为 6 项 |
| v5（+ 段 5 反馈 + 段 6 合并表）| 几乎兼容 | 段 5 升级为 6 项，追加段 7 |
| v6（目标版本）| — | — |

### 3.4 不破坏既有 v5/v4/v3/v2/v1 历史派工

v6 严格遵循"只增可验证门禁，不删除已有兼容项"原则：

- 段 1–4 完全沿用 v5/v4，无任何文字删除或弱化
- 段 5 是"反馈必填"的扩展，不替换 v5 的"三类反馈"
- 段 6 完全沿用 v5，不动一字符
- 段 7 是**全新段**，纯增量

## §4 W68 第 14 批 15 agents 应用 v5 → v6 升级实战反馈

W68 第 14 批 15 agents（A-1 ~ A-4 + B-1 ~ B-3 + C-1 ~ C-4 + D-1 ~ D-3）派工时已应用 v5。v6 升级基于以下实战反馈：

### 4.1 v5 段 1 实战反馈（有效）

- A-2 memory 沉淀："v5 段 1 清晰，'不范围'段让 agent 不越界。"
- B-1/B-2/B-3 alembic 实施："段 1'依赖 agent'列清晰，串单链顺序明确。"
- C-1 qa-bench D8 调研："段 1 范围'调研 + 调研报告 docs'清晰，未越界到 D8 实施。"

**结论**：段 1 全部有效，v6 保留原文。

### 4.2 v5 段 2 实战反馈（有效）

- B-1/B-2/B-3 alembic："段 2 交付物'070/071/072 alembic + 4 API + audit'清晰。"
- D-1 prompt v6："段 2 交付物'docs/w68-dispatch-candidates-v6 + memory/w68-route-14-d1-*'清晰。"
- C-2 mobile dark："段 2 禁止'mobile 桌面端组件'让 agent 不动桌面端 CSS。"

**结论**：段 2 全部有效，v6 保留原文。

### 4.3 v5 段 3 实战反馈（有效）

- B-1/B-2/B-3 alembic："段 3'alembic 串单链'门禁让 agent 在开工前跑 `alembic heads`，避免了双头事故。"
- A-3 W70+ 调研："段 3'plans 真验证'门禁让 agent 跑 grep plans + git show + 审计单证。"
- C-1 qa-bench D8："段 3'其他任务'让 agent 自定义 D8 调研前置检查。"

**结论**：段 3 全部有效，v6 保留原文。

### 4.4 v5 段 4 实战反馈（有效 + web 约束待补）

- B-4 notify v2 验证："段 4'PS 5.1 binding'让 agent 用 [string]$Mode + -eq 严格判断。"
- D-2 docs 同步："段 4'git diff --check'让 agent 验证 markdown 无空白错误。"
- C-2 mobile dark：**段 4 未明确说"web 改动必须 `npm run build`"**——agent 默认 `vite build` 直跑，绕开 postbuild，违反 PWA manifest 410 铁律（commit `59187ce8` 教训）。

**结论**：段 4 全部有效，但需在 v6 显式加入 "web 改动必须 `npm run build`，禁止 vite build 直跑"。

### 4.5 v5 段 5 实战反馈（**不充分**，v6 升级）

W68 第 14 批 15 agents 段 5 回传分析：

- 5/15 agents 仅写"全部有效"或"无 fail"，未指明具体句子
- 8/15 agents 写了"有效段/多余段/偏离段"但颗粒度粗（如"段 6 合并表有帮助"）
- 2/15 agents 完全未填段 5，主指挥合并时才发现

**v6 升级方案**：必填 6 项（有效段 / 多余段 / 新增段 7 候选 / 旧段升级建议 / 派工前提错误 / 锚点范式变化），每项需具体句子/短语。颗粒度强制提升。

### 4.6 v5 段 6 实战反馈（有效，v6 保留）

- B-1/B-2/B-3 alembic："段 6 合并表'串单链位置'列让 agent 知道 078→079→080 接续关系。"
- A-1 主指挥部署："段 6 表格让主指挥合并时按 Step 顺序操作，节省沟通时间。"
- D-3 锚点范式："段 6 表格让 agent 在自己 worktree 内只 commit 不 merge。"

**结论**：段 6 全部有效，v6 保留原文。v6 不做段 6 改动。

### 4.7 v5 → v6 实战派工前提错误（W68 第 14 批 5 大类）

| 类别 | 派工时假设 | 实际验证结果 | 修正方式 |
|---|---|---|---|
| alembic 串单链 | B-1 down_revision 接 077 | 派工时未明确 W68 第 13 批已完成 070→074/075/076/077 串链 | 主指挥拍板预分配 078/079/080 |
| PS 5.1 binding | 跨平台 PS 7 默认 | 部分 agent 工作站在 Windows PowerShell 5.1 默认 | 强制 [string]$Mode + -eq + 双模式实跑 |
| plans 真验证 | plan Status 段自报为 completed | A-3 W70+ 调研发现 6 个 plan 仍需真验证 | 必跑 grep + git show + 审计单证 |
| web `npm run build` | vite build 直跑即可 | C-2/C-3 web 改动需走 postbuild 链路 | v6 段 4 强制 `npm run build` |
| baseline 守恒 | 71 PASS + 7 SKIP 不变 | 任何新增 PASS/SKIP 即视为回归 | 守恒红线写入 v6 段 4 + 段 7 |

**结论**：5 大派工前提错误必须沉淀到段 7 表格 + memory/w68-*-派工前提-2026-07-24.md。v6 段 7 强制 24h 内回填。

## §5 7 条新铁律（v6 沉淀）

1. **段 5 反馈必填 6 项**——agent 完工回传必须含段 5（"有效段 / 多余段 / 新增段 7 候选 / 旧段升级建议 / 派工前提错误 / 锚点范式变化"六类至少各 1 条具体建议），否则视为"完工未达标"，主指挥合并时不视为有效交付。
2. **段 6 合并顺序表必含 alembic 串单链位置 + 跨 PR 部署 checklist**——派工消息段 6 的"串单链位置"列必须写明 `base=<上游 revision> down_revision=<上游 revision>`，且 `base` 必须等于同表"前置依赖"列指向的 commit 的 alembic revision；agent 不得擅自修改。涉及跨 PR 部署时（如 alembic 078/079/080 串链），必须附"docker cp + clear cache + upgrade + restart" 4 步 checklist。
3. **v6 默认应用从 W68 第 15 批开始**——任何 W68 第 15 批及以后的派工必须使用 v6；W71 派工也默认 v6；W72 调研任务**必须**用 v6（避免 plans 自报偏差）。
4. **段 7 派工前提错误必 24h 内回填**——agent 完工后 24h 内必须回填段 7（5 大类前提表格 + 真实案例引用 + 沉淀位置），未填视为派工流程违规，主指挥应在下批派工前提检查清单中加严。
5. **段 7 5 大派工前提错误必含**——alembic 串单链 / PS 5.1 / plans 真验证 / web `npm run build` / baseline 守恒。任何派工必含这 5 类前提的隐含假设，agent 完工后必回填每类的"假设/实际/修正"3 段。
6. **web 改动必须 `npm run build`，禁止 vite build 直跑**——v6 段 4 显式写入。违反者按 PWA manifest 410 铁律（commit `59187ce8` 教训）处理，dist 必须 `git add -f`，否则服务器 410 + 浏览器 PWA install 失败。
7. **v6 段 5 第 6 项必填"锚点范式变化"**——agent 完工后必须显式声明本批是否推进了锚点范式数字、推进多少、为什么。主指挥 grand closure 时据此核对四维度（编号连续性 / agent 任务映射 / 可复现证据 / 0 production code 守恒），不依赖"只统计数量"。

## §6 v1/v2/v3/v4/v5/v6 完整兼容矩阵

| 版本 | 段数 | 核心能力 | Alembic 门禁 | PS 5.1 门禁 | plans 真验证 | web build 约束 | 反馈循环 | 合并顺序 | 派工前提沉淀 | 适用 |
|---|---|---|---|---|---|---|---|---|---|---|
| v1 | 3 | 基本角色/范围/交付物 | 未明确 | 未明确 | 未明确 | 未明确 | 无 | 无 | 无 | 历史单 agent 小任务 |
| v2 | 4 | + 测试/证据/分支边界 | 有基本 heads | 有调用示例但不严格 | 以 status 为主 | 未明确 | 无 | 无 | 无 | 一般文档/前端任务 |
| v3 | 5 | 5 段结构雏形 | 强调串链但未覆盖 070 三方 | 未覆盖 PS 5.1 | 未覆盖自报偏差 | 未明确 | 无 | 无 | 无 | W68 第 12 批 |
| v4 | 5 | 完整 5 段 + 前置门禁 + 证据闭环 | revision 唯一 + 最新 head + 双头拍板 + 重编号串链 | 空格参数 + [string] + 严格 -eq + 双模式实跑 | grep plans + 5 项 backlog + COMPLETED+真 commit | 未明确 | 无 | 无 | 无 | W68 第 13 批 + W71 |
| v5 | 6 | v4 + 段 5 反馈 + 段 6 合并顺序 | 同 v4 | 同 v4 | 同 v4 | 未明确 | 段 5 必填 3 项 | 段 6 表必含 | 无 | W68 第 14 批 + W71+ |
| **v6** | **7** | **v5 + 段 5 升级 6 项 + 段 7 派工前提错误 + web build 约束 + 锚点范式显式追踪** | **同 v4 + 预分配 revision 号段** | **同 v4** | **同 v4** | **强制 `npm run build`** | **段 5 必填 6 项 + 24h 内** | **段 6 表 + 跨 PR 部署 checklist** | **段 7 必填 5 类 + 24h 内** | **W68 第 15 批 + W72** |

### v5 → v6 升级路径

- **段 1–4**：完全沿用 v5，不改任何字。
- **段 5**：从 3 项必填升级为 6 项必填（每项需具体句子/短语）。
- **段 6**：完全沿用 v5，不改任何字。
- **段 7**：全新段，24h 内必填 5 大类前提错误表格。
- **web build 约束**：v6 段 4 显式加入 "web 改动必须 `npm run build`" 一句。

### v4 → v6 升级路径（一步到位）

1. 段 1–4：沿用 v4，不动。
2. 追加段 5（v6 6 项版本）+ 段 6（合并顺序表）+ 段 7（派工前提错误复盘）。
3. 段 4 加入 "web 改动必须 `npm run build`" 约束。

### v3/v2/v1 → v6 升级路径

不推荐逐步升级；直接整模板替换为 v6。v3 段 5/v2/v1 无反馈/合并/前提机制，无法兼容。

### v6 默认应用范围

- **W68 第 14 批已应用 v5**：A-1 ~ D-3 全部 15 agents。
- **W68 第 15 批及之后**：默认 v6；不再回退到 v5。
- **W71 派工**：默认 v6；尤其是 migration、PowerShell、plans 审计任务，必须把对应门禁原样写进 prompt。
- **W72 调研任务**：必须用 v6（避免 plans 自报偏差，必须有段 5 反馈 + 段 7 派工前提错误）。

## §7 v6 发布前自检清单

- [ ] 段 1 写清角色、范围、不变量、当前分支、基线 commit、依赖 agent。
- [ ] 段 2 列出文件、规模和禁止修改项。
- [ ] 段 3 含 plans grep 和 Alembic heads 前置检查。
- [ ] 段 3 写明双头必须报主指挥，不能私改。
- [ ] 段 4 含 PS 5.1 三项 binding 约束。
- [ ] 段 4 显式 "web 改动必须 `npm run build`，禁止 vite build 直跑"。
- [ ] 段 4 要求真 commit、测试结果和 BLOCKED 原因。
- [ ] 段 5 必填 6 项提示词齐全（有效段 / 多余段 / 新增段 7 候选 / 旧段升级建议 / 派工前提错误 / 锚点范式变化）。
- [ ] 段 5 含"6 项缺一不可"约束。
- [ ] 段 6 含合并链表格 + alembic 串单链位置列。
- [ ] 段 6 含"agent 不主动 merge / push"约束。
- [ ] 段 6 含跨 PR 部署 checklist（alembic 场景）。
- [ ] 段 7 必填 5 大类派工前提错误（alembic 串单链 / PS 5.1 / plans 真验证 / web `npm run build` / baseline 守恒）。
- [ ] 段 7 沉淀规则统一在 memory/。
- [ ] 段 7 24h 内未填视为派工违规。
- [ ] 文档任务完成 `git diff --check`。
- [ ] migration 合并完成后只有一个 head。
- [ ] plans 调研结果有 backlog docs 或 COMPLETED 真 commit。
- [ ] 段 5 反馈至少收到 N ≥ 5 agents 才能汇总升级 v7。
- [ ] 锚点范式变化显式追踪（段 5 第 6 项 + 段 7 第 5 类）。

## §8 v5 派工任务如何升级到 v6（兼容方式）

如果主指挥以前用 v5 派工（未含段 7 + 段 5 升级 6 项），需要升级到 v6：

1. **找到原 v5 prompt**：在派工频道搜索最近 7 天内的 prompt 文本。
2. **升级段 5**：从 3 项必填（有效段/多余段/偏离段）升级为 6 项必填（+ 新增段 7 候选 + 旧段升级建议 + 派工前提错误 + 锚点范式变化）。
3. **段 4 加 web build 约束**：补一句 "web 改动必须 `npm run build`，禁止 vite build 直跑"。
4. **追加段 7**：写 5 大类派工前提错误表格 + 沉淀规则 + 24h 限时。
5. **保留段 1–3/段 6 原文**：不要改任何字，否则派工追溯链断。
6. **追加时机**：派工发出后 1 小时内追加 v6 段 5 升级 + 段 7 给已开工 agent；agent 必须承认收到后再继续。

这种方式保证 v5 派工和 v6 派工可并存，不强制迁移。

## §9 v6 默认应用范围

- **W68 第 14 批已应用 v5**：A-1 ~ D-3 全部 15 agents 在派工时已含段 5 + 段 6。v6 在派工后由 D-1 沉淀，本批回溯性追加段 7 派工前提错误。
- **W68 第 15 批及之后**：默认 v6；不再回退到 v5。
- **W71 派工**：默认 v6；尤其是 migration、PowerShell、plans 审计任务，必须把对应门禁原样写进 prompt。
- **W72 调研任务**：必须用 v6（避免 plans 自报偏差）。

## 结语

v6 不是替换 v5，而是补一段让模板"派工前提错误显式沉淀 + 反馈颗粒度强制提升 + web build 命令约束 + 锚点范式显式追踪"。段 5 升级让 agent 反哺模板更具体，段 7 让主指挥派工的隐含前提事后被显式记录。这样 W68 第 15 批 + W71 + W72 派工可以基于"经过实战反馈的模板 + 经过派工前提沉淀的纪律 + 经过 web build 强约束的命令"启动下一轮，更可预测、更易追溯、更不易合并错。

---

**版本 v6，2026-07-24，W68 第 14 批 D-1 起草，主指挥合并后正式生效。**

**Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>**