# W88-C-2 npm audit 政策 (类 20.44)
#
# audit-level=high — `npm audit` 只在 high/critical 时以非 0 退出。
# moderate 仍会被列出 (可见), 但不阻断 CI。
#
# ⚠️ 严禁在此文件写 `omit=dev`
# 实测 (W88-C-2): .npmrc 的 omit=dev 会同时作用于 `npm install` / `npm ci`,
# 导致 vite / vitest / playwright / stylelint 等 devDependencies 完全不装,
# 直接摧毁 build 与测试链路。
# dev-only 豁免必须用**命令行 flag**在审计那一步单独指定:
#     npm audit --omit=dev
# 详见 docs/npm-audit-policy.md
audit-level=high
