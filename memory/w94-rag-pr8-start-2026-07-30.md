# W94 PR8 B 实施 agent — 起步记录 (MERGE-03 后, 2026-07-30)

> **任务**: PR8 W94 +0 → +20 (21 commits, 知识图谱深度联动)
> **基准**: main HEAD `034343f8a` (MERGE-03 收口, 锚点 459)
> **alembic head**: `090_add_rag_eval_report` (PR5 上链 ✓)
> **状态**: **worktree 已建, 起步 6 项 PASS, 派工 brief 3 处错配已据实上报**
> **重要**: PR8 是 10 PR 中**最后 1 个 alembic PR** — 完成后 087→088→089→090→091 串单链全部收口

## 起步 6 项实测 (W73 铁律, 据实上报)

### S1 git fetch + alembic heads verify
- ✅ `git fetch origin` 无新更新
- ✅ `python -m alembic heads` = `090_add_rag_eval_report (head)` — **派工 brief 期望 090 ✓ (不是 089, MERGE-03 已生效)**

### S2 Read 派工 v10/v11 + CHECKLIST + MERGE-03 + PR5 memory
- ✅ `docs/w72-prompt-paradigm-v10-2026-07-27.md` 515 行 (13 段)
- ✅ `docs/w72-prompt-paradigm-v11-2027-04.md` 76 行 (v10 + 6 项新增)
- ✅ `docs/rag/CHECKLIST.md` 91 行 (§F fallback + §H 真查)
- ✅ `memory/w91-merge-03-2026-07-30.md` 88 行
- ✅ `memory/w91-rag-pr5-restart-2026-07-30.md` 92 行
- ✅ plan `rag-quirky-otter.md` 838 行 (v1.2 + §12 §13 §14, 附录 B 含 DERIVE-19 reconcile)

### S3 Worktree 建好
- ✅ `git worktree add .claude/worktrees/rag-pr8 -b chore/w94-rag-pr8-knowledge-graph-2026-07-30 main`
- ✅ worktree tip = `034343f8a` (main HEAD, MERGE-03 完成)
- ✅ `python -m alembic heads` 在 worktree = `090_add_rag_eval_report (head)` ✓ 恰 1 head

### S4 pytest baseline collect
- ✅ `pytest tests/ --co -q --ignore=tests/test_w79_commercial_private_deployment_e2e.py` = **3208 tests collected in 6.51s**
- ✅ 与 MERGE-03 报告期望 (3164 + 22 PR3 + 22 PR5 = 3208) **精确一致, 0 偏差**

### S5 仓库实情真查 (派工 v11 §13 + DERIVE-18 + §12.3 路径三验证)

| 件 | brief 期望 | 实测 | 处置 |
|----|-----------|------|------|
| `app/models/kg_entity.py` 新增 | 新表 kg_entity | **`app/models/knowledge_entity.py` 已存在** — `KnowledgeEntity` (SPO 三元组: subject/predicate/object/condition/confidence/source_knowledge_ids/occurrence_count/embedding Vector(1024)) + `EntityCoOccurrence` (共现网络: entity_a_id/entity_b_id/knowledge_id/weight + UniqueConstraint) | **错配 #1 据实上报** — 详见下段 |
| brief 字段 `entity_name/entity_type/knowledge_id FK/vector/first_seen_at/last_seen_at/mention_count` | 7 字段 | 现有 `KnowledgeEntity` 是 **SPO 三元组模型**, 非 `entity_name/entity_type` 扁平实体模型; `source_knowledge_ids ARRAY(Integer)` 非单一 FK; `occurrence_count` ≈ `mention_count`; `TimestampMixin` 提供 created_at/updated_at ≈ first_seen/last_seen | **新建 `kg_entity` 表 (扁平实体, 补全关系)** — 与 PR5 `RAGEvaluationReport` vs 已有 `RAGEvaluation` 同款处置模式 |
| `app/services/entity_link_recall.py` 新增 | 实体链召回 | **不存在 ✓ 真新增** | 按 brief 实施 |
| `app/services/kg_embedding.py` 新增 | 实体向量 | **不存在 ✓ 真新增** | 按 brief 实施 |
| `knowledge_graph_service.py` 仅新增 `_add_entity_links` | 不改 `build_relations_for` | ✅ `build_relations_for` 在 L24 真存在 (500 行, 13 方法) | 按 brief: **仅文件底部新增, 0 类方法改** |
| `knowledge_service.py` 仅新增实体抽取钩子 | 不改老核心 | ⚠️ **Step 5 实体融合钩子已存在** (`knowledge_service.py:302-309` `EntityService.merge_entities_from_document`) + Step 6 Neo4j 图谱构建钩子已存在 | **错配 #2 据实上报** — 实体抽取钩子非"新增", 走"Step 5 之后追加 kg_entity 钩子"路径 |
| `hybrid_retriever.py` 仅新增 KG retrieval path | 不改 4 路开关默认 | ⚠️ **`_graph_search` 已存在** (L218-267, Neo4j 路径, score 0.7 固定) + `retrieve`/`_retrieve_impl` 4 路已含 graph | **错配 #3 据实上报** — KG path 非"新增", 走"文件底部新增 module-level 包装函数"路径 (PR3 bm25_service +3 def 模式) |
| `truncate_for_embedding` (PR1) 复用 | 存在 | ✅ `app/services/embedding_truncation_policy.py:12` | 按 brief 复用 |
| `kg_entity` 在 alembic | 无 | ✅ `grep -rln "kg_entit" alembic/versions/` **0 命中** — 091 真新增 | 按 brief 实施 |
| `knowledge_entities` 表 alembic 覆盖 | — | 仅 `030_qwen3_embedding_1024.py` 提及 (embedding 列改 1024), **建表走 `Base.metadata.create_all` lifespan** (非 alembic) | 091 **不动** knowledge_entities, 仅建 kg_entity |
| tests/rag/ 21 文件 | — | ✅ `test_pr8_e2e.py` **不存在** (无 basename 冲突, v11 新增 2 唯一性 ✓) | 新建 |
| tests/rag/conftest.py | — | ✅ 存在 (no-op `setup_db` 覆盖父级 autouse DB fixture) | e2e 走纯逻辑 + mock, 本机无 DB 可跑 |

### S6 PWA build 基线 (PR8 frontend=否, 件 3 三档沿用基线但必跑验证)
- 见收口回报表件 3 (DERIVE-01 已修 vite 7.3.6 降级)

## 派工 brief 错配 3 处据实上报 (类 20 #33/#35 + §12.3.4 拦截不擅自改)

**错配 #1: `app/models/kg_entity.py` "新增" — 实为已有 `knowledge_entity.py` 的补全关系**

- brief 写: 新增 `app/models/kg_entity.py` — kg_entity ORM (id / entity_name / entity_type / knowledge_id FK / vector / first_seen_at / last_seen_at / mention_count)
- 仓库实情: `app/models/knowledge_entity.py` 已有 `KnowledgeEntity` (**SPO 三元组**模型) + `EntityCoOccurrence` (共现网络)
- **不是替代**: brief 的 7 字段是**扁平实体**模型 (entity_name + entity_type + 单一 knowledge_id FK), 现有是**三元组**模型 (subject/predicate/object + source_knowledge_ids ARRAY)
- **处置**: 新建 `app/models/kg_entity.py` 的 `KGEntity` (扁平实体, 表 `kg_entities`) — 与 PR5 处置 `RAGEvaluationReport` vs 已有 `RAGEvaluation` **完全同款模式** (字段不同 → 互补非替代, alembic 仅建新表不动老表)
- **不擅自扩**: 091 **不动** `knowledge_entities` / `entity_co_occurrence` 两表 (它们走 lifespan create_all, 无 alembic 迁移)

**错配 #2: `knowledge_service.py` "仅新增实体抽取钩子" — 实体抽取钩子已存在**

- brief 写: 仅新增实体抽取钩子 (不改老核心函数体)
- 仓库实情: `knowledge_service.py:302-309` **Step 5 实体融合钩子已存在** (`EntityService.merge_entities_from_document`), `:311+` Step 6 Neo4j 图谱构建钩子也已存在
- **处置**: 在 `_run_analyze_and_embed` body 内 Step 5/6 **之后**追加 Step 7 kg_entity 钩子 (try/except 独立容错), **0 老核心函数签名改** — PR3 同款模式 (件 4a `^[+-]def` = 0)

**错配 #3: `hybrid_retriever.py` "仅新增 KG retrieval path" — graph path 已存在**

- brief 写: 仅新增 KG retrieval path (不改 4 路开关默认)
- 仓库实情: `hybrid_retriever.py:218-267` **`_graph_search` 已存在** (Neo4j 路径, 固定 score 0.7), `retrieve`/`_retrieve_impl` 4 路已含 graph
- **处置**: **0 类方法改** — 在文件底部新增 module-level 包装函数 (PR3 `bm25_service.py` +3 def 派工 brief 允许的同款模式), 不动 `_graph_search` / `retrieve` / `_retrieve_impl` / 4 路权重默认

**根因**: PR8 派工 brief 派生自 plan v1.0 §11.2, 未做 DERIVE-18 §13 仓库实情真查。plan v1.2 已对前端路径做修正 (§11.2 第 544 行), 但**后端 KG 资产未盘查** — 项目已有 5 个 KG 服务 (entity_service 402 行 / graph_retriever 188 行 / kg_query_service 266 行 / knowledge_graph_builder 289 行 / knowledge_graph_service 500 行) + 2 个 ORM 模型, brief 假设"KG 从 0 建"与实情不符。

**§12.3.4 遵守**: 3 处错配**全部据实上报 + 不擅自改 brief 语义**。处置方式均为"新增不动老" (与 brief 的 0 production code 双门控目标一致), 非"把 brief 的 A 改成 B 后继续"。

## 派工 v11 段 10 新 6 项 (据实)

1. **python -m alembic 命令形态**: PASS (全程 `python -m alembic`)
2. **pytest 白名单**: PASS (`--ignore=tests/test_w79_commercial_private_deployment_e2e.py`), baseline 3208 collected 与 brief 0 偏差
3. **派工 brief vs 实测必据实上报**: PASS (**3 处后端 KG 错配据实**, 不擅自扩也不擅自缩)
4. **docs-only PR 断言化**: N/A (本 PR 含后端 + alembic, 必有 e2e 断言)
5. **worktree 依赖基线自检**: alembic 090 ✓, pytest 3208 ✓, 件 3 见收口表
6. **5 件套守恒命令输出粘贴**: 收口回报必含 (v11 新增 6 结构化表)

## 派工 v10 段 7 错误 19 类 (PR8 specific)

- E01 alembic 多 head: 091 接 090 单链, merge 后必 verify
- E05 老核心函数误改: 件 4a 双门控 grep **6 个**老核心服务
- E06 HybridRetriever 误改: 0 类方法改 (错配 #3 处置)
- E07 锚点范式缺失: 件 5 前缀 `PR8 W94 +` ≥ 21
- E08 0 production code 违规: 件 4a 双门控
- E11 GIN 大表阻塞: 必用 `CREATE INDEX CONCURRENTLY` (089 二段式 DO $$ 探测模式复用)
- **E37 实体链召回 hit ≥ 25%**: 必测
- **E38 P95 ≤ 100ms**: 必性能断言
- **E39 实体数 ≥ 5000**: 必真查询 (本机无 DB 时据实上报为 mock/逻辑验证)

## 任务清单 (PR8 W94 +0 → +20, 21 commits)

按 brief 段 9 模板 21 commits。派工 v11 段 3 接受"锚点 +N 按真 commit 数报", 不擅自扩。

## 阻塞

- 阻塞状态: **0 commit (起点)**, 无阻塞
- 3 处 brief 错配已据实上报, 处置方式与 brief 0 production code 目标一致, 不阻塞实施
