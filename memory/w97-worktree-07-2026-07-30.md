# WORKTREE-07 #4 patch 抢救 + 删 (2026-07-30 W97)

> **类型**: WORKTREE-07 patch 抢救 + worktree/branch 删除
> **时间**: 2026-07-30
> **派工 ID**: WORKTREE-07 W97
> **主拍签字**: 默认建议 C (patch 抢救 + 删) 已签
> **协作前提**: WORKTREE-03 已实测 #4 并报 4 选项 (0 操作) / WORKTREE-05 已处置 10 个有 origin 备份的 worktree (0 删除)

---

## 0. #4 agent-w89-x14-swipe-bug 处置

**HEAD**: `7a1978c1e0bba6805bba414e69d9d25f4aeb278f`
**branch**: `claude/w89-x14-swipe-bug`
**origin**: **MISSING** (二次确认)
**磁盘**: 46 MB

---

## 1. Step 1 二次实测 (派工前提铁律 12 第 5 条)

```
HEAD:     7a1978c1e0bba6805bba414e69d9d25f4aeb278f
branch:   claude/w89-x14-swipe-bug
status:   ## claude/w89-x14-swipe-bug...origin/main [ahead 1, behind 165]
          (worktree 内 clean, 0 dirty)
log -1:   7a1978c1e refactor(w89): tests/e2e/ 重构
          (15 vitest → tests/unit/components/, 3 playwright → tests/visual/e2e/) (W89-P-10)
ahead:    1  (origin/main..HEAD)
behind:   165 (HEAD..origin/main)
parent:   3a1ab24b37313785ad9a84fbd620fabe09da6f6c
磁盘:     46 MB
```

### 1.1 与前序报告偏离 (派工 v10 §1.2 据实上报铁律)

| 维度 | WORKTREE-03 (较早) | WORKTREE-07 实测 | 偏离 |
|------|-------------------|-----------------|------|
| HEAD | `7a1978c1e` | `7a1978c1e` | 0 偏离 |
| branch | `claude/w89-x14-swipe-bug` | 同 | 0 偏离 |
| dirty | clean | clean | 0 偏离 |
| ahead | 1 | 1 | 0 偏离 |
| **behind** | **162** | **165** | **+3** (main 又前进 3 commits) |
| origin | MISSING | MISSING (0 hit / **274** refs) | 0 偏离 (refs 271→274) |
| 磁盘 | ~46MB (派工 brief) | 46 MB 实测 | 0 偏离 |

**派工 brief 写 "1 ahead / 162+ behind"** — 实测 **165 behind**, brief 的 `162+` 表述成立 (带 `+`), 据实记 165.

### 1.2 main 基线

- 本任务开始时 main HEAD = `329017a56` (WORKTREE-03 时 `f13636888`, WORKTREE-05 时 `f13636888`)
- main 在 W97 期间持续被其他 agent (merge-0x / derive-0x / pr-x) 推进

---

## 2. Step 2 二次确认 origin MISSING

```
git ls-remote origin "refs/heads/claude/w89-x14-swipe-bug"   → 0 行输出 (exit 0)
git ls-remote origin | grep -c "w89-x14"                     → 0
git ls-remote origin | wc -l                                 → 274 refs
git remote -v → origin git@github.com:gg320324492-lgtm/microbubble-agent.git (fetch/push)
```

**结论**: origin 上 **0 hit across 274 refs** — 该 branch 从未 push. remote **可连** (ls-remote 成功返回 274 refs), 即 push 备份技术上可行, 但主拍已签字选项 C (patch 抢救 + 删), 不走 push 路径.

---

## 3. Step 3 Commit 实质内容 (patch 抢救对象)

### 3.1 `git show 7a1978c1e --stat`

```
 memory/w89-p10-e2e-restructure-2026-07-30.md       | 124 +++++++++++++++++++++
 web/playwright.config.js                           |   5 +-
 .../components}/chat-topbar-3zone.spec.js          |   3 +-
 .../components}/desktop_admin_kb_monitor.spec.js   |   0
 .../components}/desktop_comment_v32.spec.js        |   0
 .../components}/desktop_drive_comments.spec.js     |   0
 .../components}/desktop_drive_mention.spec.js      |   0
 .../desktop_drive_v33_thumbnail.spec.js            |   0
 .../components}/desktop_drive_version_diff.spec.js |   0
 .../components}/desktop_drive_versions.spec.js     |   0
 .../components}/desktop_emoji_lazy.spec.js         |   0
 .../components}/kb-monitor-dashboard.spec.js       |   0
 .../components}/mobile_build_validation.spec.js    |   0
 .../components}/mobile_dark_v33.spec.js            |   3 +-
 .../components}/mobile_drive_comments.spec.js      |   0
 .../{e2e => unit/components}/nav-rail.spec.js      |   3 +-
 .../components}/thinking-mode-breadcrumb.spec.js   |   0
 .../e2e/mobile_push_notification.spec.js           |   0
 .../{ => visual}/e2e/mobile_swipe_gesture.spec.js  |   0
 .../{ => visual}/e2e/mobile_voice_input.spec.js    |   0
 20 files changed, 133 insertions(+), 5 deletions(-)
```

### 3.2 内容摘要 (W89-P-10)

`tests/e2e/` 命名误导重构:
- 实测 18 文件 = **15 vitest + 3 playwright** (派工 brief 估 14+2, 据实 +1/+1)
- 15 vitest → `web/tests/unit/components/`
- 3 playwright → `web/tests/visual/e2e/`
- 3 处 `__dirname` 相对路径同步 (子目录深 1 层多 1 个 `../`)
- `playwright.config.js` testMatch 扩展 (`.spec.mjs` → `.spec.m?js` + `e2e/` 前缀)

**e2e 验证 (commit message 自带)**:
- vitest: 1029 total = 1009 PASS + 19 failed + 1 skipped (与 main baseline 一致, 0 regression)
- 19 pre-existing failed 与该任务无关
- playwright `--list`: 232 tests in 35 files (worktree vs main 一致)
- a11y `--list`: 50 tests in 2 files (与 main 一致)

**派生类 20.58 (4 条, 该 commit 沉淀)**:
1. 派工前必 `head -3` + grep 分类 (实测 vs brief 必据实)
2. `git mv` 后必查 `__dirname` / `process.cwd()` 相对路径
3. config `testMatch` 必同步 + 双后缀 (`.spec.m?js` 正则)
4. pre-existing 死代码必据实上报 (3 playwright 在原位置从未跑过)

### 3.3 ⚠️ 该重构 **未落 main** (关键实测)

```
git cat-file -e HEAD:memory/w89-p10-e2e-restructure-2026-07-30.md  → ABSENT
git ls-tree HEAD web/tests/e2e/         → 18 文件仍在原位
git ls-tree HEAD web/tests/unit/components/  → 空 (不存在)
git ls-tree HEAD web/tests/visual/e2e/       → 空 (不存在)
```

**含义**: main 仍是重构前状态. 该 commit 是**唯一载体**, 且 origin MISSING → **patch 抢救是唯一保全手段**. 若只删不抢救 = 永久失传 (派工 v10 段 7 **E50** 命中).

---

## 4. Patch 备份路径 (双份, 1 份持久)

| 路径 | 命令 | 大小 | 行数 | 持久性 |
|------|------|------|------|--------|
| `/tmp/swipe-bug-patch-2026-07-30.patch` | `git show 7a1978c1e` | 17,721 B | 302 | ⚠️ **易失** (/tmp 重启即清) |
| `memory/w97-x14-swipe-bug-patch-2026-07-30.patch` | `git format-patch -1 --stdout` | 20,456 B | 350 | ✅ **持久** (git-tracked + push origin) |

### 4.1 为何额外做持久份 (派工 v10 段 7 E86 强化)

派工 brief 只要求 `/tmp/swipe-bug-patch-2026-07-30.patch`. 但 **`/tmp` 是易失存储** — 删了 worktree + branch + origin MISSING 后, `/tmp` 一清即三重丢失, E86 ("删 worktree 但 patch 未保存") 实质复现.

故本任务在 brief 基础上**增强**: 用 `git format-patch` (含完整 author/date/subject 元数据, 可 `git am` 直接还原) 落到 `memory/`, 随本 commit push origin. **抢救物与 main 同寿**.

### 4.2 还原方法

```bash
cd /e/microbubble-agent
git checkout -b claude/w89-x14-swipe-bug-restore 3a1ab24b3   # 原 parent
git am memory/w97-x14-swipe-bug-patch-2026-07-30.patch
```

或直接在当前 main 上 apply (需处理 rename 冲突, 因 main 的 `web/tests/e2e/` 18 文件仍在原位):

```bash
git apply --3way memory/w97-x14-swipe-bug-patch-2026-07-30.patch
```

---

## 5. 4 选项派主拍决策 (WORKTREE-03 已列, 本任务执行 C)

- **A** 保留 + 0 commit local-only (46 MB 占用)
- **B** push origin 备份 (remote 可连, origin MISSING → push 需创建 branch)
- **C** `git show` 取 patch + 删 worktree + branch + patch 入 memory ← **本次执行 (主拍已签)**
- **D** 主拍签字后 `git worktree remove --force`

**默认建议 C** — 已执行.

---

## 6. Step 5 删除实测

```
$ git worktree remove --force agent-w89-x14-swipe-bug
  (0 输出, exit 0)

$ git branch -D claude/w89-x14-swipe-bug
  Deleted branch claude/w89-x14-swipe-bug (was 7a1978c1e).

$ git worktree list | grep "agent-w89-x14-swipe-bug"   → 0 hit ✅
$ git branch | grep "claude/w89-x14-swipe-bug"         → 0 hit ✅
$ ls agent-w89-x14-swipe-bug/                          → No such file or directory ✅
$ git worktree list | wc -l                            → 164 (删前 165)
$ ls -d agent-*/ | wc -l                               → 12 (删前 13)
```

**注**: WORKTREE-05 实测 registry 160, 本任务实测删前 **165** — 期间其他 agent 又注册了 5 个 worktree. 据实记 165 → 164.

## 7. 磁盘释放实测

| 维度 | 删前 | 删后 | 释放 |
|------|------|------|------|
| `agent-w89-x14-swipe-bug` | 46 MB | 0 (不存在) | **46 MB** |
| `E:` 可用 | 3.1T (620G used, 17%) | 3.1T (619G used, 17%) | 1G 级波动 (含并行 agent 活动), 46MB 不改变百分比 |
| worktree registry | 165 | 164 | -1 |
| `agent-*/` 目录 | 13 | 12 | -1 |

**注**: 46 MB 相对 3.1T 可用空间无实质压力 (WORKTREE-05 同结论: 无删除压力). 本次删除的价值在于**清理 registry 噪音 + 完成 origin MISSING 的抢救闭环**, 非空间回收.

---

## 8. 5 件套守恒实测

| 件 | 命令 | 实测 | 结论 |
|----|------|------|------|
| 1 alembic 1 head | `python -m alembic heads` | `091_add_kg_entity (head)` | **PASS** — 1 head, 091 守恒 |
| 2 pytest ≥ 3230 | `pytest tests/ --co --ignore=...w79...` | **3230 tests collected** | **PASS** — 守恒 |
| 3 PWA build | 本 commit `--name-only \| grep -c '^web/'` = **0** | 0 diff | **PASS** — 守恒 |
| 4a 双门控 (0 老核心 diff) | 本任务仅新增 `memory/` 2 文件 | 0 `app/` 0 `web/src/` 0 `alembic/` | **PASS** |
| 5 锚点范式 | `git log --grep "w97-worktree-07" --oneline \| wc -l` | **1** | **PASS** |

**5/5 PASS**

### 8.1 件 2 pytest 实测

```
3230 tests collected in 3.67s
```
与 WORKTREE-05 实测 3230 完全一致 — 守恒.

### 8.2 件 5 锚点 grep 口径澄清 (据实上报)

派工 brief 写 `git log --grep "WORKTREE-07 W97"` → 实测 **0 hit**, 因 commit message 用小写 kebab 前缀 `chore(w97-worktree-07):` (与 W97 全批 commit 风格一致), 非大写空格式.

实测有效口径:
```
git log --grep "w97-worktree-07" --oneline | wc -l   → 1 ✅
git log --grep "worktree-07" -i --oneline | wc -l    → 1 ✅
```
**据实上报**: 锚点 grep = **1**, 但 brief 给的大小写口径 0 hit. 未为凑 brief 字面而改 commit message 风格 (类 20.13 "不擅自扩也不擅自缩").

---

## 9. 0 production code 改动铁律 守恒

本任务写操作 = **memory/ 2 个新文件** (本 md + patch) + **worktree/branch 删除**.

- ❌ 0 `app/` 改动
- ❌ 0 `web/src/` 改动
- ❌ 0 `alembic/` 改动 (087-091 未触碰)
- ❌ 0 backend service / model 改动
- ❌ 0 触碰 **#10 `agent-w90-x11-win32`** (必保留)
- ❌ 0 触碰 WORKTREE-05 的 10 个 worktree
- ❌ 0 触碰 WORKTREE-04 的 #8

---

## 10. 派工 v10 段 7 错误 19 类 — 新增实例沉淀

### E50 实例 #8 (origin MISSING 重要 commit 已抢救, 未裸删)
- **场景**: #4 origin MISSING + ahead 1, 派工签字删除
- **拦截动作**: 删前先 `git show` + `git format-patch` 双份备份, 并**实测确认该重构未落 main** (main 仍 18 文件在 `tests/e2e/`)
- **教训**: origin MISSING 的 branch 删除前, 必须**同时**验证 (a) patch 已生成 (b) 内容是否已被 main 覆盖. 仅 (a) 不足 — 不知道 (b) 就无法评估丢失代价

### E86 实例 #1 (patch 落 /tmp 易失 → 增强为持久份)
- **场景**: 派工 brief 指定 patch 落 `/tmp/`
- **真相**: `/tmp` 重启即清; worktree + branch + origin 三处已无, `/tmp` 一清 = 永久失传
- **动作**: 在 brief 基础上增强, `git format-patch` 落 `memory/` 并随 commit push origin
- **教训**: "patch 抢救" 的落点必须**至少与被删对象同寿**. 派工写 `/tmp` 时, agent 应据实增强而非机械照做 (类 20.13 "不擅自缩" 的正向应用)

### 类 20.13 实战 #24 (behind 数字 brief vs 实测)
- **场景**: brief 写 "1 ahead / 162+ behind", 实测 **165 behind**
- **动作**: 据实记 165, brief 的 `162+` 带 `+` 号故成立, 不算错配, 但记录实测值
- **教训**: 跨 agent 交接的 behind/ahead 计数随 main 推进而变, 每个 agent 必自测不抄

---

## 11. 锚点范式

- **base main HEAD**: `329017a56`
- **本任务**: +1 (仅 memory commit: 本 md + patch)
- **锚点**: **484 → 485** (W97 +1 据实)
- **0 production code 改动铁律**: **守恒**

---

## 12. 沉淀 + 主拍知悉项

1. 本 memory 文件 (#4 实测 + patch 抢救 + 删除实测)
2. `memory/w97-x14-swipe-bug-patch-2026-07-30.patch` (350 行, `git am` 可还原)
3. **主拍知悉**: 该 `tests/e2e/` 重构 (15 vitest → `tests/unit/components/`, 3 playwright → `tests/visual/e2e/`) **至今未落 main**. main 仍是 18 文件混在 `web/tests/e2e/` 的命名误导状态. 若要落地, 用 §4.2 还原方法; 因 base 老 (parent `3a1ab24b3`) 建议 `git apply --3way` 或 rebase, **不可直接 merge** (WORKTREE-05 §4.3 同类风险)
4. W97 派生 E50 实例 #8 + E86 实例 #1 + 类 20.13 实战 #24

---

**执行 agent**: WORKTREE-07
**派工 ID**: WORKTREE-07 W97
**状态**: patch 双份抢救完成 + worktree/branch 已删 + 46 MB 释放
**锚点范式**: 484 → 485 (W97 +1 据实, 仅 memory commit)
</content>
