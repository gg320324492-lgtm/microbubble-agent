# W97 WORKTREE-02 — 13 active worktree 详情清单 + 4 选项决策 (派工 v10 段 7 E50 强制)

> **类型**: WORKTREE-02 详情报主拍
> **时间**: 2026-07-30
> **状态**: 0 commit (list-only + memory) + 0 rm-rf — 报主拍逐个决策
> **派工 ID**: WORKTREE-02 W97
> **协作前提**: WORKTREE-01 已拦截 (派工前提错配), 本任务列真实详情

## 0. 任务边界

派工原文要求"列出 12 个 active worktree 详情 + 报主拍逐个决策". 实测发现:

- **dispatch 列表 12 个** (本任务目标)
- **WORKTREE-01 列出 12 个** (含 `agent-w91-wr1-play-icon` + `agent-w91-x22-viewport` + `agent-w90-x8-prod-chunk` 不在 dispatch 原列表)
- **实测 13 个 active linked worktree 在 E:/microbubble-agent/ + E:/ 根** (合并覆盖 dispatch + WORKTREE-01 两份)
- **本任务** 实测全部 13 个, 全部派主拍

派工 v10 段 7 E50 强制: **不擅自 rm -rf, 必报主拍签字**. 派工前提铁律 12 第 5 条: 必先实测再决策. 派工 v10 §1.2 据实上报铁律.

## 1. 13 个 worktree 5 项信息表 (派工前提实测)

**实测时间**: 2026-07-30 (与 WORKTREE-01 间隔约 1h30min, 此时 main HEAD = `c53444dfd`, 锚点 W97 +3)
**main HEAD**: `c53444dfd docs(w97): MERGE-06 W97 +1 主状态段 W95 → W97 同步 (FF 后补 commit)`

| # | worktree 路径 | branch | HEAD commit | dirty | ahead | behind | origin remote | merge 状态 | 默认建议 |
|---|---------------|--------|--------------|-------|-------|--------|---------------|------------|----------|
| 1 | `agent-w89-p2-mobile-comments` | `claude/w89-p2-mobile-comments-rerun` | `c90dc99c6` test(w89) mobile-comments 5 case 限流修复 | clean | 1 | 160 | **EXISTS** c90dc99c6 | 未合, 1 commit 独立 | **D 保留** (origin 备份 + 1 commit 待评估) |
| 2 | `agent-w89-p7-visual-debug` | `claude/w89-p7-visual-debug` | `83eb3ec59` docs(w89) P-7 visual snapshot 漂移真因查清 | clean | 1 | 158 | **EXISTS** 83eb3ec59 | 未合, 1 docs commit | **D 保留** (origin 备份 + docs 沉淀) |
| 3 | `agent-w89-p8-visual-sweep` | `claude/w89-p8-visual-sweep` | `9e4dade76` docs(w89) P-8 visual 全 sweep 35 spec + FAIL 清单 | clean | 1 | 158 | **EXISTS** 9e4dade76 | 未合, 1 docs commit | **D 保留** (origin 备份 + docs 沉淀) |
| 4 | `agent-w89-x14-swipe-bug` | `claude/w89-x14-swipe-bug` | `7a1978c1e` refactor(w89) tests/e2e/ 重构 (15 vitest → tests/unit/components/, 3 playwright → tests/visual/e2e/) | clean | 1 | 158 | **MISSING** (not on origin) | 未合, 1 commit (origin 无备份!) | **D 保留** (local-only, 重要测试重构, **慎删**) |
| 5 | `agent-w89-x21-collect-config` | `claude/w89-x21-collect-config` | `677dba793` fix(w89) vitest.config.js exclude Playwright spec | clean | 1 | 42 | **EXISTS** 677dba793 | 未合, 1 config fix | **D 保留** (origin 备份 + 配置修复) |
| 6 | `agent-w89-x22-desktop-drive` | `claude/w89-x22-desktop-drive` | `e903b8557` fix(w89) DesktopFileCommentsView 真修 route.query + dark mode | clean | 1 | 42 | **EXISTS** e903b8557 | 未合, 1 fix | **D 保留** (origin 备份 + 真修) |
| 7 | `agent-w89-x24-visual-flaky` | `claude/w89-x24-visual-flaky` | `adcbcc49e` fix(w89) visual-regression 等 UI locator 而非 networkidle | clean | 1 | 42 | **EXISTS** adcbcc49e | 未合, 1 fix | **D 保留** (origin 备份 + 真修) |
| 8 | `agent-w89-x9-grand-closure` | `claude/w89-x9-grand-closure` | `3a1ab24b3` merge: chore/w86-mini-16-docs-update | clean | **0** | 158 | **MISSING** (not on origin) | **已合入 main** (ahead=0) | **C/D 可删** (branch 已完成 merge) |
| 9 | `agent-w90-x7-swipe-bug` | `claude/w90-x7-swipe-bug` | `a545ed072` fix(w90) mobile_swipe_gesture test.use 移到 projects[].use | clean | 1 | 26 | **EXISTS** a545ed072 | 未合, 1 fix | **D 保留** (origin 备份 + 修复) |
| 10 | `agent-w90-x11-win32` | `main` ⚠️ | `33e9aa5e0` docs(w97) CLAUDE.md 主状态段 W95 → W97 同步 | clean (worktree 内), 但父目录有 12 untracked | 0 (= main) | 0 | n/a (branch=main) | **特殊: branch=main** | **A 必保留** (主仓 FF 后状态, **不能动**) |
| 11 | `agent-w90-x8-prod-chunk` (在 `E:/` 根, 不在本仓) | `claude/w90-x8-prod-chunk` | `911190824` docs(w90) X-8 prod build chunk 策略调研 (只调研不修) | clean | 1 | 26 | **EXISTS** 911190824 | 未合, 1 docs commit | **D 保留** (origin 备份 + 调研沉淀) |
| 12 | `agent-w91-wr1-play-icon` | `claude/w91-wr1-play-icon` | `5ff388b9f` fix(w91) RAGEvalPanel.vue Play → VideoPlay (P0 必修) | clean ⚠️ (WORKTREE-01 报 2 staged 现已 commit) | 1 | 7 | **EXISTS** 5ff388b9f | 未合, 1 P0 fix | **D 保留** (origin 备份 + P0 必修) |
| 13 | `agent-w91-x22-viewport` | `claude/w91-x22-viewport` | `85d293381` docs(w91) X-22 viewport race 拦截 (派工前提错配, 0 production code) | clean | 1 | 7 | **EXISTS** 85d293381 | 未合, 1 docs commit (拦截记录) | **D 保留** (origin 备份 + 派工拦截沉淀) |

### 1.1 关键事实摘要

- **总 ahead commits**: 12 个 (11 个 worktree 各 1 commit, 1 个 `agent-w89-x9-grand-closure` 0 ahead 因为已 merge main)
- **branch 不在 origin**: 2 个 — `claude/w89-x14-swipe-bug` (重要测试重构, **删前必报**) + `claude/w89-x9-grand-closure` (已合 main, **可安全删**)
- **branch=main 的特殊 worktree**: 1 个 — `agent-w90-x11-win32` (FF 后主仓状态, **必保留**)
- **有 dirty 改动**: 0 个 (WORKTREE-01 报 `agent-w91-wr1-play-icon` 2 staged files 现已 commit `5ff388b9f`)
- **总磁盘占用**: ~3.26GB (与 WORKTREE-01 一致)

### 1.2 派工前提铁律 12 第 5 条实战

**派工 v10 §1.2 据实上报铁律 — 必实测, 不信派工 brief 假设**. 派工原文"11 个 untracked `agent-*/` 根目录残留" + "W68 已合入 main" 假设全面错误. 实测:

| 派工假设 | 实测真相 |
|----------|----------|
| "untracked 残留" | 全部 13 个是 git linked worktree, 已注册 worktree list |
| "W68 已合入 main" | 全部 W89-W91 时代, **无 W68 worktree** 在 E:/microbubble-agent/agent-*/ 残留 |
| "无未提交改动" | 0 dirty (WORKTREE-01 报 `w91-wr1-play-icon` 2 staged 现已 commit) |
| "2.71GB 残留" | 实测 ~3.26GB |

**WORKTREE-01 已拦截, 本任务不再重复** — 详见 `memory/w97-worktree-01-2026-07-30.md` 第 1-3 段.

## 2. 4 选项决策 (派主拍)

派工 v10 段 7 E50 强制: **不擅自决策, 报主拍**. 列出 4 选项 + 默认建议.

### 选项 A: 保留全部 13 个

- 接受 ~3.26GB 磁盘占用
- 适合: 主拍尚未评估每个 commit 价值, **优先**保护数据
- **本任务默认建议**: 部分保留 (#10 `agent-w90-x11-win32` 必保留, #8 `agent-w89-x9-grand-closure` 可保留)

### 选项 B: 全部 `rm -rf` (批量删)

- 释放 3.26GB
- **风险**: 销毁 12 个 worktree + 1 个 main worktree + 丢失 #4 `claude/w89-x14-swipe-bug` (origin 无备份, **重大损失**)
- **本任务默认建议**: **强烈不建议** (E50/E69 错误实战)

### 选项 C: 逐个决策 (主拍逐个签字)

- 列出每个 worktree 详情 + 默认建议 (本任务清单 1)
- 主拍逐个拍板: 删 / 保留 / 评估
- 适合: 主拍需要逐个审查 commit 内容
- **本任务默认建议**: **推荐** (派工 v10 段 7 E50 标准流程)

### 选项 D: 按 status 分组 (智能默认)

- 自动按 ahead/origin/dirty 分组:
  - **D1 必保留**: branch=main (`#10 agent-w90-x11-win32`) + 0 ahead (`#8 agent-w89-x9-grand-closure` — 已合 main, 但工作产物仍在 origin missing, **需评估**)
  - **D2 origin 备份**: 9 个有 origin 备份 + 1 commit ahead, 删后 commit 仍可从 origin 拉回 (但 working tree 丢失)
  - **D3 local-only**: `#4 agent-w89-x14-swipe-bug` (origin MISSING, **删前必报**)
  - **D4 dirty**: 0 个
- **本任务默认建议**: D1 全保留 + D3 必报 + D2 派主拍逐个签

### 2.1 每个 worktree 处置建议 (主拍参考表)

| # | worktree | 默认建议 | 主拍必看原因 |
|---|----------|----------|--------------|
| 1 | agent-w89-p2-mobile-comments | **保留** (origin 备份) | W89-P-2 限流修复 |
| 2 | agent-w89-p7-visual-debug | **保留** (origin 备份) | W89-P-7 visual 漂移真因 (docs) |
| 3 | agent-w89-p8-visual-sweep | **保留** (origin 备份) | W89-P-8 visual 全 sweep 35 spec (docs) |
| 4 | **agent-w89-x14-swipe-bug** | **必报, 不擅自删** ⚠️ | **origin MISSING**, local-only 重要测试重构 |
| 5 | agent-w89-x21-collect-config | **保留** (origin 备份) | W89-X-21 vitest.config.js exclude Playwright |
| 6 | agent-w89-x22-desktop-drive | **保留** (origin 备份) | W89-X-22 DesktopFileCommentsView 真修 |
| 7 | agent-w89-x24-visual-flaky | **保留** (origin 备份) | W89-X-24 visual-regression 等 UI locator |
| 8 | **agent-w89-x9-grand-closure** | **可删** (已合 main) | ahead=0, 但 origin MISSING, **删前主拍签字** |
| 9 | agent-w90-x7-swipe-bug | **保留** (origin 备份) | W90-X-7 mobile_swipe_gesture test.use 修复 |
| 10 | **agent-w90-x11-win32** | **必保留** ⚠️ | branch=main, **主仓状态**, 不能动 |
| 11 | agent-w90-x8-prod-chunk | **保留** (origin 备份) | W90-X-8 prod build chunk 调研 (只调研不修) |
| 12 | agent-w91-wr1-play-icon | **保留** (origin 备份) | W91-WR-1 RAGEvalPanel.vue Play → VideoPlay (P0 必修) |
| 13 | agent-w91-x22-viewport | **保留** (origin 备份) | W91-X-22 viewport race 拦截 (派工前提错配, 0 production code) |

## 3. WORKTREE-01 vs WORKTREE-02 对比

| 维度 | WORKTREE-01 (1h 前) | WORKTREE-02 (本任务) |
|------|---------------------|---------------------|
| dispatch 描述 | "11 untracked `agent-*/` 残留" + "W68 已合入 main" | "12 active worktree 详情 + 报主拍" |
| dispatch 列表 | (无明确列表, 推断 11) | 12 (agent-w89-p2/p7/p8/x14/x21/x22/x24/x9 + agent-w90-x7/x11 + agent-w91-wr1) |
| 实测 worktree 数 | 12 + `agent-w90-x8-prod-chunk` (13) | 13 (合并覆盖 dispatch + WORKTREE-01) |
| `agent-w91-wr1-play-icon` dirty | 2 staged (memory + test_play_to_video.py) | **0 dirty (已 commit `5ff388b9f`)** |
| `agent-w90-x11-win32` branch | (未明) | **branch=main** ⚠️ (FF 后状态) |
| 决策 | 拦截 (0 commit 0 rm-rf, 仅 memory 沉淀) | 报主拍 (4 选项 + 13 个建议) |
| 累计拦截实例 | E50 #5 | (无新增拦截, 仅 13 实测详情) |

## 4. 派工 v10 段 7 错误 19 类 — 新增实例沉淀

### E50 实例 #6 (WORKTREE-02 据实上报 + 详情清单)

- **场景**: dispatch "12 active worktree 详情" + "3.26GB" 假设, 但未列 13 个全 + 未明 dispatch 列表与 WORKTREE-01 列表差异
- **真相**: 13 个 worktree 全部实测, 1 个 `agent-w90-x11-win32` 特殊 (branch=main), 1 个 `agent-w91-wr1-play-icon` dirty 已 commit, 2 个 branch 在 origin missing
- **教训**: 派工"列详情"必须含 (a) branch (b) HEAD (c) dirty (d) ahead/behind (e) origin 状态 5 项, 缺一不可. dispatch 只列路径名 = 不充分

### E69 风险预判

- **场景**: 主拍默认签字全部 `rm -rf` 触发
- **拦截理由**: 2 个 branch 在 origin missing (其中 `claude/w89-x14-swipe-bug` local-only 含重要测试重构), 1 个 branch=main 是主仓状态, 0 dirty 也不能批量删
- **缓解**: 本任务列 4 选项 + 13 个默认建议, 派主拍逐个签字

### E70 风险预判

- **场景**: `agent-w91-wr1-play-icon` WORKTREE-01 报有 2 staged files
- **实测**: 现已 commit `5ff388b9f`, dirty 空, **0 风险**
- **缓解**: WORKTREE-01 拦截已起效, 本任务 1.5h 后重测 dirty 状态变化

### E71 风险预判

- **场景**: 派工"按 origin 删, 没 origin 备份保留"
- **实测**: 2 个 branch origin missing (其中 1 个 `claude/w89-x14-swipe-bug` local-only), 派工若按 origin 删会**误删** local-only 重要 worktree
- **缓解**: 本任务列出 origin 状态列, 派主拍决策时必看

### E50 跨批次教训汇总 (本任务 1 增量)

- W82 C-2 派工 (锚点范式第 300 守恒): 363 branches + 209 worktree 清理, 仅清理确实 stale (`.git` 不存在或指向不存在路径)
- W97 WORKTREE-01 #5: 派工描述失误, 拦截 rm -rf
- **W97 WORKTREE-02 #6: 派工列表不全 + 未列 5 项详情, 报主拍 4 选项**

## 5. 5 件套守恒

| 件 | 实测 |
|----|------|
| 1 head (alembic) | **PASS** — 本任务 0 commit, main HEAD `c53444dfd` 不变, 锚点 W97 +3 (merge-05 + merge-06 + claudemd-append) |
| 2 pytest | **PASS** — 不动 tracked files, 0 测试影响 |
| 3 PWA build | **PASS** — 不动 web/dist |
| 4a 双门控 (0 老核心 diff) | **PASS** — 仅 memory 沉淀, 0 老核心改动 |
| 5 锚点范式 | **PASS** — 0 commit, 锚点不变 |
| **5/5 PASS** | **+ 1 详情报主拍 (派工 v10 段 7 E50 强制)** |

## 6. 0 production code 改动铁律 守恒

本任务 **0 commit 0 rm-rf 0 production code 改动** — 完全守恒.

派工前提铁律 12 (W82 实战) + 派工 v10 段 7 E50 强制不擅自删, 实战 4 选项 + 13 个默认建议报主拍决策.

## 7. 沉淀

- 本 memory 文件 (13 worktree 详情 + 4 选项 + 派主拍)
- 派主拍 (建议选项 C 逐个决策, 或 D 按 status 分组)
- 锚点不变 (0 commit)
- W97 派生类 20.13 实战 #22 (派工"列详情"必含 5 项 + origin 状态)

---

**执行 agent**: WORKTREE-02
**派工 ID**: WORKTREE-02 W97
**状态**: 0 commit 0 rm-rf, 仅 memory 沉淀 + 派主拍决策 (4 选项 + 13 个默认建议)
**锚点范式**: 不变 (主仓 +0, 仍为 `c53444dfd` W97 +3)
