# W89-P-9 真 CI 触发 + PLAYWRIGHT_TEST_TOKEN 部署留口 (2026-07-30)

> **派工 v6 §5 反馈**: **类 20.57** 新增 — "Playwright CI secret 部署必含: 3 secret + gh CLI 验证 + 文档化"
>
> **0 production code 改动铁律 1/1 守恒**: 仅 docs/ci-secret-setup.md + docs/ci-trigger.md + tests/ci_trigger/ + memory/ 本文件, 不改 app/web/alembic/nginx/docker/commercial/.github/workflows/playwright.yml.

## 任务背景

W89-P-3 (`a765adf2f`, commit on `claude/w89-p3-playwright-ci` 分支, 未合并 main) 写了 `.github/workflows/playwright.yml` (a11y hard fail + visual continue-on-error first, 2 job 分跑). 本任务:
1. 文档化 3 个 secret (TOKEN/USERNAME/PASSWORD) 部署命令
2. 文档化真 CI 触发方式 (workflow_dispatch + push + PR)
3. e2e 验证文档存在性 + 内容完整性
4. 诚实报告本机限制 (gh CLI 未装, 真部署留主指挥)

## 实操流水

### 步骤 1 - worktree 校核

```bash
git worktree add -b claude/w89-p9-ci-trigger ../agent-w89-p9-ci-trigger main
cd ../agent-w89-p9-ci-trigger
# base = main @ 3a1ab24b3 (W86 mini-16 docs update merge)
```

### 步骤 2 - gh CLI 检查

```bash
$ which gh
(no output)  # gh CLI 未装
$ gh --version
bash: gh: command not found
```

**结论**: 真部署 secret + 真 CI 触发留主指挥手动执行.

### 步骤 3 - 写 docs/ci-secret-setup.md

3 secret 表格 + 部署命令 + 拿 token 的 API 调用模板 + 本机限制报告 + 5 条铁律.

### 步骤 4 - 写 docs/ci-trigger.md

3 种触发方式 (workflow_dispatch / push / PR) + 2 job 预期行为表 + 本机限制表 + 主指挥合并清单.

### 步骤 5 - 写 tests/ci_trigger/test_secret_setup.py

7 个 e2e (派工 brief 估 3, 实 7, 多 4 个 robustness):
1. `test_ci_secret_setup_doc_exists` — ci-secret-setup.md 存在
2. `test_ci_trigger_doc_exists` — ci-trigger.md 存在
3. `test_secret_names_documented` — 3 secret 名全在 secret setup doc
4. `test_ci_trigger_doc_has_dispatch` — workflow_dispatch + gh workflow run 命令在 trigger doc
5. `test_ci_trigger_doc_acknowledges_gh_cli_missing` — gh CLI 限制诚实报告
6. `test_class_20_57_referenced` — 类 20.57 必出现在两个 doc
7. `test_no_secret_in_git_tracked_files` — 防御性检查, git tracked doc 不含真实 JWT (`eyJxxxxx` 正则)

**关键**: 必须用 `SKIP_DB_SETUP=1` (conftest.py 重型 import 会触发 DB 连接, 本机无 TEST_DB).

```bash
SKIP_DB_SETUP=1 python -m pytest tests/ci_trigger/ -v --override-ini="addopts="
# 7 passed in 0.05s
```

### 步骤 6 - 边界复检

```bash
git diff main..HEAD --name-only
# docs/ci-secret-setup.md
# docs/ci-trigger.md
# tests/ci_trigger/__init__.py
# tests/ci_trigger/test_secret_setup.py
# memory/w89-p9-ci-trigger-2026-07-30.md (本文件)
```

**未动** (派工 v6 §5 反馈 类 20.57 #4 守恒):
- `.github/workflows/playwright.yml` (W89-P-3 已写, 本任务不动)
- `app/`, `web/src/`, `alembic/versions/`, `nginx/`, `docker/`, `web/dist/`, `commercial/`
- 任何 secret / env file (不入 git)

## 类 20.57 新增铁律 (派工 v6 §5 反馈 实战沉淀)

> **派工 v6 §5 反馈 类 20.57**:
> **"Playwright CI secret 部署必含"** — 5 条铁律:

1. **3 secret 名 (TOKEN/USERNAME/PASSWORD) 必文档化** — 仅 TOKEN 必传 (workflow 直注), USERNAME/PASSWORD 留口备用 (未来 workflow 改回 "动态登录" 模式时用).
2. **gh CLI 必先验证** — `which gh` + `gh auth status` 必须先过, 否则部署会半途失败. 本任务据实上报: 本机 gh 未装.
3. **真 token 拿法必含 login API 调用模板** — curl + json parse 必写出, 留主指挥直接复制. JWT 默认 24h TTL 考虑用 long-lived API key.
4. **本机限制必诚实报告** — gh 未装 / 账号未验证 / token 未实测, 据实上报不伪造可达性. 派工 v6 §1.2 "Status 段必真验证" 守恒.
5. **CI 触发必配 workflow_dispatch** — 仅 push 触发不够, 留手动触发兜底 (紧急 fix / 手动验证 / 历史 commit 重测).

## 派工 v6 §5 反馈 类 20 累计

本任务新增类 20.57. 累计类 20 实战: W72-W89 累计 57 实例 (本任务 +1).

## 锚点范式

- base main @ `3a1ab24b3` = 锚点 338
- 本任务: +1 (单 commit 文档化, 不涉及 production code) → 339

**注**: W89-P-3 的 +1 (workflow 接入) 算在 P-3 任务内, 已合并 `a765adf2f`. P-3 任务未实际 merge 到 main (分支领先, 待主指挥合并).

## 主指挥合并清单 (W89-P-9 移交)

```bash
# 1. 合并 W89-P-3 branch (含 .github/workflows/playwright.yml + web/package.json scripts + tests/playwright_ci/)
git checkout main
git merge --no-ff claude/w89-p3-playwright-ci -m "merge(w89): P-3 Playwright CI 接入 (a11y + visual 2 job)"
git push origin main

# 2. 合并 W89-P-9 branch (本任务: docs + tests + memory)
git merge --no-ff claude/w89-p9-ci-trigger -m "merge(w89): P-9 真 CI 触发文档化 + secret 部署留口"
git push origin main

# 3. 部署 3 secret (详见 docs/ci-secret-setup.md §"部署命令")
gh secret set PLAYWRIGHT_TEST_USERNAME --body "xiaoqi_testbot"
gh secret set PLAYWRIGHT_TEST_PASSWORD --body "testbot_pass_2026"
gh secret set PLAYWRIGHT_TEST_TOKEN --body "<token>"

# 4. 触发 workflow_dispatch 验证
gh workflow run playwright.yml --ref main

# 5. 看 CI 日志
gh run list --workflow=playwright.yml --limit 5
```

## 留 W89+

- 主指挥手动 gh secret set 3 个 secret
- 主指挥手动 merge W89-P-3 + W89-P-9 branch 到 main
- 真 workflow_dispatch 触发 + CI 日志解读
- visual job baseline 稳定后撤掉 `continue-on-error: true` 改 hard fail
- a11y 跑通后视情况启用 xiaoqi_testbot 真登录态 (类 20.25 续)

## 相关文档

- `docs/ci-secret-setup.md` — secret 部署详细指南 (本任务新)
- `docs/ci-trigger.md` — 真 CI 触发指南 (本任务新)
- `tests/ci_trigger/test_secret_setup.py` — 7 PASS e2e (本任务新)
- `.github/workflows/playwright.yml` — workflow 定义 (W89-P-3 写, 在 `claude/w89-p3-playwright-ci` 分支, 待合并)
- `memory/w89-p3-playwright-ci-2026-07-30.md` — W89-P-3 workflow 设计沉淀 (P-3 任务)

## 派工 v6 锚点范式守恒

- W89 第 1 批 (含 P-3 + P-9): 0 production code 改动铁律 守恒
- 累计 W89 第 1 批锚点: P-3 +1 (待合并) + P-9 +1 (本任务) = 339 + 339 (待合并) = 待主拍
- 派工前提铁律 12 + 类 20 累计 57 实例 (本任务 +1) 守恒