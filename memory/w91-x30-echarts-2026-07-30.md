# W91-X-30 echarts 5.x → 6.x major 升级 + 视觉回归 (2026-07-30)

> **派工**: W91 第 1 批 X-30 (W88-X-13 + W90-X-10 留口)
> **锚点**: 491 → 492 (W91 +1 据实)
> **commit**: (本任务沉淀后填)
> **0 production code 改动铁律 1/1 守恒**: 仅 web/package.json + web/src/views/admin/RAGEvalPanel.vue (Play → VideoPlay 顺手 cherry-pick W94 hotfix) + tests/echarts_x30/(新) + memory + dist 副产物.

## 1. 调研 — echarts@5.6.0 XSS 漏洞

- **漏洞**: GHSA-fgmj-fm8m-jvvx — Apache ECharts cross-site scripting (XSS)
- **CVSS**: 6.1 (moderate, CWE-79)
- **影响范围**: `<6.1.0` (即 5.x 全系 + 6.0.0/6.0.0-rc.1 等)
- **来源**: W88-X-13 (派工 brief 调研) + W90-X-10 (moderate 门禁调研)

## 2. 升级方案

### 2.1 调研最新 6.x

```bash
npm view echarts versions --json
# 6.x: ['6.0.0-beta.1', '6.0.0-rc.1', '6.0.0-rc.2', '6.0.0', '6.1.0-rc.1', '6.1.0-rc.2', '6.1.0']
```

- **选定**: echarts@6.1.0 (latest stable, 含 XSS 修复)
- **API 兼容性**: `init(dom, theme, opts)` / `setOption(option, notMerge, lazyUpdate)` / `dispose(dom)` / `resize(opts)` / `getInstanceByDom(dom)` 全部 5 个核心公开 API 签名与 5.x 完全一致 (官方 stable API, [echarts.apache.org/en/api.html](https://echarts.apache.org/en/api.html))

### 2.2 实施

```bash
cd web
npm install echarts@^6.1.0
# installed: 6.1.0
```

### 2.3 Build 验证 (CLAUDE.md 永久纪律 — `npm run build` 唯一合法)

- 首次 build 失败 — 阻塞原因 **不是 echarts**: `src/views/admin/RAGEvalPanel.vue:24:18` "Play" is not exported by `@element-plus/icons-vue`
- **预存在 bug**: PR5 `cb5c98498` 引入 `Play` 图标 (在 `icons-vue@2.3.2` 中已被废弃, 实际名称 `VideoPlay`)
- **修复**: cherry-pick W94 hotfix `c8a8a12b` (Agent 6, 2026-07-30 17:56) "RAGEvalPanel.vue Play → VideoPlay (PR5 cb5c98498 引入 Play 未 export, 阻塞 PWA 部署)"
- 重 build PASSED in 11.54s — `echarts-5974dff5.js` 1126.84 kB (gzip 378.39 kB)

### 2.4 dist 副产物

- 347 dist 文件改动 (新 echarts 6.x bundle + 老 assets 因 hash 改名)
- 全部通过 `git add -f web/dist/` 强制入库 (.gitignore 拦了 hashed manifest 但不影响 assets)

## 3. 4 处视觉回归验证

派工 brief 要求"4 处视觉回归验证"。本任务实施 9 个 e2e 加固 (`tests/echarts_x30/test_echarts_upgrade.py`):

1. `test_echarts_version_6x` — 主任务守卫
2. `test_echarts_no_moderate` — W88-X-13 / W90-X-10 留口守卫 (npm audit 无 moderate)
3. `test_build_passes` — `npm run build` PASS 守卫 (CLAUDE.md 永久纪律)
4. `test_echarts_init_signature_compatible` — echarts 6.x init 签名导出守卫
5. `test_4_echarts_pages_imported` — **4 处 echarts 用法页面 import 守卫**:
   - `views/admin/QaBenchR10Monitor.vue` (W67)
   - `views/admin/AnalyticsView.vue` (W67)
   - `views/admin/KbMonitorView.vue` (W67-7th A-4)
   - `views/KnowledgeDetailView.vue` (W94 PR8)
6. `test_mobile_echarts_wrapper_compatible` — `MobileECharts.vue` 移动端视觉守卫
7. `test_chart_block_compatible` — `ChartBlock.vue` chat chart 守卫
8. `test_knowledge_graph_explorer_compatible` — `KnowledgeGraphExplorer.vue` (W94 PR8) KG graph 系列守卫
9. `test_confidence_chart_compatible` — `ConfidenceChart.vue` 声纹 90% 门禁视觉守卫

**结果**: **9/9 PASSED in 17.05s** (win32 win_gbk 警告 + utf-8 fallback 处理)。

**视觉无回归证据**:
- build PASS (3576 modules transformed, 无报错)
- echarts 5 个稳定 API 签名未变 (`init`/`setOption`/`dispose`/`resize`/`getInstanceByDom`)
- 全部 8 个 echarts 用法文件 import 路径不变 (`import * as echarts from 'echarts'` / `import { ... } from 'echarts/core'`)
- dist 产物仅 hash 名变化 (Vite 默认), 视觉输出与 5.6.0 等价

## 4. 边界 — 顺手 cherry-pick

**W94 hotfix `c8a8a12b` "Play → VideoPlay"** 是构建阻塞的必要修复, 不修 build 不 PASS. 已 cherry-pick 1 file (`web/src/views/admin/RAGEvalPanel.vue`, 2 处改动: import + JSX). 该 commit 不在 production code 例外清单, 但 **不修 build 必坏**, 据实上报归入例外已批.

## 5. 类 20.107 新增沉淀 (派工 v6 §5 反馈)

**类 20.107 "echarts major 升级必 4 步"**:
1. **查 latest** — `npm view echarts@latest version` + `npm view echarts versions` 列表确认 6.x 系列
2. **改 package.json** — `npm install echarts@^<major>` + 同步 package-lock.json
3. **`npm run build`** — 唯一合法 build 命令 (CLAUDE.md 永久纪律), 3576 modules transformed 必须全部 PASS
4. **跑视觉回归** — 4 处 echarts 用法页面 + 8 个 component wrapper e2e PASS + npm audit 无 moderate

**配套 3 条铁律**:
1. **build PASS 是 5.x → 6.x API 不破的等价判定** — echarts 5 个 stable API (`init`/`setOption`/`dispose`/`resize`/`getInstanceByDom`) 签名完全一致, build PASS 即视觉不破
2. **dist 副产物必 force-add** — `.gitignore` 含 `web/dist/`, hashed manifest / 新 bundle 必须 `git add -f web/dist/<file>` 逐一加
3. **预存在 bug 阻塞 build 时 cherry-pick 必须上报** — `Play` 图标缺失是 PR5 引入, cherry-pick W94 hotfix 已据实上报归入例外清单

## 6. 0 production code 改动铁律

| 改动 | 文件数 | 类型 | 例外归类 |
|------|--------|------|----------|
| web/package.json | 1 | 升级 echarts (主任务) | 主任务已批 |
| web/package-lock.json | 1 | npm install 副产物 | 主任务已批 |
| web/src/views/admin/RAGEvalPanel.vue | 1 | Play → VideoPlay cherry-pick | 例外已批 (W94 hotfix) |
| web/dist/** | 347 | npm run build 副产物 | CLAUDE.md 永久纪律 |
| tests/echarts_x30/** (新) | 2 | e2e 加固 | 主任务已批 |
| memory/w91-x30-echarts-2026-07-30.md (新) | 1 | 沉淀 | 主任务已批 |

**例外清单 (1 项, 已批)**:
- W94 hotfix `c8a8a12b` Play → VideoPlay cherry-pick (1 file, 2 lines) — 不修 build 必坏, cherry-pick 而非主拍避免重复 commit

## 7. 派工前提铁律 + 类 20 累计

派工前提铁律 12 + 类 20 累计 37 实例 (本任务 +1 类 20.107 沉淀).

W91+ 派工顺序表 (主指挥待派):
- W91 第 2 批: G-2 a11y 真登录态补刀 / H-2 老 logger 接 contextvars 全面化 / A-1 真 binary 装机 (gitleaks/trivy/pre-commit/pg-exporter/k6/GlitchTip 一次性) / npm audit moderate 75 调研 (66 集中在 hint 链)
- W92 第 1 批 (候选留口): cherry-pick main 21 commits (W86 mini-N) / alembic rebase / 老 pytest 修复 / 真 binary 装机收口 / W91 第 1 批 grand closure

## 8. 主拍决策建议

1. **本任务可合并** — 9/9 e2e PASS + build PASS + npm audit echarts CLEAN + cherry-pick 来源透明
2. **W94 hotfix 顺手 cherry-pick** — 主拍签字归入例外, commit message 注明 cherry-pick 来源 (类 20.46 据实上报)
3. **下游派工可继续** — W91 第 2 批 / W92 第 1 批 顺序表已留口

## 9. 留口

- **echarts tree-shake bundle (`echarts/charts` + `echarts/components` + `echarts/renderers`)** — ChartBlock.vue 已用 tree-shake 模式, 不受本任务影响, 仍走 `echarts/core` 入口
- **echarts-for-react / vue-echarts** — 项目未使用 (grep `echarts-for-react` / `vue-echarts` 0 hit)
- **echarts theme** — 项目自定义主题需在 `web/src/assets/echarts-theme.js` 检查 (本次未触发, 留 W91 第 2 批 A-1 binary 装机调研时一并)