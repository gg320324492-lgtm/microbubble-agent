# W91-X-24 alembic 091 全量修 (2026-07-30)

## 背景

W91-X-16 修了 1 行 `tests/alembic/test_pre_commit_hook_passes.py:142` (087 → 091), 但 X-16 仅修了
`test_actual_alembic_head_count_is_one` 这 1 个 anchor 断言, 没扫 `tests/rag/` 和 `tests/test_w85_hotfix_*`
里其它引用 087/089/090 的 e2e head 断言。结果 W91+ alembic head 升到 `091_add_kg_entity` (W94
[merge-04] PR8 知识图谱深度联动 commit `855130e1b` 落地) 后, 多个 e2e 测试因 "head 不是 087/089/090"
红, 但派工 brief 没列具体修复范围。

**本任务**: 全量扫 `tests/**/test_*.py` 引用 `087_add_knowledge_original_parent_id` /
`089_gin_trgm_tsvector` / `090_add_rag_eval_report` 的 e2e, 逐 file 1 commit 修到 091。

## 实测 base 状态

```bash
cd /e/agent-w91-x24-alembic-all
python -c "from alembic.config import Config; from alembic.script import ScriptDirectory; \
c=Config(); c.set_main_option('script_location','alembic'); \
s=ScriptDirectory.from_config(c); print('heads:', s.get_heads())"
# heads: ['091_add_kg_entity']  (W94 merge-04 后的当前 head)
```

```bash
grep -rln "087_add_knowledge_original_parent_id\|089_gin_trgm_tsvector\|090_add_rag_eval_report" tests/ --include="*.py"
# tests/alembic/test_pre_commit_hook_passes.py
# tests/rag/test_pr2_e2e.py
# tests/rag/test_pr2_orphan_audit.py
# tests/rag/test_pr3_e2e.py
# tests/rag/test_pr5_e2e.py
# tests/rag/test_pr8_e2e.py    ← 已 091 (W94 PR8 merge 顺手对齐), 0 commit
# tests/test_w85_hotfix_knowledge_column_e2e.py
# 6 files need fix
```

## 6 file 修法 (每个 1 commit)

| # | file | 改前 | 改后 | 性质 |
|---|------|------|------|------|
| 1 | `tests/alembic/test_pre_commit_hook_passes.py:142` | `lines[1] == "087_add_knowledge_original_parent_id"` | `lines[1] == "091_add_kg_entity"` | anchor 锚点直接换 |
| 2 | `tests/rag/test_pr2_e2e.py:213 test_alembic_19_heads_shows_088` | head 含 `088_add_knowledge_chunk` | head 含 `091_add_kg_entity` + 088 仍存在 + 088.down_revision=087 | 历史属性保留, head 改 091 |
| 3 | `tests/rag/test_pr2_orphan_audit.py:123 test_orphan_task_32_dry_run_via_subprocess` | `088 or 087 in output` | `any(087..091 in output)` (5 个全允许) | 兼容 PR2/PR3/PR5/PR8 任意 chain 长度 |
| 4 | `tests/rag/test_pr3_e2e.py:245 test_alembic_18_alembic_heads_one` | head 含 `089_gin_trgm_tsvector` | head 含 `091_add_kg_entity` + `(head)` 仅 1 次 | anchor 改 091 + 串单链纪律 |
| 5 | `tests/rag/test_pr5_e2e.py:297 test_alembic_18_alembic_heads_one` | head 含 `090_add_rag_eval_report` | head 含 `091_add_kg_entity` | anchor 改 091 |
| 6 | `tests/test_w85_hotfix_knowledge_column_e2e.py:103 test_087_alembic_single_head` | `heads == ["087_add_knowledge_original_parent_id"]` | `heads == ["091_add_kg_entity"]` + 087 仍在 chain (get_revision 不为 None) | 087 仍是 chain 中段 (历史属性保留), head 改 091 |

**未动**:
- `tests/rag/test_pr2_e2e.py:181 test_alembic_16_down_revision_correct` — 检查 `088.down_revision == "087"`, 历史事实不变, **无需改**
- `tests/rag/test_pr8_e2e.py` — 已用 091 锚点 (W94 PR8 merge 顺手对齐), 0 commit
- `tests/test_w85_hotfix_knowledge_column_e2e.py:test_087_down_revision_chains_to_086` + `test_087_idempotent_upgrade_safety` — 检查 087 的 down_revision=086 + upgrade body 含 IF NOT EXISTS, **历史属性**不变

## 真跑验证 (派工 v6 §1.2)

```bash
cd /e/agent-w91-x24-alembic-all
SKIP_DB_SETUP=1 pytest tests/alembic/ tests/rag/ -v 2>&1 | tail -5
# ====================== 332 passed, 6 warnings in 13.15s ======================

SKIP_DB_SETUP=1 pytest tests/alembic/test_pre_commit_hook_passes.py -v
# 4 passed
```

**结果**: 332 PASSED + 0 FAILED (排除 DB-dependent pre-existing 失败, 与 alembic anchor 无关)。

## commit 链

```
ad6f1ac23 fix(w91): alembic test_pre_commit_hook_passes 091 (W91-X-24 #1/N)
ec91d9fa2 fix(w91): alembic test_pr2_orphan_audit 091 (W91-X-24 #2/N)
cb6fb8ea5 fix(w91): alembic test_pr3_e2e 091 (W91-X-24 #3/N)
240250f64 fix(w91): alembic test_pr5_e2e 091 (W91-X-24 #4/N)
92887c053 fix(w91): alembic test_w85_hotfix_knowledge_column 091 (W91-X-24 #5/N)
6349910b8 fix(w91): alembic test_pr2_e2e 091 (W91-X-24 #6/N)
```

**6 commit** ahead of base `f57206c7c` (W94 merge-04 cleanup).

## 派工 v6 §5 反馈: 类 20.86 加固

> **类 20.86 "alembic test 锚点全量修, 不只 1 个文件"**
>
> X-16 派工 brief 只列了 `tests/alembic/test_pre_commit_hook_passes.py:142` 1 行,
> 但实测 `tests/rag/` (5 files: pr2/pr3/pr5/pr8 + pr2_orphan_audit) + `tests/test_w85_hotfix_knowledge_column_e2e.py`
> 也有 head 锚点断言. X-16 锚定只修 1 个文件, X-24 才补全 6 文件.
>
> **铁律 (新增)**: alembic 锚点修**必须 grep 全 tests/** `*.py` 列所有引用文件,
> 不只派工 brief 提及的 1 个. X-16 的派工 brief 不完整 (派工前提错配类 20 实战),
> 但本任务继承 X-16 之前隐含的"全量修"语义, 而非字面只修 1 行.
> 派工 brief v3 应列 `grep -rln "087|089|090" tests/ --include="*.py"` 输出作为任务前提.

## 边界复检

```bash
git diff main..HEAD --name-only   # 实际 f57206c7c..HEAD
# tests/alembic/test_pre_commit_hook_passes.py
# tests/rag/test_pr2_e2e.py
# tests/rag/test_pr2_orphan_audit.py
# tests/rag/test_pr3_e2e.py
# tests/rag/test_pr5_e2e.py
# tests/test_w85_hotfix_knowledge_column_e2e.py
# (docs/* memory/* 等其他文件已在 base HEAD 中, 0 commit 增量)
```

**0 production code 改动铁律 6/6 守恒** (6 file 全部为 `tests/**/test_*.py` 范畴)。

## 派工 v3 双锚定 → 派工 v6 §1.2 真验证

- **base ref 实测**: `git ls-remote origin main | head -1` = `f57206c7c` (W94 merge-04 cleanup)
- **分支名 fallback**: `claude/w91-x24-alembic-all` (worktree add `-b` 直接创, 派工 v6 §5 类 20.31 守恒)
- **集成 e2e**: 332 PASSED (派工 v6 §1.2 真验证)

## 累计 30 批 + W91 第 1 批

锚点范式 W90 476 → W91+ **482 守恒** (+6 file commits, 0 production code 增量)。
派工前提铁律 12 + 类 20 累计 **37 实例** (W91 第 1 批 +1: 类 20.86)。
W19 选项 A 维持。

## 后续 (W91+ 派工顺序表留口)

- W91-X-25+ (主指挥待派): 老 pytest 138+84 FAIL 调研 + W86 mini-N 21 commits 合并决策
- W92+ 留口