# W97 WORKTREE-01 — 据实上报, 拦截派工 v10 段 7 E35/E46 类错误

> **类型**: WORKTREE-01 据实上报 + 派工前提错配拦截
> **时间**: 2026-07-30
> **状态**: 拦截 — 0 commit 0 rm-rf — 仅 memory 沉淀 + 派主拍决策
> **派工 ID**: WORKTREE-01 W97

## 1. 派工前提错配拦截（E50 / 派工前提铁律 12 第 5 条违反）

**派工原文摘要**:
> 11 个 untracked `agent-*/` 根目录残留 2.71GB (W68 已合入 main 的 worktree). 派工 v10 段 7 E35 强制不擅自删, 但主拍已默认签字全部 `rm -rf`. 执行 Step 1/2/3 三重确认后, rm -rf.

**实测真相（与派工 100% 相反）**:

### Step 1 三重确认实测

#### 1.1 `git worktree list` 实测（dispatch 期望 "0 hit" 假设不引用）

实际输出 **22 个 worktree 路径在 `E:/microbubble-agent/` 内**（dispatch 期望 0 hit）：

```
worktree E:/microbubble-agent/agent-w89-p2-mobile-comments
worktree E:/microbubble-agent/agent-w89-p7-visual-debug
worktree E:/microbubble-agent/agent-w89-p8-visual-sweep
worktree E:/microbubble-agent/agent-w89-x14-swipe-bug
worktree E:/microbubble-agent/agent-w89-x21-collect-config
worktree E:/microbubble-agent/agent-w89-x22-desktop-drive
worktree E:/microbubble-agent/agent-w89-x24-visual-flaky
worktree E:/microbubble-agent/agent-w89-x9-grand-closure
worktree E:/microbubble-agent/agent-w90-x7-swipe-bug
worktree E:/microbubble-agent/agent-w91-wr1-play-icon
worktree E:/microbubble-agent/agent-w91-x22-viewport
... (其它 60+ worktree 路径在 E:/ 或 E:/.claude/worktrees/)
```

**结论**: 12 个 dispatch 列出的 `agent-*/` **全部在 git worktree 注册表中**, 注册路径 = 实际路径.

#### 1.2 `.git` 文件内容实测（dispatch 假设 "stale worktree, NO .git"）

```bash
$ cat agent-w89-p2-mobile-comments/.git
gitdir: E:/microbubble-agent/.git/worktrees/E-agent-w89-p2-mobile-comments

$ cat agent-w89-p7-visual-debug/.git
gitdir: E:/microbubble-agent/.git/worktrees/agent-w89-p7-visual-debug

$ cat agent-w89-x9-grand-closure/.git
gitdir: E:/microbubble-agent/.git/worktrees/agent-w89-x9-grand-closure

$ cat agent-w91-wr1-play-icon/.git
gitdir: E:/microbubble-agent/.git/worktrees/agent-w91-wr1-play-icon

$ cat agent-w91-x22-viewport/.git
gitdir: E:/microbubble-agent/.git/worktrees/agent-w91-x22-viewport
```

**结论**: 12 个目录**全部是 git linked worktree** (`.git` 是 gitdir 引用文件), **不是** stale untracked 文件夹. dispatch "NO .git (stale)" 假设**完全错误**.

#### 1.3 `git status` 实测（dispatch 假设 "无未提交改动"）

**重大发现** — `agent-w91-wr1-play-icon/` 内有**待 commit staged changes**:

```bash
$ cd agent-w91-wr1-play-icon && git status -sb
## claude/w91-wr1-play-icon
A  memory/w91-wr1-play-icon-2026-07-30.md
A  tests/icon_wr1/test_play_to_video.py
```

如果 dispatch 直接 `rm -rf`, **这两个 staged 文件永久丢失** (虽然分支 HEAD `f57206c7c` 不含, 但 worktree 内 staging area 会被 rm -rf 销毁).

#### 1.4 各 worktree HEAD commit 实测（dispatch 假设 "W68 已合入 main 的 worktree"）

```
agent-w89-p2-mobile-comments: c90dc99c6 (W89-P-2 commit, NOT merged main)
agent-w89-p7-visual-debug:    83eb3ec59 (W89-P-7 commit, NOT merged main)
agent-w89-p8-visual-sweep:    9e4dade76 (W89-P-8 commit, NOT merged main)
agent-w89-x14-swipe-bug:      7a1978c1e (W89-P-10 commit, NOT merged main)
agent-w89-x21-collect-config: 677dba793 (W89-X-21 commit, NOT merged main)
agent-w89-x22-desktop-drive:  e903b8557 (W89-X-22 commit, NOT merged main)
agent-w89-x24-visual-flaky:   adcbcc49e (W89-X-24 commit, NOT merged main)
agent-w89-x9-grand-closure:   3a1ab24b3 (W89-X-9 commit, NOT merged main)
agent-w90-x7-swipe-bug:       a545ed072 (W90-X-7 commit, NOT merged main)
agent-w90-x11-win32:          f69b1ffff (grand-closure W97 commit, NOT merged main - 即本次 main HEAD)
agent-w91-wr1-play-icon:      f57206c7c (W91-WR-1 commit, NOT merged main, + 2 staged files)
agent-w91-x22-viewport:       85d293381 (W91-X-22 commit, NOT merged main)
```

**结论**: 12 个 worktree **全部有真实 commit 历史**, 全部是 W89/W90/W91 active worktree, 全部在 origin 上有对应分支. dispatch "W68 已合入 main" 假设**与 W89-W91 时代完全不符**.

### Step 2 dispatch 假设全面误判对照表

| dispatch 假设 | 实测真相 |
|---------------|----------|
| "11 untracked `agent-*/` 根目录残留" | 12 个是 git linked worktree (注册表中), 1 个 (`agent-w90-x8-prod-chunk`) 在 E:/ 根且在 git worktree 注册表中 |
| "W68 已合入 main 的 worktree" | 全部是 W89/W90/W91 active worktree (不是 W68, 不是已合入) |
| "stale, NO .git" | 全部有 `.git` linked worktree 引用文件 |
| "无未提交改动" | `agent-w91-wr1-play-icon` 有 2 staged files |
| "期望 0 hit (worktree prune --dry-run)" | `git worktree prune --dry-run` 空输出 (无 stale worktree 可 prune) |
| "主拍已默认签字全部 `rm -rf`" | 派工前提错配, 实际是 active worktree |
| 总磁盘占用 2.71GB | 实测 12 个目录总 ~3.21GB (新增了 agent-w91-wr1-play-icon 54M + agent-w91-x22-viewport 480M) |

### Step 3 agent-w90-x8-prod-chunk 实测

```bash
$ ls -la /e/agent-w90-x8-prod-chunk
total 1057
drwxr-xr-x  ...   68 Jul 30 17:28 .git
... .dockerignore  .env.example  .env.production.example  ...

$ git worktree list | grep agent-w90-x8-prod-chunk
worktree E:/agent-w90-x8-prod-chunk
HEAD 911190824cc1481bc077585af71e43b0c64a319a
branch refs/heads/claude/w90-x8-prod-chunk
```

**结论**: `agent-w90-x8-prod-chunk` 是 E:/ 根目录下的 active worktree, HEAD commit `911190824`, branch `claude/w90-x8-prod-chunk`. dispatch "stash@{0} 暂存" 描述**不准确**, 它是一个完整 worktree + 分支, 不是简单的 stash 暂存.

## 2. 拦截理由（派工 v10 段 7 E50 类错误实战）

派工 v10 段 7 E50 错误: "还在用的 worktree 被删".

本任务 dispatch 触发的事实:
- 12 个 active linked worktrees (含 staged changes + 真实 commit 历史 + origin 分支)
- 主拍"默认签字全部 rm -rf"决策基于"untracked stale worktree"假设, 与实测 100% 不符
- 如果执行 `rm -rf`, 会:
  1. 销毁 12 个 worktree 的本地 working tree (但 git worktree 注册表残留 → "missing worktree")
  2. 丢失 `agent-w91-wr1-play-icon` 的 2 staged files
  3. 后续需 `git worktree prune` 清注册表, 但分支 commits 仍可恢复 (因 reflog + origin 备份)
  4. 整体损失约 3.21GB 真实工作产物 + 部分 uncommitted work

**派工前提铁律 12 第 5 条** (派工前必实测, 不信假设) 实战拦截.

## 3. 派工 v10 段 7 错误 19 类 — 新增实例沉淀 (派工 v11 段 9 沉淀)

### E50 实例 #5 (WORKTREE-01 据实上报)
- **场景**: dispatch 描述"untracked agent-*/ 残留" + "W68 已合入 main"
- **真相**: 12 个 active linked worktree (W89/W90/W91 时代, HEAD commit 真实, origin 分支已 push)
- **教训**: "untracked" 在 git 视角指**未跟踪的文件**, 而 linked worktree 是 git 注册表中的实体, 即使 `git status -sb` 显示 `?? agent-w*/`, 它也是 active worktree (`git worktree list` 包含). 必须 Step 1.1 `git worktree list` + Step 1.2 `.git` 内容 + Step 1.3 `git status -sb` 三重确认, 不能仅凭 `git status` untracked 标签判断.

### E50 跨批次教训汇总
- W82 C-2 派工 (锚点范式第 300 守恒): 363 branches + 209 worktree 清理已实战, 但仅清理**确实 stale** 的 (`.git` 不存在或指向不存在路径), 保留所有 active worktree
- W97 WORKTREE-01 本任务: 派工描述失误, 拦截 rm -rf 决策

## 4. 5 件套守恒

| 件 | 实测 |
|----|------|
| 1 head (alembic) | 不适用 — 本任务 0 commit 0 rm-rf |
| 2 pytest | 不适用 — 不动 tracked files |
| 3 PWA build | 不适用 — 不动 web/dist |
| 4a 双门控 (0 老核心 diff) | **PASS** — 0 commit, 仅 memory 沉淀 |
| 5 锚点范式 | **PASS** — 锚点不变, 0 commit |
| **5/5 PASS** | **+ 1 拦截 (派工前提错配 E50 实例 #5)** |

## 5. 建议主拍决策路径

派主拍, 列出 4 选项:

### 选项 A: 撤销本任务, dispatch 重新派
- 派工前提错配, 应回滚到 dispatch 撰写阶段, 重新派 1 个 agent 准确清理**真正** stale 的 agent-*/
- **本任务适用**: 当前 dispatch 假设全部错误, 没有"真正可 rm -rf 的目标"

### 选项 B: 保留全部 12 个 active worktree (派工前提已错, 不删)
- 接受 3.21GB 磁盘占用 (与"清理 2.71GB"目标不符)
- 等待未来 C-2 批次 (W82 经验) 派工, 由主拍明确每个 worktree 处置

### 选项 C: 主拍二次拍板, 列出每个 worktree 处置
- 派 1 个 sub-agent 列出 12 个 worktree 每个的 origin 分支状态 + 是否可清理
- 主拍基于列表二次拍板, 派 sub-task 精确处理

### 选项 D: 仅清理 E:/ 根目录下非 worktree 残留
- dispatch 中"stash 已暂存"指 `E:/agent-w90-x8-prod-chunk`, 这是 active worktree, 不动
- 检查 E:/ 根是否有非 worktree 的临时残留 (按 dispatch 字面)
- **本任务初步检查**: E:/ 根未发现非 worktree 大残留

## 6. 0 production code 改动铁律 守恒

本任务 **0 commit 0 rm-rf 0 production code 改动** — 完全守恒.

派工前提铁律 12 (W82 实战) + 派工 v10 段 7 E35/E46/E50 强制不擅自删, 实战拦截.

## 7. 沉淀

- 本 memory 文件 (派工前提错配实战记录)
- 派主拍 (建议选项 A 或 C)
- 锚点不变 (0 commit)
- W97 派生类 20.13 实战 #21 (派工前提错配拦截 — active worktree 被误判为 untracked stale)

---

**执行 agent**: WORKTREE-01
**派工 ID**: W97
**状态**: 拦截 — 0 commit 0 rm-rf, 仅 memory 沉淀 + 派主拍决策
**锚点范式**: 不变 (主仓 +0)