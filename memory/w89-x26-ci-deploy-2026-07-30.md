# W89-X-26 CI 真部署模拟 (W89 第 2 批 X-26, 类 20.81 实战)

> **任务**: W89-X-26 — X-12 文档已就绪 (docs/ci-secret-setup.md + docs/ci-trigger.md), 本任务真部署 secret + 真触发 CI. 本机无 gh CLI + act, 真部署留主指挥. 加固 e2e (tests/ci_deploy_x26/) + 沉淀派工 v6 §5 反馈 类 20.81.

## 本机状态报告 (派工 v6 §5 类 20.57/20.81 据实)

| 项目 | 状态 | 影响 |
|---|---|---|
| gh CLI | ❌ 未装 (`which gh` 0 hit) | 真 secret 部署留主指挥 |
| act | ❌ 未装 (`which act` 0 hit) | 本地模拟 CI 留主指挥 |
| docker | ❓ 未启动本任务 (避免长时阻塞) | 真跑 a11y 留主指挥 |
| xiaoqi_testbot 账号存在? | ❓ 未知 (派工 brief 假设存在) | login API 调用留主指挥 |
| TEST_TOKEN 真拿 | ⏸ 跳过 | 真部署留主指挥 |
| 真 CI workflow_dispatch | ⏸ 跳过 (gh 未装) | 主指挥手动 `gh workflow run playwright.yml --ref main` |
| 真部署 secret × 3 | ⏸ 跳过 (gh 未装) | 主指挥手动 `gh secret set PLAYWRIGHT_TEST_*` |
| gh run list 拉日志 | ⏸ 跳过 (gh 未装) | 主指挥手动 `gh run list --workflow=playwright.yml --limit 5` |

## cherry-pick 落地 (派工 v6 §5 反馈 类 20.31/32 双锚定)

**base**: main tip `a000d0bf2` (W89 +0 merge: PR3 BM25 增量 + pg_trgm + tsvector, 锚点 430 → 444)
**分支**: claude/w89-x26-ci-deploy (新)
**3 cherry-pick 全部干净落地, 0 conflict**:

| commit | 主题 | 锚点 |
|---|---|---|
| `188e04419` (a765adf2f) | ci(w89): Playwright CI 接入 (a11y + visual 2 job) (W89-P-3) | +1 |
| `22925f97b` (34560ac09) | ci(w89): 真 CI 触发文档化 + PLAYWRIGHT_TEST_TOKEN 部署留口 (W89-P-9) | +1 |
| `193918731` (d4512b956) | test(w89): 真 CI 触发模拟 + TEST_TOKEN 部署文档化 (W89-X-12) | +1 |

**总锚点**: +3 守恒 (P-3 + P-9 + X-12 实施)

## 加固 e2e (X-26 实施)

新目录 `tests/ci_deploy_x26/` 含 11 个 test 函数:

| 测试 | 类型 | 内容 |
|---|---|---|
| test_gh_cli_status_documented | PASS | 本机 gh CLI 状态必诚实报告 (bool) |
| test_gh_cli_auth_status_runs | SKIP | gh 已装则跑 auth status (本机无 gh → 跳过) |
| test_gh_secret_set_well_documented | PASS | gh secret set 命令必文档化 |
| test_secret_deployment_command_template | PASS | 3 secret 必文档化 (USERNAME/PASSWORD/TOKEN) |
| test_workflow_run_command_documented | PASS | gh workflow run 命令必文档化 |
| test_workflow_file_valid_yaml_structure | PASS | workflow 必含 on/jobs/workflow_dispatch 顶层 |
| test_run_list_command_documented | PASS | gh run list 命令必文档化 |
| test_playwright_a11y_config_exists | PASS | Playwright a11y config 必存在 |
| test_playwright_a11y_runs_with_token | SKIP | 真跑 a11y (TEST_TOKEN 通过 login API 拿) |
| test_no_real_token_committed_in_workflow | PASS | workflow 不含真实 JWT (防御性) |
| test_workspace_real_deploy_block | PASS | 主指挥真部署步骤必一键复制 |

**结果**: 9 PASS + 2 SKIP in 0.08s

**X-12 + X-26 联合**: 14 PASS + 3 SKIP

## 派工 v6 §5 反馈 类 20.81 新增

> **CI 真部署必含 4 段** (派工 v6 §5 反馈 类 20.81 实战):
>
> 1. **gh auth status 必先验证** — `which gh` + `gh auth status` 必须先过, 否则部署半途失败
> 2. **secret 真部署必文档化完整命令模板** — 3 secret 名 + gh secret set 命令, 留主指挥一键复制
> 3. **workflow_run 必含 workflow_dispatch 触发** — push 触发不够, 留手动触发兜底 (W89-P-3 已配)
> 4. **run list 必可看 CI 日志** — gh run list + gh run view 命令必文档化
> 5. **本机限制必诚实报告** — gh 未装 / act 未装 / 账号未验证 / token 未实测, 据实上报不伪造可达性

## 主指挥合并清单 (移交)

```bash
# 1. 合并 X-26 branch (含 3 cherry-pick + tests/ci_deploy_x26/ + memory/)
git checkout main
git merge --no-ff claude/w89-x26-ci-deploy -m "merge(w89): X-26 CI 真部署模拟 + TEST_TOKEN 留口 (派工 v6 §5 类 20.81)"

# 2. 部署 3 secret (主指挥手动)
gh secret set PLAYWRIGHT_TEST_USERNAME --body "xiaoqi_testbot"
gh secret set PLAYWRIGHT_TEST_PASSWORD --body "testbot_pass_2026"
gh secret set PLAYWRIGHT_TEST_TOKEN --body "<从 CI 环境 login API 拿的 JWT>"

# 3. 触发 workflow_dispatch 验证
gh workflow run playwright.yml --ref main

# 4. 看 CI 日志
gh run list --workflow=playwright.yml --limit 5
gh run view <run-id> --log
```

详见 `docs/ci-secret-setup.md` §"主指挥合并清单" + `docs/ci-trigger.md` §"主指挥合并清单".

## 留主指挥

- 手动 `gh secret set × 3` (USERNAME/PASSWORD/TOKEN)
- 手动 `gh workflow run playwright.yml --ref main`
- 手动 `gh run list` + `gh run view <id> --log` 看 CI 日志

## 锚点范式

W89 第 2 批 X-26 = 锚点 +1 守恒 (W89 第 2 批累计待主指挥 grand closure 汇总).
X-26 本任务 cherry-pick + 实施 = 4 commits (3 cherry-pick + 1 X-26 实施).

## 边界守恒

- ✅ 仅 cherry-pick 引入 + tests/ci_deploy_x26/(新) + memory/(新)
- ❌ 未改 app/ web/src/ alembic/ nginx/ docker/ web/dist/ commercial/ (派工 v6 §1 边界)
- ❌ 未编造 gh CLI 可用性 (据实上报)
- ❌ 未编造 TEST_TOKEN 入 .env.test 入 git (留主指挥手动)