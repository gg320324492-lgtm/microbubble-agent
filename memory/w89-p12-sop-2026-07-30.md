# W89-P-12 axe SOP + visual 撤容错 (2026-07-30)

## 当前状态

- main HEAD: `3a1ab24b3` (W86 mini-16 docs 更新 + 减负, 锚点 338)
- 新分支 `claude/w89-p12-sop` (parent: `3a1ab24b3`)
- worktree: `E:/agent-w89-p12-sop`
- 派工纪律: 派工 v6 §5 反馈类 20.60 (axe SOP 沉淀规则) + 派工前提铁律 12

## 任务定义

W89-P-5 留口: 写 `docs/axe-rules.md` axe 修复 SOP
W89-P-3 留口: visual `continue-on-error: true` 3 次稳定后撤

## 已实施

### 1. axe-rules.md 创建 (本任务核心交付)

`docs/axe-rules.md` — 5 条 axe 规则 SOP 完整版:

| 规则 | 修法核心 |
|------|----------|
| color-contrast | 改 token (CSS variable), 加深到 ≥ 4.5:1, 不要 inline 改色 |
| html-has-lang | `<html lang="zh-CN">` 一行, web/index.html + web-minimal/index.html |
| aria-command-name | 必含具体语义 (`"删除任务"` 而非 `"delete"`) |
| scrollable-region-focusable | `tabindex="0"` + `role="tree/list/grid"` + `aria-label` |
| link-name / button-name | icon SVG 必 `aria-hidden="true"`, 装饰性 AT 跳过 |

每个规则 3 段: **触发 + 修法 + 验证**. 加上 CI 集成段 (`npm run build:a11y` / `.github/workflows/playwright.yml`) + 边界复检 + 留 W89+ + 5 条铁律.

### 2. e2e 加固 (硬门禁)

`tests/axe_sop/test_doc_exists.py` (3 test):

```python
def test_axe_rules_doc_exists()        # axe-rules.md 必存在
def test_axe_rules_doc_has_5_rules()   # 5 规则名必出现
def test_axe_rules_doc_has_ci_section() # CI 集成段必存在
```

实测: `SKIP_DB_SETUP=1 pytest tests/axe_sop/ -v` → **3 passed in 0.11s**.

## 未实施 (留 W89+, 原因据实上报)

### 3. visual `continue-on-error: true` 撤容错 — **暂停, 报告主指挥**

派工 brief 前提: W89-P-8 已跑 visual sweep + 主指挥拍板更新 baseline.

**实测结论** (实测结果, 不在 main HEAD `3a1ab24b3`):

| 路线 | 状态 | 解释 |
|------|------|------|
| `.github/workflows/playwright.yml` 在 main 中存在? | **❌ 不存在** | 当前 main 仅含 `build-image.yml` / `image-scan.yml` / `lint-css.yml` / `qa-bench-baseline.yml` / `qa-bench-ci.yml` / `qa-bench-smoke.yml` / `secret-scan.yml`. W89-P-3 提交 `a765adf2f` 创建, 但**未**合并到 `3a1ab24b3` |
| W89-P-3 commit 所在分支 | `claude/w89-p3-playwright-ci` (`a765adf2f`) | 仅在分支存在, 不在 main |
| W89-P-8 (visual sweep + baseline 更新) | **❌ 未跑** | `git log --all --oneline --grep="P-8\|visual sweep"` 0 命中 |
| W89-P-9 / P-10 / P-11 分支 | 存在但**未**merged | `claude/w89-p9-ci-trigger` / `claude/w89-p10-e2e-restructure` / `claude/w89-p11-dark-accent` 都基于 `3a1ab24b3` |
| `git status` 当前 | working tree clean | main tip base |

**决策**: **不擅自撤容错** — 派工纪律明确"如果前提不满足, 报告并暂停, 留 W89+".

撤回风险 (如果硬撤):
1. `.github/workflows/playwright.yml` 不在 main → 没文件可改
2. 即使文件存在, baseline 未更新 → visual job 必 fail 红 PR
3. W89-P-3 本就尚未 merge, W89-P-12 改它的 visual job 改的就是分支版本 (后续 merge 时会带这条改动, 主指挥会惊讶)

**正确做法 (W89-P-12 沉淀新纪律)**:
1. 主指挥合并 W89-P-3 (`.github/workflows/playwright.yml` 首次落地 main)
2. 主指挥派 W89-P-8 (全 visual sweep + baseline 重 sync)
3. 跑满 3 次 visual sweep 稳定 (无 baseline 漂移)
4. **再**派 W89-P-12 v2 (撤 visual `continue-on-error: true`)

## 派工 v6 §5 反馈类 20.60 沉淀

**类 20.60**: "axe SOP doc 必含 ≥ 5 规则, 每条 ≥ 3 段 (触发/修法/验证) + CI 集成段 + 留 W89+ 段; 否则不算沉淀, 仅算 inception doc"

派工 brief 反例:
- 仅列 5 规则名 + 一句话修法 = **半成品**
- 列规则名 + 修法 + 无 CI 段 = **与派工 brief 漂移**
- 列规则名 + 修法 + CI 段 + 无留 W89+ 段 = **缺闭环**

SOP doc 验收口径 (本任务 W89-P-12 沉淀):
1. 必含 `color-contrast` / `html-has-lang` / `aria-command-name` / `scrollable-region-focusable` / `link-name` (5 规则)
2. 每规则必含 *触发 + 修法 (含具体代码示例) + 验证* 3 段
3. 必含 `## CI` 或 `build:a11y` 段
4. 必含 `## 留 W89+` 段
5. 必含 `## 5 条铁律` 段 (本项目沉淀实践型)
6. e2e `tests/axe_sop/test_doc_exists.py` 3 case 必通过 (e2e 入口)

## 锚点范式

| 段 | 主锚点 (实际) | 主锚点 (base + 累计) | delta |
|----|---------------|---------------------|-------|
| main tip | 338 | 338 | base |
| W89-P-12 | 339 (预期) | 338 + 1 | +1 |

实施 = 1 实战 (axe SOP doc + e2e). 锚点 +1 守恒.

## 留 W89+ 拍板清单

1. **W89-P-8 visual sweep 派工** — 主指挥必派, 前提不满足 visual 撤容错就不能做
2. **W89-P-3 playwright.yml merge** — 当前仅在分支, 需先 merge 才能改 visual job
3. **dark mode 4 accent 主题完整 a11y 验证** — W89-P-11 待 merge, 跑 baseline
4. **moderate violations 是否逐项修** — 当前仅 warn, 主指挥拍板是否提升门禁
5. **AVT (Accessibility Verification Test) 全套** — 主指挥拍板是否提升视觉验证等级

## 边界复检

`git diff main..HEAD --name-only` 预期:
- `docs/axe-rules.md` (新)
- `tests/axe_sop/__init__.py` (新)
- `tests/axe_sop/test_doc_exists.py` (新)
- `memory/w89-p12-sop-2026-07-30.md` (新)

**未改** (派工纪律严格边界):
- `app/`, `web/src/`, `alembic/versions/`, `nginx/`, `docker/`, `web/dist/`, `commercial/`
- `.github/workflows/playwright.yml` (W89-P-3 分支版本, 不动)
- 已有 memory, 已有 e2e 测试
