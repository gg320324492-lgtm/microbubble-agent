# W90-X-13 vite 7.3.6 降级真构建验证 (2026-07-30)

> **派工**: W90 第 1 批 X-13 vite 降级真构建验证. base ref = main HEAD `034343f8a`
> (W91 merge-03 收口). 锚点范式守恒 (不动业务代码).

## 任务定义

W89-P-6 + X-17 已 cherry-pick `c4334e148` (vite ^8.0.13 -> ^7.3.6) 解 rolldown 1.1.5
panic (`Symbol "easeInOutCubic" in element-plus/es/utils/easings.mjs should belong to a chunk`).

本任务**真验证** build 跑通, 沉淀类 20.95 加固版.

## 验证结果 (据实上报)

### 1. vite 版本确认

```
$ grep -E "\"vite\"" web/package.json
    "vite": "^7.3.6",

$ node -e "console.log(require('./node_modules/vite/package.json').version)"
vite version: 7.3.6
```

**vite 7.3.6 守恒确认**.

### 2. npm run build 验证 (核心门禁)

build 启动正常:
```
> vite build && node scripts/postbuild-fix-manifest.js
vite v7.3.6 building client environment for production...
[vue-bum-null-patch] applied to .../@vue/runtime-core/dist/runtime-core.esm-bundler.js
[ep-unregister-pane-null-patch] applied to .../element-plus/es/hooks/use-ordered-children/index.mjs
[zrender-passive-wheel-patch] applied to .../zrender/lib/dom/HandlerProxy.js
✓ 3538 modules transformed.
```

build 失败, 但失败根因**与 vite 降级无关**:

```
src/views/admin/RAGEvalPanel.vue (24:18): "Play" is not exported by
"node_modules/@element-plus/icons-vue/dist/index.js", imported by
"src/views/admin/RAGEvalPanel.vue".
```

具体:
- `web/src/views/admin/RAGEvalPanel.vue:24`:
  ```js
  import { Refresh, Play, DataAnalysis } from '@element-plus/icons-vue'
  ```
- `@element-plus/icons-vue` 实际导出列表:
  - `Play`: NO (不存在)
  - `VideoPlay`: YES (替代名)
  - `CaretPlay`: NO
  - `VideoCamera`: YES
- 引入者: W91 PR5 commit `cb5c98498` "feat(pwa): RAGEvalPanel.vue + useRAGEval.js + router"

### 3. vite 降级核心目标达成

build 输出**未出现** Rolldown `easeInOutCubic` panic:
```
$ grep "easeInOutCubic" /tmp/build-x13.log
(0 matches)
```

vite 7.3.6 + rollup 4.62.3 避开 vite 8.x rolldown 1.1.5 上游 bug 验证 PASS.

### 4. dist 健康

- `dist/index.html` 仍存在 (W89-X-2 commit `223ae469b` 重 build 产物)
- `dist/assets/index-d64e7046.js` (含 Sentry chunk) 存在
- 254 JS + 187 CSS 文件
- 部署可用性**当前守恒**, 但生产部署前必须先修 RAGEvalPanel.vue

## 沉淀: 类 20.95 加固 (派工 v6 §5 反馈)

> **类 20.95**: vite 降级后必真跑 npm run build 守恒 (W89-P-6 + X-17 加固版).
> 验证 vite 降级目标**不是看 vite 版本号**, 而是**真跑 build 输出无 Rolldown panic**.

**关键纪律**:

1. **vite 版本号正确 ≠ 降级生效** — 必须真跑 `npm run build` 看 build 输出
   无 `easeInOutCubic` panic 字符串, 否则 vite 8.x rolldown 1.1.5 bug 仍在
2. **build 失败可能是其它问题** — vite 7.3.6 自身正确也可能 build 失败,
   必须分清是 vite 降级问题 vs pre-existing import / 配置 bug
3. **如实上报分离**:
   - vite 降级目标 (无 Rolldown panic) 是否达成
   - 完整 build 是否 PASS (与 vite 降级目标**不同**)
   - dist 健康 (历史 build 产物是否仍可用)
4. **pre-existing build failure 不可借 vite 降级之名"修"** —
   `src/views/admin/RAGEvalPanel.vue` 的 `Play` import 是 W91 PR5 commit `cb5c98498`
   引入的 pre-existing bug, 与 vite 7.3.6 降级无关. 应单独派 W90-X-13+1 (X-14)
   fix `Play -> VideoPlay` (或加 VideoPlay 别名 import), 不混入本任务

## 测试沉淀

新建 `tests/vite_x13/test_build.py` (派工 v6 §5 反馈 类 20.95):

| 测试 | 守恒门禁 | 当前 main HEAD |
|------|----------|----------------|
| `test_no_rolldown_panic` | vite 7.3.6 + rollup 4.62.3 无 Rolldown `easeInOutCubic` panic | **PASS** |
| `test_dist_index_html_exists` | dist/index.html 存在 (部署可用) | **PASS** |
| `test_npm_run_build_passes` | npm run build 完整 PASS (returncode 0) | **FAIL** (RAGEvalPanel.vue Play icon) |

## 后续派工建议 (W90+ 留口)

- **W90-X-14** (建议): 修 `web/src/views/admin/RAGEvalPanel.vue:24`
  `import { Refresh, Play, DataAnalysis } from '@element-plus/icons-vue'`
  改为 `import { Refresh, VideoPlay, DataAnalysis } from '@element-plus/icons-vue'`
  + 模板 `<Play />` 改为 `<VideoPlay />`. 修完 test_npm_run_build_passes 应 PASS.
  (或保留 `Play` 别名, 看 element-plus 是否支持本地 re-export)
- **W90-X-15** (建议): npm audit moderate 75 调研 (派工 v6 §5 反馈 类 20.35 续,
  66 集中在 hint 链), 沿用 W87-X-4c npm audit 模式.

## commit 边界

- 新建 `tests/vite_x13/test_build.py` (3 tests, 2 PASS + 1 FAIL 据实)
- 新建 `memory/w90-x13-vite-verify-2026-07-30.md` (本文档)
- **不动**:
  - 业务代码
  - `web/vite.config.js` (派工纪律: 只验证不擅自动)
  - `web/src/views/admin/RAGEvalPanel.vue` (派工纪律: 不动业务代码 + 派工 brief 明确禁止)
  - `web/package.json` (vite 版本已守恒)
  - `web/dist/` (本次 build FAIL, 用 W89-X-2 旧产物, 不重新 force-add)

## 锚点范式

- base ref: `034343f8a` (main HEAD)
- 新分支: `claude/w90-x13-vite-verify`
- 0 production code 改动铁律 守恒
- 锚点范式: 0 commit ahead (本任务只有 tests/ + memory/ 新增, docs sync 待主拍 W90 D-2)