# W68 第 10 批 A-3：5 分支顺序合并与 Alembic 单链守恒

**日期**：2026-07-24
**Agent**：W68 第 10 批 A-3
**分支**：`chore/w68-10th-batch-a3-pr9-11-merge-2026-07-24`
**锚点范式**：第 122 守恒
**纪律**：0 production code 改动铁律维持；PR12/PR13/PR11 fallback/Desktop v3.2 为已批新功能例外

## 1. 目标与顺序

按依赖顺序合并 5 个远端分支：

1. `feat/drive-v2-pr12-reactions-2026-07-24`（067）
2. `feat/drive-v2-pr13-combined-notifications-2026-07-24`（068）
3. `feat/drive-v2-pr11-recursive-fallback-2026-07-24`（069）
4. `feat/desktop-comment-v32-2026-07-24`（无 migration）
5. `test/qa-bench-phase3-matrix-2026-07-24`（无 migration）

硬门禁：不自动改 `down_revision`；每个 migration merge 后验证唯一 head；每批 push `origin/main`；最终 baseline 71 PASS + 7 SKIP。

## 2. 前置基线纠偏

启动时抓到的 `origin/main=f14cb43c1` 尚不含 066。直接合并 PR12 后 Alembic 报：

```text
KeyError: '066_drive_comments_path'
```

调查确认 A-1 完整整合已在远端分支：

- `origin/chore/w68-10th-batch-a1-merge-2026-07-24`
- tip `c0eae0c76`
- 包含 A-2 anchor、PR11 path 物化和 066 hot-fix
- 066 已接 `065_push_subscriptions`
- 唯一 head 为 `066_drive_comments_path`

因此丢弃未 push 的错误基线合并，重置到 `c0eae0c76` 后重做。并发期间 A-2 又把 066 hot-fix merge + memory 推到 main（`67e7b06fa`、`5e047448f`），PR12 后显式同步，保证后续 push 均为快进。

## 3. PR12 reactions（067）

源 tip：`21a1906a8`
合并提交：`e8720771d`
实际冲突：`app/main.py`

原因：当前主线已有 PR10 `drive_collab` 和 Mobile v3.2 `push_notifications`；PR12 在同一 import/router 区域新增 `drive_reactions`。

解决：三者全部保留并注册，不覆盖既有路由。

```text
Alembic heads = ['067_drive_reactions']
```

同步 A-2 后第 1 批远端 tip：`bac9a6563`。

## 4. PR13 combined notifications（068）

源提交链：`53a2ea40c` → `1e5f93938` → `261434626`
合并提交：`666032d30`

实际冲突：

- `app/api/v1/drive_reactions.py`（add/add）
- `app/services/drive_comment_service.py`
- `app/services/drive_event_publisher.py`

额外发现：PR13 tip 的 `app/main.py` 已提交 5 行字面冲突标记；大量 diff 是行尾重写。`mention_parser.py` 实际未冲突。

不能整文件取 `theirs`。以当前主线为底，从 PR13 相对其自有 PR12 基础的 diff 提取真实语义增量：

1. mention 改走持久去重的 `publish_combined_notification`。
2. comment reaction 创建后补 combined notification。
3. publisher 新增 combined notification 函数并接去重 service。

同时合入 068、dedup model/service、docs、memory。最终 staged 结果仅 115 行新增、8 行删除，避免约 3000 行行尾噪声，并保留 PR12 WS 能力。

```text
Alembic heads = ['068_drive_notification_dedup']
```

## 5. PR11 recursive fallback（069）

源 tip：`abf3f1132`
合并提交：`31f25d041`
实际冲突：`app/api/v1/drive_comments.py`

当前主线已有 PR11 `/by-path` 与 `/breadcrumb`；fallback 旧基线再次定义 `/breadcrumb` 并新增 `/descendants`。直接保留两侧会产生重复路由。

解决：

- 保留当前 `/by-path` 与 path schema。
- 引入 recursive schema/service。
- 用 B-2 的 `gin → recursive` fallback 版本升级原 `/breadcrumb`。
- 新增 `/descendants`。
- 069 原样保留，不改 `down_revision`。

```text
路由计数: breadcrumb=1 descendants=1 by-path=1
Alembic heads = ['069_drive_comments_recursive_func']
```

## 6. 两个无 migration 分支

### Desktop 评论 UI v3.2

- 源 tip：`bdce2635b`
- merge：`136e4fef5`
- 冲突：无
- 合入评论组件/view、reaction/breadcrumb composables、E2E、指南与 memory

### qa-bench Phase 3 matrix

- 源 tip：`c496862b7`
- merge：`b9ada515e`
- 冲突：无
- 合入 matrix runner/report、D6 roadmap、MEMORY 索引与专项 memory

第 4 批推送后远端 main tip：`b9ada515e`。

## 7. Alembic 最终单链

```text
heads = ['069_drive_comments_recursive_func']
066_drive_comments_path           → 065_push_subscriptions
067_drive_reactions               → 066_drive_comments_path
068_drive_notification_dedup      → 067_drive_reactions
069_drive_comments_recursive_func → 068_drive_notification_dedup
```

结论：唯一 head 069；066→067→068→069 无分叉；本任务没有修改任何 migration 的 `down_revision`。

## 8. Baseline 与质量验证

Baseline audit：

```text
39 passed in 2.91s
```

其中 collect 门禁确认 9 个 baseline 文件仍为 78 tests。

Docker 真实 baseline：

```text
71 passed, 7 skipped, 14 warnings in 2.11s
```

其他结果：

- typing import：163 文件，0 错误。
- 5 个目标 source tip 均为最终 HEAD ancestor。
- app/Alembic/web/qa-bench 冲突标记扫描 0 残留。
- PR13、recursive fallback import smoke 通过。
- matrix runner `py_compile` 通过。

专项测试说明：PR12 12 tests 与 fallback 11 tests 在宿主 fixture 建库阶段因 PostgreSQL 未启动而 `ConnectionRefusedError`，未进入断言；Docker 固定 baseline 已真实执行并守恒。

## 9. 五条新铁律

1. **顺序重要**：migration 功能必须按拓扑合并；先 067，再 068，再 069。
2. **Alembic 串单链**：每个 migration merge 后立即验证唯一 head，前一批正确才进入下一批。
3. **service 冲突做语义合并**：独立 cherry-pick 导致整文件冲突时，先提取相对其自有基线的真实增量。
4. **优先新功能不等于盲取 theirs**：theirs 含冲突标记、行尾噪声或主线回退时，把新语义叠到当前干净版本。
5. **合并后 baseline 守恒**：必须同时通过 audit collect、Docker 71+7、typing import、Alembic single-head 四道门禁。

## 10. 收口

- 5 个目标分支全部顺序合并并 push `origin/main`。
- Alembic 唯一 head 为 069。
- baseline 71 PASS + 7 SKIP 守恒。
- typing import 163 文件 0 错误。
- 冲突标记 0 残留。
- 锚点范式第 122 守恒。
