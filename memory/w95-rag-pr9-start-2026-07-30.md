# W95 RAG PR9 起步 (2026-07-30)

> **派工**: 主指挥协调范式第 N 次派工. PR9 B-9 实施 (锚点范式 W95 +0 → +16, 17 commits).
> **基线**: main HEAD = `3a1ab24b3` (W86 mini-16 锚点 338). 当前 worktree `chore/w95-rag-pr9-auto-research-2026-07-30` 分支.

## 起步 6 项完成状态

| # | 项 | 状态 | 备注 |
|---|----|------|------|
| 1 | Read plan §2 + §11.2 | ✅ | `C:\Users\pc\.claude\plans\rag-quirky-otter.md` 613 行已读 |
| 2 | worktree 创建 | ✅ | `.claude/worktrees/rag-pr9` + `chore/w95-rag-pr9-auto-research-2026-07-30` 分支 |
| 3 | `python -m alembic heads` | ✅ 1 head | `087_add_knowledge_original_parent_id` 守恒 (W95 D-2 同基线) |
| 4 | baseline pytest | ⚠ 跳过 | DB 未启动 (ConnectionRefusedError), 仅 test_activity_service.py 错误属于 DB 测试, 24 errors 全 DB 不可达. **PR9 e2e 设计 mock DB, 不依赖实跑 baseline** |
| 5 | `npm run build` | 待 PR9 完成时跑 | 步骤 5 留 W95 +16 commit 前 |
| 6 | 本 memory 沉淀 | ✅ | 本文件 |

## plan 关键约束复述 (PR9 据实执行)

**目标 4 门禁**:
1. 联网命中自动入 KB 成功率 ≥ 70%
2. 跨文档去重准确率 ≥ 95%
3. 同义改写覆盖 query ≥ 50% (synonym_dict 扩到 ≥ 200 条)
4. qa-bench PASS ≥ 96.5%

**新增 4 文件** (plan §11.2):
- `app/services/auto_research_v2.py` — v2 联网 + LLM + 入库闭环
- `app/services/dedup_cross_doc.py` — 跨文档去重 (pgvector 余弦 + LLM-as-judge)
- `app/services/query_rewriter.py` — 同义改写 (接 synonym_dict)
- `tests/rag/test_pr9_e2e.py` (22/22 PASS)

**修改 2 文件 (限制面)**:
- `app/services/auto_research_service.py` — 仅在 `research_topic` 末尾加 v2 钩子 (feature flag 默认 False)
- `app/services/search_service.py` — 仅加 `enable_rewriting` 参数 (默认 False)

**严禁修改**:
- `auto_research_service.research_topic` 原签名
- `search_service._search_sogou` / `_search_bing`
- `knowledge_service.py` 老核心
- alembic 任何已有迁移

**5 件套验证**:
1. `python -m alembic heads` → 1 head
2. `pytest tests/rag/test_pr9_e2e.py -v --ignore=test_w79` → 22/22
3. `cd web && npm run build` → OK
4. `git diff main -- app/services/auto_research_service.py | wc -l` → ≤ 10 行
5. `git log --grep "W95 +" | wc -l` → ≥ 17

**commit 锚点范式 (17 commits)**:
```
[W95 +0..+3] feat(rag/research): auto_research_v2 + LLM-as-judge 入库
[W95 +4..+7] feat(rag/research): dedup_cross_doc 跨文档去重
[W95 +8..+11] feat(rag/research): query_rewriter 同义改写
[W95 +12..+14] test(rag/research): 22/22 PASS + 跨文档去重 e2e
[W95 +15..+16] docs(rag/research): CHANGELOG + CLAUDE.md + 据实上报
```

## v1 现状关键发现 (影响 PR9 设计)

1. **`auto_research_service.research_topic` (L77-155)** — v1 已有 _exists_by_source URL 检查 + LLM extract + _ingest_knowledge 闭环. 但仅按 URL 去重, 未按内容向量去重. PR9 v2 必须补内容级去重.
2. **`search_service.search` (L19-59)** — 单签名, 仅 max_results 参数. PR9 加 `enable_rewriting` 不破坏, 默认 False.
3. **`knowledge_graph_service._calc_similarity` (L201)** — 已有 pgvector 余弦算法, dedup_cross_doc.py 可复用其调用模式.
4. **`_ingest_knowledge` (L365-402)** — v1 直接入库, 不经 LLM-as-judge. PR9 v2 必须插入 LLM-as-judge 兜底.
5. **`SynonymDict`** — PR4 已建 (`app/services/synonym_dict.py`), query_rewriter.py 接 PR4 资产 (派工纪要 v6 §2 复用纪律).

## PR9 设计决策 (据实, 不脑补)

- **auto_research_v2**: 在 v1 `_ingest_knowledge` 后插入 `_llm_as_judge` 步骤. 仅当 LLM judge `relevant=true` 且 `not duplicate` 才 commit. feature flag `AUTO_RESEARCH_V2_ENABLED=false` 默认 False, v1 行为不变.
- **dedup_cross_doc**: 用 pgvector 余弦 (threshold 0.92) + LLM-as-judge (语义对比). threshold 默认可调 (PR9 e2e test).
- **query_rewriter**: 调 PR4 `synonym_dict.expand(query)` + LLM 同义改写. synonym_dict 若未建 (PR4 未合) 则降级仅 LLM. feature flag `QUERY_REWRITER_ENABLED=false` 默认 False.

## W73/W74 起步纪律

- PWA 410 4 层: W95 +15 跑 `npm run build` 验证
- anchor 0 regression: W95 +0..+16 17 commits 全 grep "W95 +"
- +N 锚点范式: 17 commits 严格递增
- 5 件套守恒: 件 1/2/3/4/5
- commit message 模式: `[W95 +N] type(scope): desc` + Co-Authored-By
