# W90 PR4 启动 (HybridRetriever 召回侧量化, 锚点 +0 → +14)

> **派工**: PR4 B-4 实施 agent (主指挥协调范式第 N 次派工)
> **锚点范式**: W90 +0 → +14 (15 commits)
> **日期**: 2026-07-30
> **plan**: `C:\Users\pc\.claude\plans\rag-quirky-otter.md` §6 + §11.2

## 起步 6 项 (W73 铁律)

1. ✅ Read plan §6/§11.2 + §2 PR4 行
2. ✅ `cd E:\microbubble-agent && git worktree add .claude/worktrees/rag-pr4 -b chore/w90-rag-pr4-hybrid-weight-2026-07-30 main`
3. ✅ `python -m alembic heads` → `['087_add_knowledge_original_parent_id']` (1 head, base OK)
4. ✅ baseline pytest SKIP_DB_SETUP=1 OK (3 passed, 1 warning)
5. ⏳ `cd web && npm run build` (待最后验证)
6. ✅ memory 沉淀 (本文件)

## base 状态实测

- main HEAD = `3a1ab24b3` (W86 mini-16 doc update, 锚点 338)
- worktree = `E:/microbubble-agent/.claude/worktrees/rag-pr4`
- branch = `chore/w90-rag-pr4-hybrid-weight-2026-07-30`
- 当前 PR3 锚点范式收口后 (W88/W89 应已被 PR1/2/3 占据, 本批为 PR4, 起 W90)
- alembic head = `087_add_knowledge_original_parent_id` (PR1/2/3 收口后)
- `hybrid_retriever.py` = 357 行, 8 methods + 1 factory = 9 def (基线对照, 0 diff 必须)
- 不动 `retrieve` / `_vector_search` / `_bm25_search` / `_graph_search` / `_refresh_bm25_index` / `_merge_results` / `_normalize_scores` / `evaluate` / `__init__` / `get_hybrid_retriever`

## plan 关键约束 (派工 prompt 摘要)

- down_revision: PR4 不需 alembic 迁移 (PR5 才走 090_add_rag_eval_report)
- type: B 实施
- 不动 hybrid_retriever.py 原 10 个 def 签名
- 新增 5 文件:
  - `app/services/hybrid_weight_config.py`
  - `app/services/synonym_dict.py`
  - `tests/rag/__init__.py`
  - `tests/rag/test_hybrid_weight_config.py`
  - `tests/rag/test_synonym_dict.py`
  - `tests/rag/test_pr4_e2e.py` (22/22 PASS)
- 新增 hybrid_retriever 辅助函数: `_apply_weights` + `_apply_synonyms` (不改原签名)
- 新增数据文件: `app/services/synonym_dict/__init__.py` ≥ 200 条中文微纳米气泡领域

## 量化门禁

| 项 | 阈值 |
|----|------|
| 四路权重可配 (yaml + DB) | 必须 |
| synonym dict 条数 | ≥ 200 |
| CrossEncoder 保留率 | ≥ 70% |
| qa-bench PASS | ≥ 95% (推荐不跑, 验证 e2e) |
| pytest PR4 e2e | 22/22 PASS |
| hybrid_retriever 原函数 diff | 0 (仅新增辅助函数) |

## 5 件套守恒 (件 1 必填 OK)

1. `python -m alembic heads` → 1 head (本 PR 不产生迁移)
2. `pytest tests/rag/test_pr4_e2e.py -v --ignore=tests/test_w79_commercial_private_deployment_e2e.py` → 22/22
3. `cd web && npm run build` → OK
4. `git diff main -- app/services/hybrid_retriever.py | wc -l` → **必须为 0** (原函数 0 diff, 仅新增辅助函数)
5. `git log --grep "W90 +" | wc -l` → ≥ 15

## 派工 brief 接收 (本批 ID)

- 类型: B 实施
- plan §6 跨 PR 共用工具集 (W90 PR4 复用 HybridRetriever 25-104 行 + bm25_service + reranker_service)
- plan §2 PR4 锚点范式: W90 +0 → +14
- 0 production code 例外: **否** (本批仅扩 HybridRetriever 权重配置 + 加 synonym, 不动老核心)

## 起步后下一步

1. 写 `app/services/hybrid_weight_config.py` (W90 +0..+2)
2. 写 `app/services/synonym_dict.py` + 数据文件 ≥ 200 条 (W90 +3..+5)
3. 在 hybrid_retriever.py **末尾追加** `_apply_weights` + `_apply_synonyms` 辅助函数 + import 新模块 (W90 +6..+8)
4. 写 3 个 test 文件 (W90 +9..+11)
5. CHANGELOG + CLAUDE.md + memory 沉淀 (W90 +12..+13)
6. 据实上报 + memory (W90 +14)