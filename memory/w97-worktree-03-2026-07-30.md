# W97 WORKTREE-03 — #10 必保留 + #4 必报实测 (派工 v10 段 7 E50/E79/E80 强制)

> **类型**: WORKTREE-03 重点双 worktree 实测
> **时间**: 2026-07-30
> **状态**: 0 commit on #10 + #4 worktrees (实测 + memory 沉淀)
> **派工 ID**: WORKTREE-03 W97
> **协作前提**: WORKTREE-02 已列 13 个 + 4 选项决策, 本任务专注 #10 / #4 两个特殊目标

## 0. 任务边界

派工原文要求"WORKTREE-03 处理 #10 必保留 + #4 必报". 实测:

- **#10 `agent-w90-x11-win32`**: 必保留, 0 操作
- **#4 `agent-w89-x14-swipe-bug`**: 必报主拍, 0 擅自删
- 两个 worktree 本任务**仅观测**, 0 任何写入 / 删除 / 修改

派工 v10 段 7 E50/E79/E80 强制: 不擅自 rm -rf, 必报主拍签字. 派工前提铁律 12 第 5 条: 必先实测再决策.

## 1. WORKTREE-02 报告 vs WORKTREE-03 实测对比 (派工 v10 §1.2 据实上报铁律)

| 维度 | WORKTREE-02 报告 (1h 前) | WORKTREE-03 实测 (本任务) | 偏离 |
|------|--------------------------|---------------------------|------|
| **#10 branch** | `main` ⚠️ | `claude/w90-x11-win32` | **branch 已变** (FF sync + 后续 1 commit) |
| **#10 HEAD** | `33e9aa5e0` | `58c7dfb21` docs(w90) X-11 visual 治理冲突 22 张 win32 baseline 调研 | **HEAD +1 commit** |
| **#10 status** | clean (worktree 内), 父目录 12 untracked | clean (worktree 内), 父目录 12 untracked | 0 偏离 |
| **#4 branch** | `claude/w89-x14-swipe-bug` | `claude/w89-x14-swipe-bug` | 0 偏离 |
| **#4 HEAD** | `7a1978c1e` | `7a1978c1e` | 0 偏离 |
| **#4 origin** | MISSING | **MISSING** (实测 ls-remote grep 0 hit across 271 refs) | 0 偏离 |
| **#4 ahead** | 1 | 1 | 0 偏离 |
| **#4 behind** | 158 | 162 | **+4** (主仓又前进了 4 commit) |
| **main HEAD** | `c53444dfd` | `f13636888` | **+4 commits** (主仓 forward) |

### 1.1 主仓 HEAD 进化链 (W97 期间)

`c53444dfd` (WORKTREE-02 时) → `f13636888` (WORKTREE-03 时), 共前进 4 commits. 这些 commits 是 WORKTREE-02 报告后, 其他 agent (merge-01/02/03/04/05/06/07 等) 推入 main 的 FF 后结果.

## 2. #10 `agent-w90-x11-win32` 必保留 (派工 v10 段 7 E79 强制)

### 2.1 实测 5 项信息

```
HEAD:       58c7dfb212883b63dc71a46e443fccebbef2a58c
branch:     claude/w90-x11-win32
status:     clean (worktree 内), 父目录 12 untracked (agent-*/ 目录)
log HEAD:   58c7dfb21 docs(w90): X-11 visual 治理冲突 22 张 win32 baseline 调研 (W90-X-11)
origin:     EXISTS (refs/heads/claude/w90-x11-win32 = 58c7dfb21, 与 local HEAD 一致)
```

### 2.2 0 操作执行 (派工 v10 段 7 E79 强制)

**0 操作**:
- 不删 worktree
- 不删 branch
- 不改任何文件
- 不 push
- 不切 branch

**保留理由**:
- 主仓分支状态快照 (虽 branch 已不是 main, 仍是 W90-X-11 视觉治理调研成果)
- 父目录 12 untracked 现存
- origin EXISTS 备份 — 即使 0 操作, 主拍仍可从 origin 拉回

### 2.3 派工 v10 段 7 E79 风险预判

- **E79 风险**: 派工原 WORKTREE-02 描述"branch=main"暗示必保留, 但实测 branch 已变 `claude/w90-x11-win32`. 0 风险因为 origin EXISTS 备份 + main HEAD 已超越 58c7dfb21 (即 #10 落后 main, 但派工 brief 是"保留同步状态"不是"必保留 main branch")
- **缓解**: 本任务 0 操作, 仅观测. 主拍决策时参考实测数据 (branch=`claude/w90-x11-win32` 非 `main`)

## 3. #4 `agent-w89-x14-swipe-bug` 必报 (派工 v10 段 7 E50/E80 强制)

### 3.1 实测 5 项信息

```
HEAD:       7a1978c1e0bba6805bba414e69d9d25f4aeb278f
branch:     claude/w89-x14-swipe-bug
status:     clean (worktree 内)
log HEAD:   7a1978c1e refactor(w89): tests/e2e/ 重构 (15 vitest → tests/unit/components/, 3 playwright → tests/visual/e2e/) (W89-P-10)
ahead:      1 (相对 origin/main)
behind:     162 (相对 origin/main)
origin:     MISSING — 实测 git ls-remote origin 2>&1 | grep -E "w89-x14" 0 hit across 271 refs
```

### 3.2 0 操作执行 (派工 v10 段 7 E50/E80 强制)

**0 操作**:
- 不删 worktree
- 不删 branch
- 不改任何文件
- 不 push (origin MISSING 无法 push 备份)
- 不切 branch

**必报理由**:
- **origin MISSING** — local-only, 删前必报主拍
- 重要测试重构 (15 vitest → tests/unit/components/ + 3 playwright → tests/visual/e2e/)
- W89-P-10 完整的派工成果未被备份

### 3.3 4 选项派主拍决策 (派工 v10 段 7 E50 强制)

派工 v10 段 7 E50 强制: **不擅自决策, 报主拍**. 列出 4 选项 + 默认建议.

#### 选项 A: 保留 #4 + 0 commit 到 origin (local-only)

- 接受 local-only 状态 + 3.26GB 占用
- 适合: 主拍尚未评估 #4 commit 价值, 优先保护数据
- **风险**: 删前主拍再决策, 给出明确保留期

#### 选项 B: 先 push 到 origin backup branch + 保留 worktree (推荐)

- **必先 push** 创建 `claude/w89-x14-swipe-bug` on origin (备份)
- 推荐:
  ```bash
  cd /e/microbubble-agent/agent-w89-x14-swipe-bug
  git push origin claude/w89-x14-swipe-bug:claude/w89-x14-swipe-bug-backup-2026-07-30
  ```
- push 后保留 worktree, 主拍后续决策
- **本任务默认建议**: B (与其他 9 个 D 保留 cases 一致: origin 备份)

#### 选项 C: 先 `git diff` 取 patch + 删 worktree + branch + patch 入 `memory/`

- 适合: 主拍已确认 #4 commit 不需保留
- 执行:
  ```bash
  cd /e/microbubble-agent/agent-w89-x14-swipe-bug
  git diff origin/main..HEAD > /e/microbubble-agent/memory/w97-x14-swipe-bug-patch-2026-07-30.patch
  git worktree remove --force /e/microbubble-agent/agent-w89-x14-swipe-bug
  git branch -D claude/w89-x14-swipe-bug
  ```

#### 选项 D: 报主拍签字后 `git worktree remove --force`

- 最激进, 不可逆
- **警告**: 必主拍签字后才执行, 不容 agent 擅自
- 适合: 必须释放磁盘空间 + 主拍拒绝选项 B/C

### 3.4 派工 v10 段 7 E80 风险预判

- **E80 风险**: 派工"清理 12 个 active worktree"触发自动 `rm -rf` → 误删 #4 → 失去 origin MISSING 重要测试重构
- **缓解**: 本任务 0 操作, 派主拍 4 选项决策. 派工 v10 段 7 E80 严禁"删 #4 origin MISSING branch 不报"

### 3.5 派工 v10 段 7 E50 警告

- **派工 v10 段 7 派工原文错误**: "建议选项 B (push origin 备份后再决策)" — 但实测 origin MISSING, push 前需先看 `git remote -v` 是否能 push
- **缓解**: 默认建议从 B 降级为 **A 保留 + 主拍决策**. 0 任何操作

## 4. 5 件套守恒

| 件 | 实测 |
|----|------|
| 1 head (alembic) | **PASS** — 本任务 0 commit on main, main HEAD `f13636888` 不变 (注: 主仓 in-memory forward 不是我改动) |
| 2 pytest | **PASS** — 不动 tracked files, 0 测试影响 |
| 3 PWA build | **PASS** — 不动 web/dist |
| 4a 双门控 (0 老核心 diff) | **PASS** — 仅 memory 沉淀, 0 老核心改动 |
| 5 锚点范式 | **PASS** — 0 commit on main, 锚点不变 (W97 累计 +1 据实仅 memory) |
| **5/5 PASS** | **+ 1 派主拍决策 (选项 A/B/C/D)** |

## 5. 0 production code 改动铁律 守恒

本任务 **0 commit on #10 + #4 + 0 老核心 diff** — 完全守恒.

派工前提铁律 12 (W82 实战) + 派工 v10 段 7 E50/E79/E80 强制不擅自删, 实战 0 操作 + 派主拍 4 选项.

## 6. 派工 v10 段 7 错误 19 类 — 新增实例沉淀

### E50 实例 #7 (WORKTREE-03 据实上报 + 0 操作)

- **场景**: dispatch "#10 必保留 + #4 必报" + WORKTREE-02 报 #10 branch=main, 实测 branch=claude/w90-x11-win32
- **真相**: #10 branch 在 WORKTREE-02 ~ WORKTREE-03 间隔 1h 内已变 (FF sync + 后续 1 commit 进入 origin branch). #4 origin MISSING 持续
- **教训**: 派工"必保留"必含实测时刻 (本任务 recorded "12 untracked + 58c7dfb21 HEAD"), 不可信 1h 前的报告. 派工 v10 §1.2 据实上报铁律实战

### E79 风险预判 (WORKTREE-03 关键拦截)

- **场景**: 主拍默认签字 #10 `agent-w90-x11-win32` 因为 WORKTREE-02 报 branch=main 触发"不能动主仓"判断
- **拦截理由**: 实测 branch=claude/w90-x11-win32, 不是 main, 拦截误导
- **缓解**: 本任务实测 5 项 完整记录, 派主拍决策时参考实测数据

### E80 风险预判 (WORKTREE-03 关键拦截)

- **场景**: 派工"清理 active worktree" 触发自动删 #4 (origin MISSING) → 失去重要测试重构
- **拦截理由**: 派工 v10 段 7 E80 严禁不报主拍擅自删 origin MISSING branch
- **缓解**: 本任务 0 操作, 派主拍 4 选项决策 (默认 A 保留 + 主拍决策)

### E50 跨批次教训汇总 (本任务 1 增量)

- W82 C-2 派工 (锚点范式第 300 守恒): 363 branches + 209 worktree 清理, 仅清理确实 stale
- W97 WORKTREE-01 #5: 派工描述失误, 拦截 rm -rf
- W97 WORKTREE-02 #6: 派工列表不全 + 未列 5 项详情, 报主拍 4 选项
- **W97 WORKTREE-03 #7: 派工"必保留"引用 1h 前过时数据 (branch=main), 实测 branch 已变, 0 操作 + 据实上报**

## 7. 沉淀

- 本 memory 文件 (#10 + #4 实测 5 项 + 4 选项派主拍)
- 派主拍 (#4 4 选项 + #10 0 操作确认)
- 锚点 W97 +1 据实 (仅 memory commit, 不影响 main commit count)
- W97 派生类 20.13 实战 #23 (派工"必保留"必含实测时刻, 不可信 1h 前报告)

---

**执行 agent**: WORKTREE-03
**派工 ID**: WORKTREE-03 W97
**状态**: 0 commit on #10 + #4, 仅 memory 沉淀 + 派主拍决策 (4 选项 + 0 操作确认)
**锚点范式**: W97 +1 据实 (本 memory commit), main HEAD `f13636888` 不变
