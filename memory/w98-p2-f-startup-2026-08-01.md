# W98 P2-F 起步确认 — 2026-08-01

## 起步 6 项验证 (W73 铁律)

**S1 alembic heads**: `python -m alembic heads` → `093_add_search_log_answer_rating (head)` (1 head, 守恒)

**S2 派工 v10 必填字段读完** — 段 0-9 + 4 阶段 + 据实上报铁律确认

**S3 worktree 已切到** `E:/agent-w98-p2-f-wechat-sync` (branch `chore/w98-p2-f-wechat-sync`, 基于 main `4e6816c39`)

**S4 pytest 基线** — 起步时未跑完整 baseline, 留待 §4 守恒验证阶段补

**S5 微信 handler 路径真查** —
```
git ls-tree -r origin/main --name-only | grep -iE "wechat|wx"
```
找到核心 5 文件:
- `app/wechat/__init__.py`
- `app/wechat/analyzer.py`
- `app/wechat/bot.py`
- `app/wechat/crypto.py`
- `app/wechat/handler.py` ← **本任务接入点**
- `app/wechat/identity.py`
- `app/wechat/kf_service.py`
- `app/wechat/notifier.py`
- `app/wechat/scheduler.py`

**`agent.chat()` 在微信 handler 3 处 callsite** (line 488 / 1104 / 1211), 全部 `session_id + user_id=member.id + db` 都有, 微信 sync 已具备接入参数。

**S6 起步报本文件** — memory/w98-p2-f-startup-2026-08-01.md

## 关键约束
- 派工 prompt §2 "wechat_service.py / api/v1/wechat.py / integrations/wechat.py" **期望不对** — 真路径是 `app/wechat/handler.py`, 已按真实施路径修正 (§13.3 已落库假设禁令遵守)
- `_ensure_session_context` 位置: `app/agent/micro_bubble_agent.py:275`
- 配套依赖: `_fetch_pg_messages` (line 194) + `_get_last_pg_id` (line 245) + `_set_last_pg_id` (line 262) — 三者**均在** micro_bubble_agent.py, **必须连同一起抽**到 `session_context.py`, 否则新公共函数不能独立工作
- `chat_history_service.list_messages` 是只读复用, 不动

## 待 Phase 2 决策
- `ensure_session_context` 公共函数参数: (db, user_id, session_id) — 与现行签名一致
- 返回: `List[Dict]` 同样保持
- 微信 handler 接入: 在 3 处 `agent.chat()` 之前**预加载**会话上下文到 `session_manager`, 替代 handler 内部 Redis-only 读取