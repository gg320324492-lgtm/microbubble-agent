# PYTEST-01 W97 pytest collection error 修复 re-verify 收口 (2026-07-30)

> **任务**: PYTEST-01 re-verify 实测 — DERIVE-03 已修, PR10 + GRAND-CLOSURE 实测仍有 2 errors 须修
> **派工 brief**: `support-pytest-w79-fix`
> **分支**: main (直接 hotfix, 不开 worktree)
> **锚点范式**: W97 +0 → +1 据实 (478 → 479)

## 起步 6 项实测

### S1 git fetch + status
- ✅ `git fetch origin` 无更新
- ✅ `git status -sb`: `## main...origin/main [ahead 1]` (本任务修复 commit 上 push 后归零)
- ✅ main HEAD = `478950089` ([chore(w97-stash-01)] drop stash@{0})

### S2 Read 3 个历史 memory
- ✅ `memory/w89-rag-pr3-full-2026-07-30.md` (127 行) — PR3 e2e 22 case + pytest baseline 3230 (--ignore test_w79)
- ✅ `memory/w96-rag-pr10-grand-closure-2026-07-30.md` (68 行) — PR10 11 commits, 据实上报 4 项, 已含 "test_w79 已被修复"
- ✅ `memory/w97-rag-grand-closure-2026-07-30.md` (218 行) — W97 §6 件 2 = 3230 tests (--ignore), §8 项 C 仍报 **test_w79 + sentry/trivy 同 basename 2 errors 待主拍拍板**
- ⚠ brief 误记: "PR10 + GRAND-CLOSURE 实测仍有 2 errors" — PR10 memory §1 §3 明确写 "test_w79 已被修复" (实指 § 据实上报 1 "test_w79 已被修复" = 仍报错, 派工 brief 据此误读)

### S3 pytest 全量 co 修前基线
- 实测: `pytest tests/ --co` = **3230 tests collected, 1 error in 4.29s** (`ERROR tests/test_w79_commercial_private_deployment_e2e.py` — single error, sentry/trivy 同 basename 实际已无冲突)
- **brief 推测 2 errors 偏差**: DERIVE-03 commit `a1b2a1c75` 已合 main (锚点 338+0 据实), sentry file `test_glitchtip_dockerfile_pinning.py` ≠ trivy `test_dockerfile_pinning.py` (basename 不同) → 实际无冲突
- brief 写 2 errors 实测只 1 error (test_w79 bracket mismatch) → 派工 v10 §1.2 据实上报: 1 error ≠ 2 errors

### S4 sentry/trivy 目录独立 co
- 实测: `pytest tests/sentry/ tests/trivy/ --co` = **71 tests collected, 0 errors** ✅
- 确认 DERIVE-03 已生效 (`a1b2a1c75` rename sentry test_dockerfile_pinning → test_glitchtip_dockerfile_pinning), main HEAD `478950089` 包含此 commit
- 类 20 实战 #22 (派工 brief 错配) — 历史 §1 §3 已修, 不算未实施

### S5 pytest baseline (--ignore test_w79) 守恒
- 实测: `pytest tests/ --co --ignore=tests/test_w79_commercial_private_deployment_e2e.py` = **3230 tests collected, 0 errors in 3.61s** ✅
- 与 W97 grand-closure §6 件 2 baseline (3230) 守恒 +0 regression

### S6 件 4a 双门控实测
- 6 老核心服务 def diff = 0 (本任务不涉及修改, 仅加 1 行 commit diff)

## 真根因调研 (派工前提铁律 12 第 5 条: 实测看真错误)

### test_w79 bracket mismatch 真根因

```python
# tests/test_w79_commercial_private_deployment_e2e.py:253
for case in ("[1/4]", "[2/4]", "[3/4]", "[4/4]"]:  # 错: ( ... ] 错配
```

- Python AST 解析失败: `SyntaxError: closing parenthesis ']' does not match opening parenthesis '(' at line 253`
- 单字符 fix: `]` → `)` (将 list close 改为 tuple close)
- 历史源 PR (W79 commit `755e44e62` 商业化私有部署) 写了 tuple 写法但末尾笔误 `[` 漏写, **pre-existing on main 未经 pytest --co 强制同步验证**

### sentry/trivy 同 basename 真状态

```bash
ls tests/sentry/:
  test_config_off_by_default.py
  test_glitchtip_dockerfile_pinning.py  (DERIVE-03 后)
  test_main_js_conditional.py

ls tests/trivy/:
  test_dockerfile_pinning.py
  test_workflow_exists.py
```

- 两目录**没有同名**文件 (sentry = `test_glitchtip_dockerfile_pinning.py` ≠ trivy = `test_dockerfile_pinning.py`)
- pytest sentry+trivy co = 71 tests, 0 errors ✅
- DERIVE-03 (commit `a1b2a1c75`, branch `chore/w89-rag-pytest-trivy-sentry-conflict-2026-07-30`) **已合 main** (本任务 §1 S4 已实测验证)

## 修复过程

### Step A: 修 tests/test_w79_commercial_private_deployment_e2e.py

```bash
# 1. Read line 253 上下文
$ cat -n tests/test_w79_commercial_private_deployment_e2e.py | sed -n '250,256p'
   250     body = mon.read_text(encoding="utf-8")
   251     assert "set -e" in body, "monitor missing 'set -e'"
   252     # 4 case 标识
   253     for case in ("[1/4]", "[2/4]", "[3/4]", "[4/4]"]:
   254         assert case in body, f"monitor missing {case}"
   255
   256     # bash 语法检查

# 2. Edit 1 character: ( → [ 关括号 (?,改 ( ...)
# 实际 old_string: ...("[4/4]"]:
# 实际 new_string: ...("[4/4]"):
```

- 1 行改动: `]` → `)` (single-character fix, 严格遵守 E56 "仅改测试文件" — 仅改 1 个字符, 0 老核心 diff)
- Edit 工具精确替换

### Step B: 二次验证 (a) test_w79 collect

```bash
$ pytest tests/test_w79_commercial_private_deployment_e2e.py --co
11 tests collected in 0.05s   # ✅ 0 errors
```

### Step C: 全量验证 + sentry/trivy re-verify

```bash
$ pytest tests/ --co
3241 tests collected in 3.83s   # ✅ 0 errors (3230 + 11 test_w79)

$ pytest tests/ --co --ignore=tests/test_w79_commercial_private_deployment_e2e.py
3230 tests collected in 3.61s   # ✅ 0 errors, 与 W97 baseline 守恒

$ pytest tests/sentry/ tests/trivy/ --co
71 tests collected in 0.05s   # ✅ 0 errors, DERIVE-03 已生效
```

### Step D: pyc 清理 (CLAUDE.md 752 行铁律)

```bash
$ find tests/ -name "__pycache__" -type d -exec rm -rf {} + 2>/dev/null
$ find tests/ -name "*.pyc" -delete 2>/dev/null
$ pytest tests/ --co
3241 tests collected in 7.94s   # ✅ pycache 清理后 0 errors
```

### Step E: 创建修复 commit

```bash
$ git add tests/test_w79_commercial_private_deployment_e2e.py
$ git commit -m "fix(w97-pytest-01): test_w79 bracket mismatch (Python AST syntax)
  ...
  Co-Authored-By: Claude Fable 5"

[main 49810ec04] fix(w97-pytest-01): test_w79 bracket mismatch (Python AST syntax)
 1 file changed, 1 insertion(+), 1 deletion(-)

To github.com:gg320324492-lgtm/microbubble-agent.git
   478950089..49810ec04  main -> main   # push origin 成功
```

- commit hash: `49810ec045` (`fix(w97-pytest-01)`)
- 锚点: 478 → 479 (PYTEST-01 W97 +1 据实, 派工 v11 段 9 锚点前缀规则)

## 5 件套守恒实测 (派工 v11 段 6 回报表)

| 件 | 命令 | 实测输出 | 判定 |
|----|------|---------|------|
| 1 | `python -m alembic heads` | `091_add_kg_entity (head)` | ✅ 1 head |
| 2 | `pytest tests/ --co` | `3241 tests collected in 3.83s` | ✅ ≥ 3230 + 0 errors |
| 3 | `cd web && npm run build` | (沿用 main pre-existing FAIL, 待 HOTFIX-01 merge) | ⚠ 沿用 (无变更) |
| 4a | `git diff main~1 -- app/services/{knowledge,hybrid_retriever,embedding,bm25,text_splitter,rag_evaluator}.py \| grep -cE "^[+-]def"` | `0` | ✅ 0 老核心 def |
| 5 | `git log --grep "PYTEST-01 W97" --oneline \| wc -l` | `1` | ✅ ≥ 1 |

## 派工 v10/v11 错误 19 类 (PYTEST-01 specific) 实战

| 类 | 状态 | 详情 |
|----|------|------|
| E29 件 4a 双门控 | PASS | 6 老核心服务 def diff = 0, 改 tests/test_w79_*.py 仅 1 字符 |
| E56 误改其他测试文件 | PASS | 仅改 tests/test_w79_commercial_private_deployment_e2e.py (1 行) |
| E57 删 __pycache__ 破坏 cache | PASS | find -exec rm -rf (本机无 docker) |
| E58 sentry/trivy 未合 | PASS | DERIVE-03 (a1b2a1c75) 已合 main, sentry+trivy co = 71 tests, 0 errors |

## 据实上报 (brief vs 实测, 2 项)

1. **brief 写 2 errors, 实测 1 error**: 派工 brief 推测 "DERIVE-03 + PR10 + GRAND-CLOSURE 实测仍有 2 errors" (test_w79 syntax + sentry/trivy 同 basename) —— 实测:
   - test_w79 syntax = 1 error ✅ 实有
   - sentry/trivy 同 basename = **0 conflict** ✅ DERIVE-03 修复已合 main (a1b2a1c75), `test_glitchtip_dockerfile_pinning.py` ≠ `test_dockerfile_pinning.py`, 派工 v10 §1.2 "据实上报" 接受 1 ≠ 2
2. **brief 写 "重新派 DERIVE-03 类任务"**: 派工 brief 担心 "MERGE-01 merge 时冲突或 cache 问题" 致 sentry/trivy 仍冲突, 实测 main `478950089` 已含 DERIVE-03 commit, 不需 rebase 重派

## 类 20 实战累计 (本次)

- **类 20 实战 #35 (PYTEST-01 错配)**: 派工 brief 推测 2 errors 实测 1 error, brief §1.2 "派工前提必真验证" 接受据实上报 (1 error ≠ 2 errors, 不擅自扩也不擅自缩)
- **类 20 实战 #36 (派工 brief 假设错误)**: brief 假设 "sentry/trivy 仍冲突 → 必 rebase 重派 DERIVE-03", 实测 main 已含 a1b2a1c75 修复, 不需重派
- **类 20 累计**: 34 + 2 = **36 实例** 据实上报 (W97 累计)

## 缺口消化 (test_w79 据实闭合)

| 项 | 描述 | 处置 |
|----|------|------|
| pytest baseline error | 1 个 Python AST syntax error (test_w79 line 253 bracket mismatch) | 单字符 fix (`]` → `)`), 1 commit, no alembic / no service / no model |
| sentry/trivy 同 basename | DERIVE-03 a1b2a1c75 已合 main, 实际 0 conflict | 沿用 (派工 brief 误判, 据实上报) |
| 件 3 PWA build | pre-existing FAIL (PR5 `cb5c98498` Play import) | 沿用 (待 HOTFIX-01 merge, §8 项 A 主拍决策) |

## 收口沉淀

- commit: `49810ec045` `fix(w97-pytest-01)` (push origin 成功)
- 锚点: 478 → 479 (W97 +1 据实, 累计 W95 350 → W97 479)
- 件 2 守恒实测: **3241 collected, 0 errors** (W97 grand closure baseline 3230 + test_w79 11 全部 collecting)
- 件 4a 双门控: 0 老核心 def diff
- 类 20 实战: +2 (总 36)
- 件 5 PWA build: 沿用 main pre-existing (3 件套 frontend=否, 沿用基线)

## 后续衔接

- **W98 主拍**: pytest 0 errors 已达成, 可继续派 W98 后端 PR / W99 docs PR
- **件 3 PWA build FAIL**: 仍待 HOTFIX-01 merge (W94 commit `c8aa1112b` branch `chore/w94-rag-pr5-play-hotfix-2026-07-30`), merge 后 PWA 应 PASS
- **派工 v11 段 9 锚点前缀**: 本任务已遵守 `PYTEST-01 W97 +1` + `Co-Authored-By: Claude Fable 5`

Co-Authored-By: Claude Fable 5
