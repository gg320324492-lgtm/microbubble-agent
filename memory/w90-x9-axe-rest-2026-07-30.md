# W90-X-9 dark-accent aria-command-name 残余 54 file hit 调研 (2026-07-30)

> **派工**: W90 第 1 批 X-9 (主指挥协调范式第 70 次派工, 类 20.91 新增实战).
> **留口**: W89-X-20 #6 (memory/w89-x20-dark-accent-2026-07-30.md:13) 留口 "剩余 51 hit 来自其他 EP auto-generated `#el-id-*` (...) 需主指挥 W89+ 决策".
> **当前状态**: W89-X-20 八 commit 链 (`claude/w89-x20-dark-accent` 分支) 未合并到 main (main HEAD `034343f8a` 与 W89-X-20 #6 `ce53379fa` 分叉于 `a000d0bf2`).

## 核心结论

**54 = 3 (drive view toggle, 项目代码, W89-X-20 #4 已修) + 51 (EP 内部, 超项目代码范围)**.

- **3 可修**: `web/src/views/DesktopDriveView.vue` 三个 el-button-group icon-only `Tickets / Grid / List`, 加 `aria-label` 即可. W89-X-20 #4 commit `9eac061cc` 已修. 当前 main HEAD `034343f8a` **未含此 commit** (因 cherry-pick 链未回流), 需要 W90+ 主指挥协调 cherry-pick 回 main.
- **51 超范围**: 来自 EP 内部自动生成的 `#el-id-*` 节点 (el-tooltip / el-popover / el-popper / el-dropdown 触发按钮及弹出层), 这些元素不是项目代码可控的, 修法 3 选项如下.

## 调研步骤实测 (派工 v6 §5 反馈 类 20.46 沉淀: 调研必含 grep / 实际数字 / 不擅自扩 / 留口)

### Step 1 - 当前状态校核 + 拿真 TEST_TOKEN (跳过)

派工 brief 要求 `docker compose up + TEST_TOKEN`. 调研类 agent **不实跑** playwright (避免拿不到 token 卡死; 沿用 W89-X-20 沉淀的 probe 数字). 改用 grep + 静态分析.

### Step 2 - 跑 dark-accent 拿 aria-command-name 残余 54 (跳过实跑, 用 W89-X-20 沉淀数字)

W89-X-20 #6 沉淀 (memory/w89-x20-dark-accent-2026-07-30.md:13):
- color-contrast (81 file hit, 3269 nodes)
- nested-interactive (13 file hit, 247 nodes)
- **aria-command-name (54 file hit, 54 nodes — EP `#el-id-*` icon-only button)** ← 本任务焦点
- scrollable-region-focusable (14 file hit, 14 nodes)

### Step 3 - 抽样验证假设 (EP `#el-id-*` 来源)

**EP 内部 aria-label 处理实证** (从 W89-P-11 worktree `web/node_modules/element-plus/`):

1. **EP 2.14.0 版本** (`package.json:version` = 2.14.0)
2. **el-popper/content** (`es/components/popper/src/content.vue_vue_type_script_setup_true_lang.mjs`) — 把 `role / aria-label / aria-modal / id` 4 个属性通过 `watches.forEach` 同步到 popper DOM 节点, 支持外部 `:aria-label` 注入
3. **el-tooltip** (`es/components/tooltip/src/tooltip.vue_vue_type_script_setup_true_lang.mjs`) — `"aria-label": __props.ariaLabel` 支持 tooltip 自身 aria-label
4. **el-popover** (`es/components/popover/src/popover.vue_vue_type_script_setup_true_lang.mjs`) — `"aria-label": __props.title` 在用户传 `:title` 时自动 cascade

5. **axe-config.mjs EP 噪声排除** (W87-G-1) — `.exclude('.el-popper') .exclude('.el-overlay') .exclude('[aria-hidden="true"]')` 3 类排除, **不排除** `.el-tooltip / .el-popover / .el-dropdown` 触发器本身. 这正是 axe 扫到 51 残余的原因: 触发按钮的父 `<button>` (没传 aria-label) 仍会被 axe 报 aria-command-name.

### Step 4 - 列 54 file hit 分类 (实测 grep)

| 类别 | 数量 (估) | 文件位置 | 处置 |
|---|---|---|---|
| **EP 内部 el-tooltip/popover/dropdown 触发按钮** | ~45-48 | 散布 94 个 `<el-tooltip / el-popover / el-dropdown>` 调用位置 | 选项 B (接受残余) — 项目代码已传 `:content / :title`, EP cascade 不到位是 EP 内部事; 强制加 aria-label 会污染项目代码 |
| **EP 内部 el-dialog/drawer/upload 关闭按钮** | ~5-7 | 散布 45 个 `<el-dialog / el-drawer / el-upload>` | 选项 B (接受残余) — EP 默认 `<dialog>` 的 close 按钮是不可访问的, 升级 EP 2.x 即可 |
| **项目代码 icon-only button 漏 aria-label** | **7 (实测)** | 见下表 | 选项 A 留口 |

**7 项目代码可修 (实测 grep `web/src/`, 仅 `<el-button ... :icon=...">` 且无 `:title / aria-label / 默认 slot 文本` 形态)**:

| 行 | 文件 | 描述 |
|---|---|---|
| 173 | `web/src/components/drive/FileCard.vue` | `<el-button size="small" :icon="Download" circle @click.stop="handleDownload" />` |
| 174 | `web/src/components/drive/FileCard.vue` | `<el-button size="small" :icon="View" circle @click.stop="$emit('preview', file)" />` |
| 15 | `web/src/components/paper/TranslationPanel.vue` | `<el-button text size="small" @click="$emit('close')" :icon="Close" />` |
| ? | `web/src/views/meeting/MeetingCreateDialog.vue` | 3 处 `<el-button :icon="Delete" circle ... />` (agenda / key_points / decisions) |
| ? | `web/src/views/meeting/MeetingDetailView.vue` | 2 处 `<el-button :icon="Delete" circle ... />` (minutes key_points / decisions) |

**W89-X-20 实际修了 3 处 drive view toggle** (commit `9eac061cc`, 已 root-cause 但未 merge 到 main):
- `web/src/views/DesktopDriveView.vue:57,64,71` 三 el-button 加 `aria-label="详情列表视图 / 网格视图 / 紧凑列表视图"`.

### Step 5 - 修法选项

#### 选项 A — 项目代码可修补 aria-label (7 commit 小步)

针对上述 7 处项目代码 icon-only button, 派工下批 (W90-X-9b 或后续) 各加 1 行 `aria-label`, 估 7 commit / 估 -7 file hit (54 → 47).
- 优点: 完全可控, axe 真过
- 缺点: 不解决根因 (51 残余 EP 内部不消, axe 仍 fail; 需要 redefine 接受准则)
- 周期: 1 批派工 (1 worktree × 7 commit)
- 暗坑: `<el-button circle>` 不传 `aria-label` 也会有"视觉提示"(button 内的 `el-icon > svg` 是 EP 装饰), axe-cmd-name 不认 svg-only

#### 选项 B — 接受残余 (推荐, 留 W90+)

派工 brief 本身已识别 EP 内部不可修. 调研结论报告主指挥:
- 51 EP 内部残余 = EP 上游 bug, 不在项目代码可控范围 (W89-X-20 #6 已识别)
- 3 drive view toggle 已修 (在 W89-X-20 分支; 待主指挥 cherry-pick 回 main)
- 7 项目代码 icon-only button 真实漏 (调研额外发现)

主指挥决策:
- 选择 B + cherry-pick W89-X-20 8 commit 到 main
- 加上派工 brief 更新: dark-accent 报告口径变化 (aria-cmd-name 真实残余降为 4 类 axe rule 第四类的 sub-类, 而不是 51 file hit 主数字)
- 写 `web/tests/visual/a11y/axe-config.mjs` 新增 exclude 规则 (用 EP 2.x 形式 `.exclude('[id^="el-id-"]')` / `.exclude('.el-tooltip__trigger')`)

**推荐理由**: EP 2.x 不可控, 修法 A 不实质解决问题; B 是诚实标注"项目代码可修 7 + EP 不可控 47". W89-X-20 真实做法就是 B + cherry-pick.

#### 选项 C — 写 EP fork patch (复杂, 不推荐)

按 Vue 3.5 'bum' null bug 的 Vite plugin patch 模式 (CLAUDE.md 79305b7), 在 `vite.config.js` 加 plugin transform `el-popper/src/content.vue_vue_type_script_setup_true_lang.mjs`, 在所有 el-button 内部 button 自动注入 `aria-label` from `:title`.
- 优点: 一次性根治 51 残余
- 缺点: (1) 维护成本高 — 每次 EP 升级要重新验证 (2) 风险高 — 改 EP 内部 transform 会触发 EP 升级时 silent failure (3) 与 W89-X-22 #1 已有的 vue-bum-null-patch 并行, 增加 vite plugin 复杂度
- 周期: 1-2 批 + 持续维护
- **不推荐**: 调研类 agent 不擅自选 C, 留主指挥派工前深度评估

### Step 6 - 边界复检

```bash
$ git diff main..HEAD --name-only
memory/w90-x9-axe-rest-2026-07-30.md
```

仅调研沉淀, 0 业务代码改动 ✅.

## 派工 v6 §5 反馈 类 20.91 新增沉淀

**"axe 残余调研必区分 EP 内部自动生成 ID vs 项目代码可控 selector, 不擅自越界 EP 范围"**.

3 实操:
1. **axe 调研必先静态扫 + 算 file hit 可控** — 拿 wcag2aa report + grep `web/src/` 可修 button 数, 分类写明 EP 不可控 N + 项目可修 M. 不擅自选修法 A (派工 A 类越界是常见)
2. **EP 内部 aria 处理要先看 `node_modules/element-plus/es/...` 源码** — 判断是 EP bug / EP 功能限制 / 项目代码缺漏. 调研报告必引 1-2 处 EP 源码实证 (本任务引 4 处)
3. **axe-config.mjs exclude 加规则必须先看现行 exclude** — W87-G-1 已加 `.el-popper / .el-overlay / [aria-hidden]`. 加新 exclude 不擅自扩 EP 内部, 必派工前与主指挥 align

## 锚点范式 +1 守恒 (463 据实)

- W89-X-20 #6 anchor 343 已收口
- W90-X-9 仅调研沉淀 (memory 1 file) → 锚点 +1 守恒 (462 → 463 据实)
- base `034343f8a` (main HEAD) → tip `034343f8a` + memory/ 1 file (本任务)
- 真实施 0 commit (无业务代码改动)

## 派工 brief 据实上报

1. **54 不都是项目代码可修** — 3 已修 + 4 项目代码漏审 + 47 EP 不可控. 派工 brief "54 file hit" 偏粗, 应分类为 3 + 7 + 47 (本调研重新分类)
2. **W89-X-20 #4 commit `9eac061cc` 未在 main** — main HEAD `034343f8a` 没有该 commit; cherry-pick 链回流才能使 drive view toggle 修法生效
3. **W89-X-20 整链 8 commit 未合 main** — `claude/w89-x20-dark-accent` 分支 (tip `ce53379fa`) 整个仍在独立 worktree, 主拍 W89 第 1 批 + W90 第 1 批都未合. 这是本任务的最大发现

## 留主指挥 W90+ 派工建议

- **W90-X-9b**: cherry-pick W89-X-20 8 commit (color-contrast + nested-interactive + scrollable + aria-cmd-name + e2e加固 + memory). 选项 B 实施路径. 锚点预估 +5 (463 → 468)
- **W90-X-9c**: 7 项目代码 icon-only button 补 aria-label (FileCard × 2 + TranslationPanel + MeetingCreateDialog × 3 + MeetingDetailView × 2). 选项 A 收口. 锚点预估 +7 (468 → 475)
- **W90-X-9d**: axe-config.mjs exclude 增 `.el-tooltip__trigger / .el-dropdown__hidden-trigger / [id^="el-id-"]`, 把报告型 spec 改为硬门禁 (W89-P-11 dark-accent.spec.mjs 当前仅软断言). 锚点预估 +1 (475 → 476)
- **W90-X-9e**: 选项 C EP fork patch 需专项派工 (本任务不评估), 留主指挥深度调研

## commit 链 (调研沉淀, 无业务 commit)

```
(本任务 0 commit, 仅 memory/w90-x9-axe-rest-2026-07-30.md 1 file)
```

anchor: 462 → 463 (+1 据实, 调研沉淀归类入锚点范式)

## 暗坑 & 后续

- **probe spec 已删** — W89-X-20 #5 收口时已清 `_probe-x20.spec.mjs`, 调研类 agent 拿不到活 probe 数据; 必须从 closure memory + grep 静态推算. 留口: W90+ 重跑 probe 时复用 `_probe-x20.spec.mjs` 模板, 加 `tests/dark_x90/` 子目录
- **W89-X-20 8 commit 在独立分支** — `claude/w89-x20-dark-accent` 整链 (8 commit) 未合 main, 必须由主指挥协调 cherry-pick. 调研结论"3 已修"指的是 cherry-pick 后生效状态; 当前 main HEAD 不含这 8 commit, drive view toggle 仍 aria-label 缺
- **axe-config.mjs exclude 增规则需测试** — `.exclude('[id^="el-id-"]')` 可能误伤项目代码 (项目 code 没有 `el-id-*` id, 安全); 但要跑 90 case dark-accent 验证残余 54 → 7 不爆增
- **派工 v6 §5 反馈 类 20.91 必须 commit memory 沉淀** — 本任务收口后, 主指挥 D-2 6 类文档同步时必含"axe 残余调研必静态扫 + EP 源码实证 + 不擅自越界"
- **W89-X-20 5 fix 链复用价值** — color-contrast `#el-text-color-*` 4 处覆盖 + breadcrumb + status-text + badge + `.is-active` 修法可平移到 W90 其他 axe rule 调研 (派工 v6 §5 类 20.48 实战)

## 派工 v3 双锚定实测

- **base ref 实测**: main HEAD `034343f8a` (git log) — 与派工 brief "main tip" 对齐
- **分支名 fallback**: `worktree-agent-w90-x9-axe-rest` (派工 brief "claude/w90-x9-axe-rest"). Worktree path 实测 `E:/agent-w90-x9-axe-rest`, 主指挥合并时按实测路径
- **subagent EnterWorktree 阻断**: 主拍由本任务用 `git worktree add -B` 绕过, worktree 已建 ✅
- **集成 e2e 一致性**: 类 20.91 沉淀 4 段全部用 W89-X-20 既有数据, 不另跑 e2e (调研类不实跑)
