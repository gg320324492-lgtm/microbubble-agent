# W94 PR8 知识图谱深度联动 — 收口全量据实 (2026-07-30)

> **任务**: PR8 W94 +0 → +20 模板 / **实测 17 commits 据实**（知识图谱深度联动）
> **基准**: main HEAD `034343f8a`（MERGE-03 收口，锚点 459）
> **alembic**: `090_add_rag_eval_report` → **`091_add_kg_entity`**
> **⚠️ PR8 是 10 PR 中最后 1 个 alembic PR** — 091 之后 alembic 链正式收口（PR9/PR10 无迁移）
> **分支**: `chore/w94-rag-pr8-knowledge-graph-2026-07-30`（**未 merge 未 push**，主指挥拍板）

---

## §1 5 件套守恒回报表（派工 v11 新增 6，命令输出原文粘贴）

| 件 | 命令 | 实测输出（原文） | 判定 |
|----|------|----------------|------|
| 1 | `python -m alembic heads` | `091_add_kg_entity (head)` | ✅ 恰 1 head = 091 |
| 2 | `pytest tests/rag/test_pr8_e2e.py -q --ignore=tests/test_w79_...` | `22 passed, 2 warnings in 0.20s` | ✅ **22/22 PASS** |
| 2b | `pytest tests/ --co -q --ignore=tests/test_w79_...` | `3230 tests collected in 3.26s` | ✅ 3208 + 22 = 3230 **精确守恒** |
| 3 | `cd web && npm run build` | `✗ Build failed in 3.93s` / `"Play" is not exported by "@element-plus/icons-vue"` @ `src/views/admin/RAGEvalPanel.vue:24` | ⚠️ **pre-existing FAIL**（PR5 引入，非 PR8，见 §5） |
| 4a | `git diff -U0 main -- <6 锁定老核心> \| grep -cE "^[+-]def"` | `0` | ✅ **0** |
| 4a' | 同上 `\| grep -cE "^[+-]    (async )?def "` | `0` | ✅ **0 类方法/缩进函数改** |
| 4a'' | `git diff --stat main -- <6 锁定老核心>` | `hybrid_retriever.py \| 127 +++`<br>`knowledge_service.py \| 14 +++`<br>`2 files changed, 141 insertions(+)` | ✅ **0 deletions 纯追加** |
| 4b | `git diff --stat main -- web/` | （空输出） | ✅ **0 web 改动**（frontend=否） |
| 4c | `git diff --stat main -- alembic/versions/` | `091_add_kg_entity.py \| 160 +++`<br>`1 file changed, 160 insertions(+)` | ✅ 仅新增 091，**0 改老迁移** |
| 5 | `git log --grep "PR8 W94 +" --oneline \| wc -l` | `14`（本 memory commit 前）→ 收口后 **17** | ✅ 全 commit 带前缀 |

---

## §2 17 commits 据实清单（模板 21，锚点合并据实上报）

| # | commit | 内容 |
|---|--------|------|
| +0 | `80d910ea6` | kg_entity ORM 模型（`KGEntity` + 8 类白名单 + 归一化/类型映射） |
| +1 | `496152c59` | alembic 091 + idempotent guard（HNSW CONCURRENTLY 二段式） |
| +2 | `184690fdd` | entity_link_recall 实体链召回（第 5 路，PG 内置） |
| +3 | `6ee6ee3b1` | kg_embedding 实体向量（**复用 PR1 truncate_for_embedding**） |
| +4 | `d7ec7c27e` | knowledge_graph_service 接入实体链（模块级，0 类方法改） |
| +5 | `70916056b` | knowledge_service Step 5b 钩子（+14，0 删除） |
| +6 | `d3866b464` | hybrid_retriever KG retrieval path（+127，0 删除） |
| +7 | `98cf81d10` | **22/22 PASS** e2e（含模板 +8/+9/+10/+11 四项，见下） |
| +12 | `bccce832c` | RUNBOOK.md §0.7 + §0.7.1 + §0.7.2 |
| +13 | `1b6fb29d6` | SCHEMAS.md §10（7 件套 → 10 件） |
| +14 | `59961f0f9` | W94-PR8-ANCHOR.md CLAUDE.md 镜像（11 节） |
| +15 | （见 git log） | 双 CHANGELOG（docs/rag + 主仓） |
| +16 | `22b119198` | README.md 近期新增段 |
| +17 | `a0ebcd494` | CHECKLIST §J（24 项） |
| +18 | 本 commit | memory 收口 |
| +19 | （见 git log） | 类 20 #33/#35 沉淀 |
| +20 | （见 git log） | GRAND-CLOSURE 前置（10 PR 串单链收口） |

### 锚点合并据实（派工 v11 新增 3：验证型 0 增量不凑 +1）

模板 21 commits，**实测 17**。差 4 个的真实原因：

| 模板条目 | 处置 |
|---------|------|
| `+8` 图谱召回 P95 ≤ 100ms 性能断言 | 落在 `+7` 的 **case 20**（真计时 20 samples） |
| `+9` 实体数 ≥ 5000 验证 | 落在 `+7` 的 **case 21**（`count_entities()` 真调用 + `assert_awaited()`） |
| `+10` 与 PR3 BM25 / PR5 RAGEvaluator 集成测试 | 落在 `+7` 的 **case 19**（件 4a subprocess 实测 6 老核心 + rag_evaluator 可用性） |
| `+11` 边界 + 实体漂移检测 | 落在 `+7` 的 **case 22**（空 query / 无种子 / 归一化一致性 / 0 regression） |

4 项**全部真实施**（在 22 case 内），只是同属一个测试文件的单次交付。**不为凑模板拆 4 个无意义 commit**（会违反 W82/W84 据实上报铁律"禁止凑锚点"）。

---

## §3 派工 brief 错配 3 处据实上报（类 20 #33 / #35，§12.3.4 拦截不擅自改）

| # | brief 写 | 仓库实情（真查） | 处置 |
|---|---------|----------------|------|
| 1 | 新增 `app/models/kg_entity.py`（entity_name / entity_type / knowledge_id FK / vector / first_seen_at / last_seen_at / mention_count） | `app/models/knowledge_entity.py` **已存在** `KnowledgeEntity`（**SPO 三元组**: subject/predicate/object/condition/confidence/`source_knowledge_ids` ARRAY/occurrence_count/embedding Vector(1024)）+ `EntityCoOccurrence`（共现网络） | 新建 `KGEntity`（**扁平命名实体**）**互补非替代** —— 与 PR5 处置 `RAGEvaluationReport` vs 已有 `RAGEvaluation` **完全同款模式**。091 仅建 `kg_entities` 新表，**0 改**两老表（走 lifespan `create_all` 无 alembic） |
| 2 | `knowledge_service.py` **仅新增**实体抽取钩子 | 实体抽取钩子**已存在**: Step 5 `merge_entities_from_document`(L302) + Step 6 Neo4j `build_graph_for_knowledge`(L311) + Step 7/7b multimodal | **不新建**"实体抽取"钩子，改为 Step 5 **之后**追加 **Step 5b** kg_entities 钩子，**复用 Step 5 产物** |
| 3 | `hybrid_retriever.py` **仅新增** KG retrieval path | `_graph_search` **已存在**（L218-267，Neo4j 路，固定 score 0.7）；`retrieve`/`_retrieve_impl` 4 路已含 `enable_graph` | **0 改** `_graph_search`/`retrieve`/`_retrieve_impl`/4 路权重默认，改为模块级**第 5 路** `retrieve_with_entity_link`（沿用 PR4 `retrieve_with_weights` "新 API，不动原 retrieve" 已批模式） |

**根因**: PR8 brief 派生自 plan v1.0 §11.2，未做 DERIVE-18 §13 仓库实情真查。plan v1.2 已修正**前端**路径（§11.2 第 544 行 `pages/*.tsx` → `views/*.vue`），但**后端 KG 资产未盘查** —— 项目已有 **5 个 KG 服务（1645 行）+ 2 个 ORM 模型**，brief 假设"KG 从 0 建"与实情不符。

**§12.3.4 遵守**: 3 处**全部据实上报 + 不擅自改 brief 语义**。处置均为"新增不动老"（与 brief 的 0 production code 双门控目标一致），**非**"把 brief 的 A 改成 B 后继续实施"。

### 已有 KG 资产 0 改清单

| 服务 | 行数 | PR8 改动 |
|------|------|---------|
| `entity_service.py` | 402 | **0 改** |
| `graph_retriever.py` | 188 | **0 改** |
| `kg_query_service.py` | 266 | **0 改**（仅复用其白名单范式） |
| `knowledge_graph_builder.py` | 289 | **0 改** |
| `knowledge_graph_service.py` | 500 | **0 类方法改**（仅文件底部模块级追加） |
| 合计 | **1645 行** | 0 类体触碰 |

老表 `knowledge_entities` + `entity_co_occurrence`: **0 改**（091 不动）。

---

## §4 e2e 2 例真失败修根因实录（未弱化断言）

| # | 失败 | 根因 | 修法 | 断言变化 |
|---|------|------|------|---------|
| case 15 | `patch("app.services.embedding_service.generate_embedding")` → `ModuleNotFoundError: No module named 'sentence_transformers'` | 本机 **ST 未装**（plan §3.7 已预警），`import embedding_service` 直接崩 → `patch` 无法 resolve target | `sys.modules` 注入 stub 模块验证成功路（比 `importorskip` 跳过**更强** —— 真跑 lazy import 契约）+ 用真实缺装环境验证降级路（不 mock，证明生产缺依赖时召回侧静默 None 不崩） | **加强**（新增 `assert_awaited()` / `assert_not_awaited()` 验证空名不调 embedding） |
| case 19 | `UnicodeDecodeError: 'gbk' codec can't decode byte 0x94` | Windows 默认 gbk 解码含中文的 `git diff` 输出必崩 | `subprocess.run(encoding="utf-8", errors="replace")` | 不变 |

**纪律**: 真失败必修根因。**禁止**改阈值 / 删断言 / 加 `xfail` / `skip` 凑 PASS（W82/W84 据实上报铁律）。

---

## §5 件 3 PWA build pre-existing FAIL 据实（v11 新增 5）

```
✗ Build failed in 3.93s
error during build:
[vite-plugin-pwa:build] "Play" is not exported by "node_modules/@element-plus/icons-vue/dist/index.js",
  imported by "src/views/admin/RAGEvalPanel.vue"
file: E:/microbubble-agent/web/src/views/admin/RAGEvalPanel.vue:24:18
24: import { Refresh, Play, DataAnalysis } from '@element-plus/icons-vue'
```

**归因证据**（非 PR8）:
- `git log --oneline -1 -- web/src/views/admin/RAGEvalPanel.vue` → `cb5c98498 [PR5 W91 +7] feat(pwa): RAGEvalPanel.vue + useRAGEval.js + router`
- `git status --porcelain -- web/` → **0 dirty**（build 前主仓 web/ 干净）
- `git diff --stat main -- web/` → **空**（PR8 frontend=否，0 web 改动）
- worktree 内 `web/node_modules` **不存在** → 按 v11 新增 5 走**主仓等价验证**

**处置**（v11 新增 5 + 0 production code）: pre-existing 故障据实上报，**不算本 PR FAIL**，**也不顺手修**。
**建议主拍派 hotfix**: `Play` → `VideoPlay`（Element Plus icons 无 `Play`，有 `VideoPlay`）。此 FAIL 会**阻塞生产部署**（PWA 410 4 层第 1 层），优先级 P0/P1。

---

## §6 门禁 4 项实测（plan §2 PR8 + §9）

| 门禁 | 阈值 | 实测 | 判定 |
|------|------|------|------|
| a 实体链 hit | ≥ 25% | `ENTITY_LINK_HIT_TARGET=0.25`；case 10 真算 3/10 = **30% PASS**，反例 1/10 = 10% 判失败 | ✅ 逻辑层 |
| b 图谱召回 P95 | ≤ 100ms | `ENTITY_LINK_P95_BUDGET_MS=100.0`；case 20 真计时 **20 samples** P95 断言；运行时超预算 `logger.warning` | ✅ |
| c 实体数 | ≥ 5000 | `ENTITY_COUNT_TARGET=5000`；case 21 `count_entities()` 真调用 + `assert_awaited()` + 门禁比较 + 异常降级返 0。**真库计数需生产 DB**（本机 PG 未连），按 RUNBOOK §0.7.1 第 3 步跑真 SQL。**不脑补数字** | ✅ 路径 / ⏸ 生产待验 |
| d qa-bench | ≥ 96% | **按推荐不跑**（沿用 PR1/PR5 处置）。PR8 为 backend 新增路径，第 5 路默认可关，对既有 4 路 **0 regression**（case 22 验证 `enable_entity_link=False` 行为等价原 `retrieve`） | ⏸ 未跑，据实 |

---

## §7 派工 v11 段 7 错误 19 类（PR8 实战）

| 类 | 处置 | 判定 |
|----|------|------|
| E01 alembic 多 head | 091 接 090 单链；`heads` = 1；e2e case 18 断言 + 串单链 087→091 | ✅ |
| E03 pytest 假 PASS | 22/22 真跑 0.20s；2 例真失败修根因不弱化断言 | ✅ |
| E05 老核心函数误改 | 件 4a grep **6** 个老核心（brief 强调 6 非 5）= 0 | ✅ |
| E06 HybridRetriever 误改 | `^[+-]def` 0 + 缩进 def 0 + 删除行 0（127 纯追加） | ✅ |
| E07 锚点范式缺失 | 全 commit `[PR8 W94 +N]` 前缀 | ✅ |
| E08 0 production code 违规 | 件 4a/4b 双门控全过 | ✅ |
| E11 GIN/HNSW 大表阻塞 | `CREATE INDEX CONCURRENTLY` + 089 二段式 `DO $$ 探测 pg_indexes` | ✅ |
| E19 commit message 格式 | 全 commit Co-Authored-By + 锚点数字 | ✅ |
| E21 pytest collection error | 全程 `--ignore=test_w79`；`test_pr8_e2e.py` basename 唯一 | ✅ |
| **E37 实体链 hit ≥ 25%** | case 10 真算，未达标判失败 | ✅ |
| **E38 P95 ≤ 100ms** | case 20 真计时 20 samples | ✅ |
| **E39 实体数 ≥ 5000** | case 21 真调用 + `assert_awaited()`；真库待生产 | ✅ 路径 |

---

## §8 派工 v11 段 10 新 6 项（PR8 实战）

1. **`python -m alembic` 命令形态**: ✅ 全程，0 次直跑 `alembic`
2. **pytest 白名单**: ✅ `--ignore=tests/test_w79_commercial_private_deployment_e2e.py`；baseline **3208 与 brief 期望 0 偏差**；收口 3230（+22 PR8）
3. **派工 brief vs 实测据实上报**: ✅ **3 处后端 KG 错配据实**（§3），不擅自扩也不擅自缩
4. **docs-only 断言化**: N/A（PR8 含后端 + alembic），但 docs 门禁仍进 e2e（case 11/16/17/22 真读文件断言）
5. **worktree 依赖基线自检**: ✅ `web/node_modules` worktree 内**不存在** → 主仓等价验证；件 3 pre-existing FAIL 据实标注；`sentence_transformers` 未装 → e2e 全程 mock/stub 不静默 SKIP
6. **5 件套守恒命令输出粘贴**: ✅ §1 结构化回报表

---

## §9 其他据实上报（禁止"应该/大概/估计"）

1. **`scripts/check_typing_imports.sh` 超时**: 2min 限内未完成（106 文件全扫）。改用 **5 模块 `importlib.import_module` 实测全 OK**（`kg_entity` / `entity_link_recall` / `kg_embedding` / `knowledge_graph_service` / `hybrid_retriever`），等价验证方案 C 铁律 2（CHECKLIST §F fallback 精神）。
2. **CLAUDE.md 0 改**: 锚点落 `docs/rag/W94-PR8-ANCHOR.md` 镜像（PR3/PR5 已批模式）。
3. **8 个 untracked `agent-w89-*/`（2.66GB）**: DERIVE-14 已记录，PR8 **不擅自删**，待主拍签字。
4. **plan v1.2 附录 B**: 读取时 plan 顶部标 v1.1，但含 §12/§13/§14 + v1.2/v1.2-derive07 附录 B 条目（DERIVE-19 reconcile 已生效）。PR8 按 v1.2 内容实施。
5. **未 merge 未 push**: 分支 `chore/w94-rag-pr8-knowledge-graph-2026-07-30` 停在 worktree，**agent 不主动 merge**（CHECKLIST §C）。

---

## §10 10 PR alembic 链收口（PR8 里程碑）

```
087_add_knowledge_original_parent_id   (hotfix, MERGE-01 前)
  └─ 088_add_knowledge_chunk           (PR2, MERGE-01)
       └─ 089_gin_trgm_tsvector        (PR3, MERGE-02)
            └─ 090_add_rag_eval_report (PR5, MERGE-03)
                 └─ 091_add_kg_entity  (PR8, 本任务)  ← 10 PR alembic 链终点
```

**PR9（auto-research 升级）/ PR10（docs 三件套）均无 alembic 迁移** → 091 是 10 PR 系列最后 1 个迁移，链正式收口，`heads` 恒为 1。

**缺口消化**: 缺口 9（图谱深度联动，plan §1.3）。第 5 路 `entity_link` 补齐 Neo4j 单点依赖短板 —— PostgreSQL 内置，Neo4j 挂时仍可召回。

---

## §11 交付文件清单

**新增（5）**:
- `app/models/kg_entity.py` — `KGEntity` ORM + 8 类白名单 + `normalize_entity_name` / `coerce_entity_type`
- `alembic/versions/091_add_kg_entity.py` — 160 行（表 + 3 约束 + 4 索引，idempotent + CONCURRENTLY）
- `app/services/entity_link_recall.py` — 第 5 路召回 + 4 门禁常量 + 4 纯逻辑函数
- `app/services/kg_embedding.py` — 实体向量（PR1 复用 + lazy import + 幂等回填）
- `tests/rag/test_pr8_e2e.py` — 22 case

**修改（7，全部纯追加 0 删除）**:
- `app/models/__init__.py` — `KGEntity` 注册（PR2 `KnowledgeChunk` 同模式）
- `app/services/knowledge_graph_service.py` — 文件底部模块级追加（0 类方法改）
- `app/services/knowledge_service.py` — Step 5b 钩子（+14，件 4a = 0）
- `app/services/hybrid_retriever.py` — 第 5 路（+127，件 4a = 0）
- `docs/rag/RUNBOOK.md` §0.7 / `docs/rag/SCHEMAS.md` §10 / `docs/rag/CHECKLIST.md` §J / `docs/rag/CHANGELOG.md` PR8
- `CHANGELOG.md` + `README.md`

**新增文档（1）**: `docs/rag/W94-PR8-ANCHOR.md`（11 节 CLAUDE.md 镜像）

**0 改**: CLAUDE.md / 派工 v10 v11 模板 / alembic 087-090 / 6 锁定老核心（def 层面）/ 5 个已有 KG 服务类体 / 2 个老实体表 / web/

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
