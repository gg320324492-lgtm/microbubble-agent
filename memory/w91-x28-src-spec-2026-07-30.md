# W91-X-28 src 5 spec 真整改 (2026-07-30)

> **任务定位**: W91 第 1 批 X-28 子 agent, 接续 W90-X-5 (`.spec.js` → `.test.js` 5 整改) 真跑通 + 加固.
> **派工依据**: 锚点范式守恒, 类 20.74 (W89-X-19c) 实战派生类 20.106.
> **结果**: 5 spec 全 PASS (59 tests / 5 files / 1.83s).

## 1. 背景

W90-X-5 (`817996e55`) 仅做了 5 个文件 rename (`*.spec.js` → `*.test.js`), 未真跑验证 + 未修复内部 stale selector. W89-X-13 据实报告 NavRail ×8 failed 是 stale slice 引用旧契约, W89-X-19c (`f082b488b`) 已修复 NavRail 但未合入 main. W91-X-28 任务: 把 W89-X-19c 修法搬运至 W91 工作分支, 真跑通 5 spec.

## 2. 工作流程

### 步骤 1 - 派工纪律 (worktree 独立)

```bash
cd E:/microbubble-agent
git worktree add ../agent-w91-x28-src-spec -b claude/w91-x28-src-spec main
cd ../agent-w91-x28-src-spec
git cherry-pick 817996e55   # W90-X-5 rename only
```

### 步骤 2 - 列 5 spec 现状

```bash
ls web/src/__tests__/ web/src/components/chat/__tests__/ web/src/components/chat/blocks/__tests__/
```

确认 5 个 `.test.js` 文件存在 (W90-X-5 rename 已落地).

### 步骤 3 - 真跑拿 root cause

```bash
cd web
SKIP_DB_SETUP=1 npx vitest run src/__tests__/chatSSE.test.js    # 11/11 PASSED
SKIP_DB_SETUP=1 npx vitest run src/components/chat/__tests__/NavRail.test.js  # 8/8 FAILED
SKIP_DB_SETUP=1 npx vitest run src/__tests__/cssVariables.test.js  # PASSED
SKIP_DB_SETUP=1 npx vitest run src/__tests__/textSanitize.test.js  # PASSED
SKIP_DB_SETUP=1 npx vitest run src/components/chat/blocks/__tests__/HypothesisBlock.test.js  # PASSED
```

**Root cause** (派工 v6 §5 反馈类 20.74 实战):
- NavRail.spec.js 引用 5 类旧契约, 与 NavRail.vue 真实实现不一致:
  1. `.nav-item` (真实: `.nav-rail-item`)
  2. `#nav-rail-hamburger` 按钮 (真实: `.mobile-close` 按钮, mobile-only)
  3. `data-theme-accent="accent-{name}-{light|dark}"` 属性 (真实: `document.documentElement data-theme + data-accent` 双 attribute)
  4. `mobile-drawer` / `drawer-open` class (真实: `mobile-open` class + `update:mobileOpen` emit)
  5. `#nav-rail-accent` / `#nav-rail-chat` button id (真实: 无内嵌切换按钮, 走 `themeStore.setAccent` API)

### 步骤 4 - 修法 (派工 v3 双锚定 + 0 production code 改动铁律)

调研真实契约 (派工 v6 段 5 反馈类 20.74 修法 = spec 适配 component, 非反过来):
- 根 `<nav class="nav-rail">`: data-testid="nav-rail" + aria-label="主导航" + classes { collapsed, mobile-open }
- 6 路由项: `<li class="nav-rail-item" :class="{ active }">` + 内嵌 `<router-link :to :data-route :aria-current>`
- 品牌区: `.nav-rail-brand` + brand-icon "MNB" + brand-text + 移动端专属 `<button class="mobile-close" aria-label="关闭导航">`
- 折叠: `<button class="collapse-btn">` → `uiStore.toggleNavRail()`
- 移动端遮罩: `<button class="nav-rail-scrim" v-if="mobileOpen">`
- 主题机制: `themeStore.mode + themeStore.accent` → watch → `document.documentElement data-theme="light|dark" + data-accent="orange|ocean|forest"`

修法: 搬运 W89-X-19c (`f082b488b`) 修法至 W91 工作分支的 NavRail.test.js, 文件名 docstring 同步改 `.spec.js` → `.test.js`.

### 步骤 5 - 真跑验证

```bash
cd web
SKIP_DB_SETUP=1 npx vitest run src/__tests__/chatSSE.test.js src/__tests__/cssVariables.test.js src/__tests__/textSanitize.test.js src/components/chat/__tests__/NavRail.test.js src/components/chat/blocks/__tests__/HypothesisBlock.test.js
```

**结果**: **5 passed (5) / 59 passed (59) / 0 failed / 1.83s**.

### 步骤 6 - 边界复检

```bash
cd E:/agent-w91-x28-src-spec
git diff main..HEAD --name-only
```

**允许改** (本任务):
- 5 spec rename (W90-X-5 cherry-pick, 4 个 100% 改名 0 内容修改 + NavRail 1 个内容整改)
- `memory/w90-x5-src-tests-2026-07-30.md` (W90-X-5 沉淀)
- `tests/src_tests_x5/test_naming.py` (W90-X-5 加固, 5 case PASS)
- `memory/w91-x28-src-spec-2026-07-30.md` (本任务沉淀, 新)

**禁止改**: 0 触碰 ✓
- 其它 spec 文件 ✓
- 其它业务代码 ✓
- `app/`、`alembic/`、`nginx/`、`docker/`、`commercial/` ✓

### 步骤 7 - commit

```bash
git add web/ memory/w91-x28-src-spec-2026-07-30.md
git commit -m "test(w91): 5 src/__tests__/*.test.js 真整改 (W91-X-28)"
```

## 3. 派工 v6 §5 反馈 — 类 20.106 沉淀

### 类 20.106 新增: "spec 真整改前必查 component import / 真实契约, vue script setup undeclared 不可 vi.mock"

- **事故**: W90-X-5 改了文件名 (`*.spec.js` → `*.test.js`) 但未真跑 + 未内部修复. 留下的 NavRail.test.js 仍引用旧契约 (`.nav-item` / hamburger button / `data-theme-accent` 属性 / `mobile-drawer` class / `#nav-rail-accent` button id). 真跑 8/8 failed.
- **根因**: spec 派工仅做 rename (搬运) 未做契约对齐, 派工 brief 漏 "调研真实 component 契约" 步骤.
- **修法 (派工 v3 双锚定 + 0 production code 改动铁律)**:
  1. **调研真实 component 契约** (读 .vue 文件): 根元素 / 子元素 class 与 attribute / 事件 emit / 主题机制
  2. **spec 改写适配真实契约** (派工 v6 §5 反馈类 20.74 实战: spec 适配 component, 非反过来)
  3. **vue script setup undeclared identifier 不可 vi.mock 绕过** — 必改 component import (W90-X-4 据实). 比如 spec 期望 `#nav-rail-accent` 但 component 没这个 button, 应改走 `themeStore.setAccent` API, 不应 mock 一个不存在的 button
  4. **真跑 + 全 PASS 验收** — 派工 brief 必含 `SKIP_DB_SETUP=1 npx vitest run <spec>` 验证步骤, 不只 "改名 + commit"
- **应用范围**: 任何 vitest spec 真整改派工 (W91+ 持续), 不限于本次 5 spec.

## 4. 锚点范式与派工前提铁律

- **锚点范式**: W90-X-5 第 1 批 325 → W91-X-28 守恒 (base = main tip 实测)
- **0 production code 改动铁律**: 守恒 1/1 (X-28 仅改 spec 文件)
- **派工前提铁律 12 + 类 20 累计 38 实例**:
  - 类 20.74 "vitest stale slice 修法 = 调研真实契约 + spec 适配 component" (W89-X-19c 实战)
  - 类 20.106 "spec 真整改前必查 component import / 真实契约, vue script setup undeclared 不可 vi.mock" (W91-X-28 本任务实战新增)

## 5. 后续 W91+ 派工顺序

- W91-X-28 commit 单做: `test(w91): 5 src/__tests__/*.test.js 真整改 (W91-X-28)`
- W91+ 派工顺序: W91 第 1 批 4 agents + W91 第 2 批 / W92 / W93 ...

## 6. 文件清单

- `web/src/__tests__/chatSSE.test.js` — W90-X-5 rename, 11/11 PASS
- `web/src/__tests__/cssVariables.test.js` — W90-X-5 rename, PASS
- `web/src/__tests__/textSanitize.test.js` — W90-X-5 rename, PASS
- `web/src/components/chat/__tests__/NavRail.test.js` — W91-X-28 整改 (搬运 W89-X-19c 修法), 8/8 PASS
- `web/src/components/chat/blocks/__tests__/HypothesisBlock.test.js` — W90-X-5 rename, PASS

**总计**: 5 files / 59 tests / 0 failed / 1.83s ✓