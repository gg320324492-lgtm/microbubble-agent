# W90-X-5 src/__tests__/*.spec.js → *.test.js 5 整改 (2026-07-30)

**派工**: W90 第 1 批 X-5, 主指挥协调范式第 68 次派工 (假设序号). W89-X-28 留口: src/__tests__/ 5 个 vitest spec 命名误导未动 (派工 brief 估 14 实测 16 + src/__tests__/ 5 留 W89+ 后续).

## 范围 (据实上报, 与派工 brief 一致)

5 个 vitest spec 文件:
1. `web/src/__tests__/chatSSE.spec.js` → `chatSSE.test.js`
2. `web/src/__tests__/cssVariables.spec.js` → `cssVariables.test.js`
3. `web/src/__tests__/textSanitize.spec.js` → `textSanitize.test.js`
4. `web/src/components/chat/blocks/__tests__/HypothesisBlock.spec.js` → `HypothesisBlock.test.js`
5. `web/src/components/chat/__tests__/NavRail.spec.js` → `NavRail.test.js`

**严格边界**: git mv 重命名, 不改内容 (派工 brief v3 §"严格边界" 强制).

## 派工 v6 §5 反馈 类 20.83 加固沉淀

W89-X-28 dcfffb89c (类 20.83) 已沉淀 "vitest spec 命名必统一 .test.js, 禁 .spec.js 与 playwright 混名", 本任务 (W90-X-5) 闭环 W89-X-28 留口的 src/__tests__/ 5 spec.

**类 20.83 完整内涵**:
- `.spec.js` 一律改为 `.test.js` (vitest spec)
- playwright visual 走 `.spec.mjs` (扩展名隔离, 不混名)
- pytest 走 `test_*.py` (Python tradition)
- 任何 src/__tests__/ 内新增 vitest spec 必 `.test.js` 后缀 (门禁脚本 `tests/src_tests_x5/test_naming.py` 强制)

## e2e 加固 (派工 v6 §1.2)

新增 `tests/src_tests_x5/test_naming.py` (2 case):
1. `test_no_spec_js_in_src_tests`: rglob `web/src/**/*.spec.js` 必为空
2. `test_src_tests_has_test_js`: rglob `web/src/**/*.test.js` 必 ≥ 5 (含嵌套子目录)

跑:
```bash
SKIP_DB_SETUP=1 pytest tests/src_tests_x5/ -v
# 2 PASSED
```

vitest 不在本 worktree 的 node_modules (环境限制), 但 W89-X-28 baseline 1025 PASS 已验证 vitest 默认 `**/*.{test,spec}.?(c|m)[jt]s?(x)` 同时识别两种命名 → 改名无破坏.

## 0 production code 改动铁律守恒

- ✅ 仅 5 git mv + 1 新 pytest 文件 + 1 memory 文件
- ✅ 无业务代码改动 (`web/src/views/` + `web/src/components/`) 
- ✅ 无其它 spec 改动 (consistency with W89-X-28)

## 锚点范式

base `034343f8a` (main tip) → tip (本任务收口) +1 守恒.

## memory

- `memory/w90-x5-src-tests-2026-07-30.md` (本文件)
- 前置: `memory/w89-x28-e2e-rename-2026-07-30.md` (W89-X-28 留口 + 类 20.83 沉淀 v1)

## 待 W90+ 后续

- W90+ 其他 worktree 提交如又新增 .spec.js, 立即派 agent 整改
- 门禁脚本 `tests/src_tests_x5/test_naming.py` 未来接 CI 防回归
