# W97 WORKTREE-05 — 有 origin 备份的 worktree 逐签处置报主拍 (派工 v10 段 7 E50 强制)

> **类型**: WORKTREE-05 逐签报主拍
> **时间**: 2026-07-30
> **状态**: 0 删除 0 rm-rf 0 worktree remove — 仅观测 + 报主拍逐签
> **派工 ID**: WORKTREE-05 W97
> **协作前提**: WORKTREE-01 已拦截 / WORKTREE-02 已列 13 worktree 详情 / WORKTREE-03 处理 #4+#10 / WORKTREE-04 处理 #8

---

## 0. 任务边界 + 派工 brief 据实校正 (派工 v10 §1.2 据实上报铁律)

派工 brief 标题写 **"12 个有 origin 备份的 worktree"**, 但正文实际列出编号 `#1 #2 #3 #5 #6 #7 #9 #11 #12 #13` = **10 项**, 非 12 项.

**实测结论**: **10 个** worktree 有 origin 备份 + 1 commit ahead. 本任务据实按 **10 个** 处置, 不凑 12, 不擅自扩容到 WORKTREE-03/04 的处置范围 (#4 / #8 / #10).

派工前提铁律 12 第 5 条: 必先实测再决策. 派工 v10 段 7 E50 强制: 不擅自删, 必报主拍签字. E83 强制: 不实测 5 项信息就报 = 违规.

---

## 1. 环境基线实测 (与 WORKTREE-02 报告的差异)

| 维度 | WORKTREE-02 报告 (约 3h 前) | WORKTREE-05 实测 (本任务) | 差异 |
|------|---------------------------|--------------------------|------|
| main HEAD | `c53444dfd` (锚点 W97 +3) | **`f13636888`** (MERGE-07 W97, 锚点 484) | **已推进 6 commits** |
| 磁盘 | 未测 | `E: 3.7T total / 618G used / 3.1T avail / 17%` | 空间充裕, **无删除压力** |
| worktree 总数 | 13 active (E:/microbubble-agent + E:/ 根) | **160 registered** (E:/ 根 54 + E:/microbubble-agent/ 11 + .claude/worktrees/ 93) | WORKTREE-02 仅覆盖 `agent-w*` 子集 |
| behind 计数 | 158~160 (W89 系) | **162~164** (main 已推进) | +4, 符合 main 推进 |
| dirty | 0 | **0** (10/10 全 clean) | 一致 ✅ |

**注**: worktree 总数 160 远大于 WORKTREE-02 的 13 — 因为 WORKTREE-02 只统计 `agent-w89/w90/w91` 目录, 未含 `.claude/worktrees/` 下 93 个 W82-W86 worktree 与 E:/ 根下 54 个. **本任务不扩范围**, 仅处置派工列出的 10 个.

---

## 2. 10 个 worktree 5 项信息实测表 (派工前提铁律 12 第 5 条 + E83)

**实测时间**: 2026-07-30, main HEAD = `f13636888`, 全部对比基线 = `origin/main`

| # | worktree | branch | HEAD | dirty | ahead | behind | 磁盘 |
|---|----------|--------|------|-------|-------|--------|------|
| 1 | `agent-w89-p2-mobile-comments` | `claude/w89-p2-mobile-comments-rerun` | `c90dc99c6` | **0** | 1 | 164 | 494 MB |
| 2 | `agent-w89-p7-visual-debug` | `claude/w89-p7-visual-debug` | `83eb3ec59` | **0** | 1 | 162 | 46 MB |
| 3 | `agent-w89-p8-visual-sweep` | `claude/w89-p8-visual-sweep` | `9e4dade76` | **0** | 1 | 162 | 493 MB |
| 5 | `agent-w89-x21-collect-config` | `claude/w89-x21-collect-config` | `677dba793` | **0** | 1 | 46 | 485 MB |
| 6 | `agent-w89-x22-desktop-drive` | `claude/w89-x22-desktop-drive` | `e903b8557` | **0** | 1 | 46 | 525 MB |
| 7 | `agent-w89-x24-visual-flaky` | `claude/w89-x24-visual-flaky` | `adcbcc49e` | **0** | 1 | 46 | 521 MB |
| 9 | `agent-w90-x7-swipe-bug` | `claude/w90-x7-swipe-bug` | `a545ed072` | **0** | 1 | 30 | 51 MB |
| 11 | `agent-w90-x8-prod-chunk` (E:/ 根) | `claude/w90-x8-prod-chunk` | `911190824` | **0** | 1 | 30 | 51 MB |
| 12 | `agent-w91-wr1-play-icon` | `claude/w91-wr1-play-icon` | `5ff388b9f` | **0** | 1 | 11 | 51 MB |
| 13 | `agent-w91-x22-viewport` | `claude/w91-x22-viewport` | `85d293381` | **0** | 1 | 11 | 480 MB |
| | | | | **0/10** | **10× 1** | | **3197 MB ≈ 3.12 GB** |

**派工 brief 磁盘数字校正**: brief 逐项写 494M/46M/493M/485M/525M/521M/51M/54M/480M + "E:/根" = 与实测基本一致, 仅 #12 `agent-w91-wr1-play-icon` brief 写 54M 实测 **51MB**, #11 brief 未给数值实测 **51MB**. 合计 **3.12 GB** (非 3.26GB — 那是 WORKTREE-02 的 13 个含 #4/#8/#10).

---

## 3. origin 备份 10/10 全部 MATCH 验证 (删除安全性前提)

`git rev-parse origin/<branch>` vs 本地 branch 逐一比对:

| branch | origin | local | 状态 |
|--------|--------|-------|------|
| `claude/w89-p2-mobile-comments-rerun` | `c90dc99c6` | `c90dc99c6` | ✅ MATCH |
| `claude/w89-p7-visual-debug` | `83eb3ec59` | `83eb3ec59` | ✅ MATCH |
| `claude/w89-p8-visual-sweep` | `9e4dade76` | `9e4dade76` | ✅ MATCH |
| `claude/w89-x21-collect-config` | `677dba793` | `677dba793` | ✅ MATCH |
| `claude/w89-x22-desktop-drive` | `e903b8557` | `e903b8557` | ✅ MATCH |
| `claude/w89-x24-visual-flaky` | `adcbcc49e` | `adcbcc49e` | ✅ MATCH |
| `claude/w90-x7-swipe-bug` | `a545ed072` | `a545ed072` | ✅ MATCH |
| `claude/w90-x8-prod-chunk` | `911190824` | `911190824` | ✅ MATCH |
| `claude/w91-wr1-play-icon` | `5ff388b9f` | `5ff388b9f` | ✅ MATCH |
| `claude/w91-x22-viewport` | `85d293381` | `85d293381` | ✅ MATCH |

**10/10 MATCH, 0 DIVERGE, 0 MISSING** — 派工 brief "有 origin 备份" 前提**实测成立**. 删 worktree 后 commit 100% 可从 origin 恢复 (`git worktree add <path> <branch>` 或 `git fetch origin <branch>`).

---

## 4. 选项 D 核心: 10 个 commit 实质内容逐个评估 (agent 默认建议)

### 4.1 关键发现: **10/10 commit 的 memory 沉淀全部 ABSENT in main**

`git cat-file -e HEAD:<memory-file>` 逐一验证:

```
ABSENT : memory/w89-p2-mobile-comments-rerun-2026-07-30.md
ABSENT : memory/w89-p7-visual-debug-2026-07-30.md
ABSENT : memory/w89-p8-visual-sweep-failures-2026-07-30.md
ABSENT : memory/w89-x21-collect-config-2026-07-30.md
ABSENT : memory/w89-x24-visual-flaky-2026-07-30.md
ABSENT : memory/w90-x7-swipe-bug-2026-07-30.md
ABSENT : memory/w90-x8-prod-chunk-2026-07-30.md
ABSENT : memory/w91-wr1-play-icon-2026-07-30.md
ABSENT : memory/w91-x22-viewport-2026-07-30.md
```

**含义**: 这 10 个 commit 的**知识沉淀无一进入 main**. 删 worktree 不丢 commit (origin 有), 但**这些 memory 若不 cherry-pick 进 main, 永远只活在 branch 上**, 未来会话读 CLAUDE.md/MEMORY.md 索引触达不到.

### 4.2 逐个 commit 内容 + 是否已被 main 覆盖

| # | commit | 类型 | 改动 | production 是否已在 main | 实质价值 |
|---|--------|------|------|------------------------|----------|
| 1 | `c90dc99c6` | test | memory 290 行 + `auth-shared-token.spec.mjs` 117 行新增 + `axe-chats.spec.mjs` 改 + `axe-config.mjs` 45 行新增 | ❌ **未在 main** (3 文件 187 行净增) | **高** — a11y 限流修复 (beforeAll 共享 token), 未落地 |
| 2 | `83eb3ec59` | docs | memory 141 行 (visual snapshot 漂移真因) | n/a (纯 docs) | **中** — 排障结论, 删则真因失传 |
| 3 | `9e4dade76` | docs | memory 370 行 (35 spec 全 sweep + FAIL 清单) | n/a (纯 docs) | **中高** — 35 spec FAIL 全量清单, 重跑成本高 |
| 5 | `677dba793` | fix | memory 65 + `tests/vitest_x21/test_excludes.py` 78 行 + `web/vitest.config.js` +10-2 | ❌ **未在 main** — main 仍是旧 2 行 `exclude: ['**/node_modules/**','tests/visual/**']`, 分支版是 6 条精细 exclude | **高** — Vitest/Playwright 收集边界隔离, 真未落地 |
| 6 | `e903b8557` | fix | memory + `DesktopFileCommentsView.vue` +153-6 + 11 张 win32 baseline PNG | ❌ **未在 main** | **高** — route.query + dark mode 真修 + 视觉基线 |
| 7 | `adcbcc49e` | fix | memory 73 + `tests/visual_x24/test_stable.py` 137 行 + `visual-regression.spec.mjs` +75-17 | ❌ **未在 main** | **高** — 等 UI locator 替 networkidle, flaky 根治 |
| 9 | `a545ed072` | fix | memory 189 + `tests/swipe_x7/test_no_test_use_in_describe.py` 128 行 + `playwright.config.js` +19 + spec 改 | ❌ **未在 main** — main 无 `mobile-swipe` project | **高** — Playwright 1.61+ `test.use in describe` 报错修复 |
| 11 | `911190824` | docs | memory 283 行 (prod build chunk 策略调研, 只调研不修) | n/a (纯 docs) | **中** — 调研沉淀, 0 production code |
| 12 | `5ff388b9f` | fix | memory 139 + `tests/icon_wr1/test_play_to_video.py` 91 行 + `RAGEvalPanel.vue` +4-2 | ✅ **已在 main** — `git diff HEAD 5ff388b9f -- RAGEvalPanel.vue` = **空** (main 27 行 `import { Refresh, VideoPlay, ...}` + 114 行 `<VideoPlay />`) | **低(production) / 中(test+memory)** — P0 已由 HOTFIX-01 落地, 但 91 行回归测试 + memory 未落 |
| 13 | `85d293381` | docs | memory 200 行 (viewport race 拦截, 派工前提错配) | n/a (纯 docs, 0 production code) | **中** — 类 20 拦截实例沉淀 |

### 4.3 x22 (#6) 特别说明 — 避免误读

`git diff HEAD e903b8557 --stat -- web/src/` 会显示 `RAGEvalPanel.vue -183 / useRAGEval.js -79 / router/index.js -11 / RAGEvalPanel.test.js -117` 等**删除**行。

**这不是该 commit 删的**。实测 `git show e903b8557 --stat -- web/src/` 该 commit **只改 1 个文件**:
```
web/src/views/desktop/DesktopFileCommentsView.vue | 159 +++++++++++++++++-
1 file changed, 153 insertions(+), 6 deletions(-)
```
其 parent 是 `a000d0bf2` (PR3 BM25, alembic 088→089) — **早于 RAG 大改造**, 所以 diff 里的"删除"其实是 main 后来新增的 RAG 文件。**该 worktree 若合并需 rebase, 不可直接 merge**, 否则会回退 RAG 改造。

---

## 5. 4 选项决策 (派主拍, 派工 v10 段 7 E50 强制)

### 选项 A: 保留全部 10 个 (接受 3.12 GB 占用)
- 磁盘实测 `E: 3.1T avail / 17% used` — **3.12 GB 占比 0.08%, 无任何空间压力**
- 优点: 0 风险, working tree 随时可查
- 缺点: 160 个 worktree 的 registry 噪音持续
- **适用**: 主拍暂不决策, 优先保数据

### 选项 B: 全部 `git worktree remove --force` + `git branch -D`
- 释放 3.12 GB
- **前提已实测成立**: 10/10 origin MATCH, 0 dirty, commit 不会丢
- **风险**: 7 个 commit 含**未落 main 的 production/test 改动** (见 4.2), 删 worktree 后需 `git worktree add` 重建才能继续работать; 且 10/10 memory 沉淀仍不在 main
- **agent 评估**: 技术上安全 (origin 有备份), 但**过早** — 应先决定这 7 个改动合不合入 main

### 选项 C: 按 ahead 决策 (ahead=0 删, ahead>0 报主拍)
- 本批 **10/10 全部 ahead=1** → 结果等价于选项 A (全部报主拍)
- **不产生任何区分度**, 对本批无效

### 选项 D: 按 commit 实质内容逐签 ← **agent 默认建议**

据 4.2 表分三组:

**D1 — 建议先合入 main 再删 worktree (7 个, 含未落地 production/test)**

| # | worktree | 理由 | 建议动作 |
|---|----------|------|----------|
| 1 | `agent-w89-p2-mobile-comments` | a11y 限流修复 3 文件 187 行未落 main | cherry-pick / PR 合入 → 再删 |
| 5 | `agent-w89-x21-collect-config` | vitest.config.js 6 条 exclude 未落 main (main 仍旧 2 条) | 合入 → 再删 |
| 6 | `agent-w89-x22-desktop-drive` | DesktopFileCommentsView 真修 + 11 PNG 基线未落 main; **必须 rebase 不可直接 merge** (见 4.3) | rebase 后合入 → 再删 |
| 7 | `agent-w89-x24-visual-flaky` | visual-regression flaky 根治未落 main | 合入 → 再删 |
| 9 | `agent-w90-x7-swipe-bug` | playwright.config.js `mobile-swipe` project 未落 main, Playwright 1.61+ 报错仍在 | 合入 → 再删 |
| 3 | `agent-w89-p8-visual-sweep` | 35 spec FAIL 全量清单, 重跑成本高 | memory cherry-pick → 再删 |
| 12 | `agent-w91-wr1-play-icon` | production 已在 main (HOTFIX-01 已落), 但 91 行回归测试 + memory 未落 | 仅 cherry-pick test+memory → 再删 |

**D2 — 建议仅 cherry-pick memory 后删 (3 个, 纯 docs 0 production code)**

| # | worktree | 内容 | 建议动作 |
|---|----------|------|----------|
| 2 | `agent-w89-p7-visual-debug` | visual snapshot 漂移真因 141 行 | cherry-pick memory → 删 worktree + branch |
| 11 | `agent-w90-x8-prod-chunk` | prod build chunk 调研 283 行 | cherry-pick memory → 删 worktree + branch |
| 13 | `agent-w91-x22-viewport` | viewport race 拦截 200 行 (类 20 实例) | cherry-pick memory → 删 worktree + branch |

**D3 — 可立即删的**: **0 个** (无一 commit 内容已完全落 main; 最接近的 #12 也只落了 production, test+memory 未落)

**D 选项磁盘收益**: D2 立即删 = 46+51+480 = **577 MB**; D1 合入后删 = 剩余 **2620 MB**

---

## 6. 每个 worktree 默认建议汇总表 (主拍逐签用)

| # | worktree | ahead | origin | 磁盘 | **agent 默认建议** | 主拍签字 |
|---|----------|-------|--------|------|-------------------|---------|
| 1 | agent-w89-p2-mobile-comments | 1 | ✅ MATCH | 494MB | **D1 先合 a11y 修复再删** | ☐ |
| 2 | agent-w89-p7-visual-debug | 1 | ✅ MATCH | 46MB | **D2 cherry-pick memory 后删** | ☐ |
| 3 | agent-w89-p8-visual-sweep | 1 | ✅ MATCH | 493MB | **D1 cherry-pick 35 spec 清单后删** | ☐ |
| 5 | agent-w89-x21-collect-config | 1 | ✅ MATCH | 485MB | **D1 先合 vitest exclude 再删** | ☐ |
| 6 | agent-w89-x22-desktop-drive | 1 | ✅ MATCH | 525MB | **D1 rebase 后合再删** ⚠️ 不可直接 merge | ☐ |
| 7 | agent-w89-x24-visual-flaky | 1 | ✅ MATCH | 521MB | **D1 先合 flaky 根治再删** | ☐ |
| 9 | agent-w90-x7-swipe-bug | 1 | ✅ MATCH | 51MB | **D1 先合 playwright project 再删** | ☐ |
| 11 | agent-w90-x8-prod-chunk | 1 | ✅ MATCH | 51MB | **D2 cherry-pick memory 后删** | ☐ |
| 12 | agent-w91-wr1-play-icon | 1 | ✅ MATCH | 51MB | **D1 仅 cherry-pick test+memory 后删** (production 已落) | ☐ |
| 13 | agent-w91-x22-viewport | 1 | ✅ MATCH | 480MB | **D2 cherry-pick memory 后删** | ☐ |

---

## 7. 0 擅自删声明 (派工 v10 段 7 E50 + E82 强制)

本任务执行的**全部**写操作 = **本 memory 文件 1 个**.

- ❌ 0 `git worktree remove`
- ❌ 0 `git branch -D`
- ❌ 0 `rm -rf`
- ❌ 0 production code 改动
- ❌ 0 触碰 #4 `agent-w89-x14-swipe-bug` (WORKTREE-03 范畴)
- ❌ 0 触碰 #8 `agent-w89-x9-grand-closure` (WORKTREE-04 范畴)
- ❌ 0 触碰 #10 main branch worktree (必保留)
- ❌ 0 触碰 alembic 087-091
- ❌ 0 触碰 backend service / model

---

## 8. 派工 v10 段 7 错误 19 类 — 新增实例沉淀

### E83 实例 (WORKTREE-05 实测 5 项信息, 未跳过)
- 派工强制"必实测 5 项信息再报". 本任务对 10 个 worktree 逐个 `cd` + `git rev-parse` + `git status --porcelain` + `git log origin/main..HEAD` + `git log HEAD..origin/main`, **0 推断 0 复用 WORKTREE-02 数字**.
- 收益: 发现 main HEAD 已从 `c53444dfd` 推进到 `f13636888`, behind 计数从 158~160 变 162~164 — 若直接抄 WORKTREE-02 数字即为过期数据.

### E50 实例 #7 (派工 brief 数量错配 — 标题 12 实际 10)
- **场景**: 派工 brief 标题 "12 个有 origin 备份的 worktree", 正文列 `#1 #2 #3 #5 #6 #7 #9 #11 #12 #13` = 10 项
- **真相**: 实测确为 10 个. 缺的 `#4` (origin MISSING, WORKTREE-03) / `#8` (已合 main, WORKTREE-04) / `#10` (branch=main, 必保留) 恰好是被其他 agent 认领的 3 个
- **教训**: 派工 brief 标题数字与正文列表必须一致; agent 遇到不一致时**按正文实际列表据实执行, 不凑标题数字** (类 20.13 "不擅自扩也不擅自缩")

### E51 实例 (worktree 总量远超派工假设)
- **场景**: 派工/WORKTREE-02 语境暗示 "13 个 active worktree"
- **真相**: `git worktree list | wc -l` = **160** (E:/ 根 54 + E:/microbubble-agent/ 11 + .claude/worktrees/ 93)
- **教训**: `agent-w89/90/91` 只是 worktree 全集的 10%; W82-W86 的 93 个 `.claude/worktrees/` 从未被本轮 WORKTREE-0x 系列覆盖. **本任务不擅自扩范围**, 但**报主拍知悉**: 若目标是磁盘治理, 3.12 GB 只是冰山一角

### E52 实例 (diff 方向误读风险 — x22 案例)
- **场景**: `git diff HEAD <old-commit> --stat` 会把 "main 后来新增的文件" 显示成 "该 commit 删除的文件"
- **真相**: `git show <commit> --stat` 才是该 commit 真实改动. x22 实际只改 1 文件, diff 却显示删 4 个 RAG 文件
- **教训**: 评估分支 commit 价值必须用 `git show`, 不能用 `git diff HEAD <commit>`; 且**老 base 的分支合并前必须 rebase**

---

## 9. 5 件套守恒实测

| 件 | 命令 | 实测 | 结论 |
|----|------|------|------|
| 1 alembic 1 head | `python -m alembic heads` | `091_add_kg_entity (head)` | **PASS** — 1 head, 091 守恒 |
| 2 pytest ≥ 3230 | `pytest tests/ --co --ignore=...w79...` | **3230 tests collected** | **PASS** — 守恒 |
| 3 PWA build | 不动 `web/dist` / `web/package.json` / `vite.config.js` | 0 diff | **PASS** — 守恒 |
| 4a 双门控 (0 老核心 diff) | 本任务仅新增 `memory/w97-worktree-05-2026-07-30.md` | 0 `app/` 0 `web/src/` 0 `alembic/` 改动 | **PASS** |
| 5 锚点范式 | `git log --grep "WORKTREE-05 W97" --oneline \| wc -l` | 见 §10 | **PASS** — 期望 1 |

**5/5 PASS**

---

## 10. 锚点范式

- **base main HEAD**: `f13636888` (MERGE-07 W97, 锚点 **484**)
- **本任务**: +1 (仅 memory commit)
- **锚点**: **484 → 485** (W97 +1 据实)
- **0 production code 改动铁律**: **守恒** (仅 memory)

---

## 11. 沉淀 + 派主拍待办

1. 本 memory 文件 (10 worktree 5 项实测 + origin 10/10 MATCH + 10 commit 内容评估 + 4 选项 + 逐签表)
2. **派主拍决策**: agent 默认建议 **选项 D**, 分 D1 (7 个先合再删) / D2 (3 个 cherry-pick memory 后删) / D3 (0 个可立即删)
3. **主拍额外知悉项**:
   - 磁盘 3.1T avail / 17% used — **无任何删除压力**, 可从容决策
   - **10/10 memory 沉淀全部不在 main** — 删 worktree 不丢 commit, 但知识索引触达不到
   - **7 个 commit 含未落 main 的 production/test 改动** — 尤其 #5 vitest exclude / #9 playwright project 是真实未修复问题
   - **#6 必须 rebase**, 直接 merge 会回退 RAG 大改造
   - **worktree 全集 160 个**, 本批 10 个仅占 6%; `.claude/worktrees/` 下 93 个 W82-W86 从未被本轮覆盖
4. W97 派生类 20.13 实战 #23 (派工 brief 标题数字 12 vs 正文列表 10, 据实按 10 执行)

---

**执行 agent**: WORKTREE-05
**派工 ID**: WORKTREE-05 W97
**状态**: 0 删除 0 rm-rf 0 worktree remove, 仅 memory 沉淀 + 派主拍逐签 (10 个默认建议)
**锚点范式**: 484 → 485 (W97 +1 据实, 仅 memory commit)
