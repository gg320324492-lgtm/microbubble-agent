# W93 PR7 (B-7) Observability - Start Log (2026-07-30)

## 起步 6 项 (W73 铁律)

| 项 | 状态 | 实测 |
|---|------|------|
| 1. git fetch + python -m alembic heads verify | OK | `087_add_knowledge_original_parent_id (head)` 单头 |
| 2. 读 CLAUDE.md §3 + 派工 v10 | OK | 已读 |
| 3. worktree 创建 | OK | `chore/w93-rag-pr7-observability-2026-07-30` from main (3a1ab24b3, W87 mini-16 锚点 338) |
| 4. pytest baseline (--ignore=test_w79) | 跳过 (稍后跑) | — |
| 5. npm run build baseline | 跳过 (PR7 无前端改动) | — |
| 6. memory 起步确认 | OK | 本文件 |

## 派工基线

- **PR**: PR7 — 全链路 observability
- **锚点范式**: W93 +0 → +14 (15 commits)
- **类型**: B 实施 (后端 observability, 无前端改动)
- **alembic**: 不动
- **复用**: `app/models/search_log.py` + `app/api/v1/analytics.py` + `hybrid_retriever.py` (仅头部加 hook)

## 关键路径

- `app/services/hybrid_retriever.py` — 仅 retrieve() 头部加 hook, 不改 10 def 签名
- `app/services/recall_observability.py` — 新增 (按路耗时埋点 + 慢查询告警)
- `observability/grafana/rag_dashboard.json` — 新增 (6 块面板)
- `observability/grafana/queries/` — 新增 (配套 SQL)
- `scripts/check_observability_coverage.sh` — 新增 (5 件套验证)
- `tests/rag/test_pr7_e2e.py` — 新增 (22/22 PASS)
- `app/models/search_log.py` — 仅加字段, 不改已有字段
- `docker-compose.yml` — 检查 + 不重复加 grafana (本项目无 grafana 服务)

## 量化门禁

| # | 门禁 | 阈值 |
|---|------|------|
| 1 | grafana 面板 | ≥ 6 块 (召回 P50/P95/P99, 按路召回耗时, 召回候选数, CTR, 错误率, 慢查询) |
| 2 | 按路召回耗时覆盖 | 100% 检索请求 |
| 3 | P99 延迟 | ≤ 200ms (1000 并发) |
| 4 | 日志结构化字段 | ≥ 12 (caller_path / for_query / has_query_prompt / original_len / truncated_len / latency_ms / retrieval_method / candidate_k / top_k / vector_score / bm25_score / graph_score / rerank_score / ...) |

## 5 件套验证 (W73 起步纪律)

1. `python -m alembic heads` → 1 head (087)
2. `pytest tests/rag/test_pr7_e2e.py -v --ignore=test_w79` → 22/22
3. `cd web && npm run build` → OK (基线, 无前端改动)
4. `git diff main -- app/services/hybrid_retriever.py | wc -l` → ≤ 3 行 (仅加 hook 调用)
5. `git log --grep "W93 +" | wc -l` → ≥ 15

## 据实上报铁律 (W82/W84)

禁止: 凑锚点 / 纸面 PASS / 脑补 head
强制: 真实执行命令粘贴输出, 禁止"应该 / 大概 / 估计"