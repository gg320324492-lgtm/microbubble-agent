# W98 P2-E2E 起步确认 (2026-08-01)

**派工**: P2-E2E — 5 铁证 e2e 脚本 (续讲 + 自洽 + 重启 + 反馈 + consistency)
**worktree**: E:/agent-w98-p2-e2e (branch: chore/w98-p2-e2e, base: main 4e6816c39)
**派工类型**: B 实施 (测试范畴)
**锚点范式**: W98 +6 → +7 (1 commit)

## 段 8 起步 6 项 (W73 铁律)

### S1 alembic heads verify
- `python -m alembic heads` 输出: `093_add_search_log_answer_rating (head)` — **1 head confirmed**
- base main 也 1 head (094 不存在, 仅 093)
- 无 SyntaxWarning 阻断 (仅 `028_figure_structured_fields.py` 的 `\d` 转义告警, 与本任务无关)

### S2 CLAUDE.md §3 + 派工 v10
- 已读 CLAUDE.md §3 "代码质量规范" + W73/W74 起步纪律 + W98 P2 系列上下文
- 派工 v10 段 0-9 完整在收到任务时已读

### S3 worktree 已切
- `E:/agent-w98-p2-e2e` 已 git worktree list 确认 (基于 main 4e6816c39)
- branch: `chore/w98-p2-e2e`
- working tree clean

### S4 pytest baseline (--ignore=test_w79)
- 7 个 chat 相关测试文件全跑: **165 PASSED + 3 SKIPPED** in 19.29s
- 包含: session_context / intent_classifier / rag_evaluator_cli / fast_path_casual / chat_feedback_api / followup_chips / session_archive_ui
- 用 `SKIP_DB_SETUP=1` 跑 (无真 DB 依赖)

### S5 tests/ 真查
- 段 1 期望的 7 个文件全部存在:
  - `tests/test_session_context.py` (P0-A)
  - `tests/test_intent_classifier.py` (P0-C)
  - `tests/test_rag_evaluator_cli.py` (P0-D)
  - `tests/test_fast_path_casual.py` (P1-B)
  - `tests/test_chat_feedback_api.py` (P1-D3)
  - `tests/test_followup_chips.py` (P1-E E2)
  - `tests/test_session_archive_ui.py` (P1-E E3)
- 微信 e2e 不存在 (无 `test_*wechat*e2e*`, 仅 jieba / ci_unique 等业务测试)
- `tests/e2e/` 目录存在

### S6 起步确认 (本文件)
- 本 memory 文件即起步确认, 已记录 S1-S5

## 关键代码路径已掌握

- **续讲**: `app/agent/intent_classifier.py:_match_follow_up()` (正则前置规则, 4 字段 4 类)
- **PG 回填 + Redis**: `app/agent/micro_bubble_agent.py:_fetch_pg_messages` + `app/services/chat_history_service.py:list_messages`
- **反馈落库**: `app/api/v1/chat_feedback.py:submit_chat_feedback` (Feedback 表 + search_logs.answer_rating)
- **session_id / user_id**: 沿用现有 test_chat_feedback_api 的 mock 模式 (mock_db + FastAPI 依赖注入)
- **intent 路由**: `casual_chat` / `follow_up` 走 fast path (不调工具, 直接人格 prompt)

## 5 铁证设计要点

1. **铁证 2 (续讲)**: mock intent_classifier 命中 follow_up, 验证 _build_followup_context 注入上轮实体
2. **铁证 3 (自洽)**: 模拟同一 session 两轮, round 2 注入 round 1 实体, 验证 LLM 收到完整 messages
3. **重启铁证**: mock Redis flushall → _fetch_pg_messages 仍能返 20+ 条 (PG 兜底)
4. **反馈铁证**: POST /chat/feedback mock 模式落 Feedback + search_log.answer_rating
5. **consistency 铁证**: qa-bench 双轮 5 题抽样 → 计算 std + 实体重叠率

## 下一步

进入阶段 2: 编写 `tests/chat_experience_fixtures.py` + `tests/test_chat_experience_e2e.py` + `tests/e2e_chat_experience_2026-08-01.py`
