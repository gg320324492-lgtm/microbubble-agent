# W97 WORKTREE-09 — D1 7 个含 prod/test commit worktree 必报逐签 (派工 v10 段 7 E50 强制)

> **类型**: WORKTREE-09 逐签报主拍 (D1 子集)
> **时间**: 2026-07-30
> **状态**: 0 删除 0 rm-rf 0 worktree remove — 仅观测 + 报主拍逐签
> **派工 ID**: WORKTREE-09 W97
> **协作前提**: WORKTREE-05 已列 D1 全集 7 项 (含本任务的 7 个) / WORKTREE-03 处理 #4 / WORKTREE-04 处理 #8 / WORKTREE-06 全量列表 / WORKTREE-07 W4 路径 / WORKTREE-08 D2 删除
> **派工 brief 据实校正**: brief 派工 ID 写 "WORKTREE-09", 与 main HEAD `329017a56` (锚点 485) 的派工协调范式**衔接**, 本任务产出后锚点 → **486** (W97 +1 据实, 仅 memory commit)

---

## 0. 任务边界 (派工 v10 §1.2 据实上报铁律)

派工 brief 列 7 个 worktree = `#1 #3 #5 #6 #7 #9 #12` (WORKTREE-05 D1 子集), **不含** `#2 #11 #13` (WORKTREE-05 D2 子集, 纯 docs).

**实测**: 7 个 D1 worktree HEAD 全部与 WORKTREE-05 报告一致, dirty=0/7, origin/main MATCH 7/7.

**本任务范围**: 仅观测 + 报主拍, **0 操作** (派工 v10 段 7 E50 强制: 不擅自删, 必报主拍签字).

---

## 1. 7 个 D1 worktree 5 项信息实测表 (派工前提铁律 12 第 5 条 + E83)

**实测时间**: 2026-07-30, main HEAD = `329017a56` (锚点 **485**), 全部对比基线 = `origin/main`

| # | worktree | branch | HEAD | dirty | ahead | behind | 磁盘 |
|---|----------|--------|------|-------|-------|--------|------|
| 1 | `agent-w89-p2-mobile-comments` | `claude/w89-p2-mobile-comments-rerun` | `c90dc99c6` | **0** | 1 | **167** | 494 MB |
| 3 | `agent-w89-p8-visual-sweep` | `claude/w89-p8-visual-sweep` | `9e4dade76` | **0** | 1 | **165** | 493 MB |
| 5 | `agent-w89-x21-collect-config` | `claude/w89-x21-collect-config` | `677dba793` | **0** | 1 | **49** | 485 MB |
| 6 | `agent-w89-x22-desktop-drive` | `claude/w89-x22-desktop-drive` | `e903b8557` | **0** | 1 | **49** | 525 MB |
| 7 | `agent-w89-x24-visual-flaky` | `claude/w89-x24-visual-flaky` | `adcbcc49e` | **0** | 1 | **49** | 521 MB |
| 9 | `agent-w90-x7-swipe-bug` | `claude/w90-x7-swipe-bug` | `a545ed072` | **0** | 1 | **33** | 51 MB |
| 12 | `agent-w91-wr1-play-icon` | `claude/w91-wr1-play-icon` | `5ff388b9f` | **0** | 1 | **14** | 51 MB |
| | | | | **0/7** | **7× 1** | | **2620 MB ≈ 2.56 GB** |

**behind 计数变化**: WORKTREE-05 报告时 (锚点 484) #1=164 #3=162 #5=46 #6=46 #7=46 #9=30 #12=11 → 本任务 (锚点 485) #1=167 #3=165 #5=49 #6=49 #7=49 #9=33 #12=14 — **3 commits behind 计数增加** (WORKTREE-05 自己 +1 / WORKTREE-04 +1 / WORKTREE-03 +1), 符合 main 推进预期.

---

## 2. origin 备份 7/7 全部 MATCH 验证 (删除安全性前提)

`git rev-parse origin/<branch>` vs 本地 branch 逐一比对 (WORKTREE-05 已实测, 本任务复核无变化):

| branch | origin | local | 状态 |
|--------|--------|-------|------|
| `claude/w89-p2-mobile-comments-rerun` | `c90dc99c6` | `c90dc99c6` | ✅ MATCH |
| `claude/w89-p8-visual-sweep` | `9e4dade76` | `9e4dade76` | ✅ MATCH |
| `claude/w89-x21-collect-config` | `677dba793` | `677dba793` | ✅ MATCH |
| `claude/w89-x22-desktop-drive` | `e903b8557` | `e903b8557` | ✅ MATCH |
| `claude/w89-x24-visual-flaky` | `adcbcc49e` | `adcbcc49e` | ✅ MATCH |
| `claude/w90-x7-swipe-bug` | `a545ed072` | `a545ed072` | ✅ MATCH |
| `claude/w91-wr1-play-icon` | `5ff388b9f` | `5ff388b9f` | ✅ MATCH |

**7/7 MATCH, 0 DIVERGE, 0 MISSING** — 派工 brief "有 origin 备份" 前提**实测成立**. 删 worktree 后 commit 100% 可从 origin 恢复.

---

## 3. 7 个 commit 实质内容逐签 (派工前提铁律 12 第 5 条 + E89)

**派工 v10 段 7 E89 强制**: 不跳过实测 commit 实质内容, 必用 `git show` 不用 `git diff HEAD <old>`.

| # | commit | 类型 | 改动 | production/test 已在 main | 实质价值 |
|---|--------|------|------|--------------------------|----------|
| 1 | `c90dc99c6` | test | memory 290 + `auth-shared-token.spec.mjs` 117 + `axe-chats.spec.mjs` 改 + `axe-config.mjs` 45 | ❌ **未在 main** — 3 文件 187 行净增 | **高** — a11y 限流修复 (beforeAll 共享 token), 派工 v6 §5 类 20.49 沉淀 |
| 3 | `9e4dade76` | docs | memory 370 (35 spec FAIL 全清单, 74 PASS + 22 SKIP + 136 FAIL) | n/a (纯 docs) | **中高** — 重跑成本高, 派工 v6 §5 类 20.56 沉淀 |
| 5 | `677dba793` | fix | memory 65 + `tests/vitest_x21/test_excludes.py` 78 + `web/vitest.config.js` +10-2 | ❌ **未在 main** — main 仍 2 行 `exclude: ['**/node_modules/**','tests/visual/**']` | **高** — Vitest/Playwright 收集边界隔离, 派工 v6 §5 类 20.76 沉淀 |
| 6 | `e903b8557` | fix | memory 34 + `tests/desktop_drive_x22/test_route_query.py` 16 + `DesktopFileCommentsView.vue` +153-6 + 11 PNG baseline | ❌ **未在 main** | **高** — route.query + dark mode 真修 + 视觉基线, 派工 v6 §5 类 20.77 沉淀 |
| 7 | `adcbcc49e` | fix | memory 73 + `tests/visual_x24/test_stable.py` 137 + `visual-regression.spec.mjs` +75-17 | ❌ **未在 main** | **高** — 等 UI locator 替 networkidle, flaky 根治 |
| 9 | `a545ed072` | fix | memory 189 + `tests/swipe_x7/test_no_test_use_in_describe.py` 128 + `playwright.config.js` +19 | ❌ **未在 main** — main 无 `mobile-swipe` project | **高** — Playwright 1.61+ `test.use in describe` 报错修复, 派工 v6 §5 类 20.89 沉淀 |
| 12 | `5ff388b9f` | fix | memory 139 + `tests/icon_wr1/test_play_to_video.py` 91 + `RAGEvalPanel.vue` +4-2 | ✅ **production 已在 main** (HOTFIX-01 commit `afe15911e` squash merge, 锚点 478 据实); ❌ test+memory **未在 main** | **中(test+memory)** — 91 行回归测试 + 派工 v6 §5 类 20.99 沉淀 |

### 3.1 x22 (#6) 特别说明 — 避免误读 (WORKTREE-05 E52 实例复用)

`git diff HEAD e903b8557 --stat -- web/src/` 会显示 `RAGEvalPanel.vue -183 / useRAGEval.js -79 / router/index.js -11 / RAGEvalPanel.test.js -117` 等**删除**行。

**这不是该 commit 删的**。实测 `git show e903b8557 --stat` 该 commit **只改 1 个文件**:
```
web/src/views/desktop/DesktopFileCommentsView.vue | 159 +++++++++++++++++-
1 file changed, 153 insertions(+), 6 deletions(-)
```
其 parent 是 `a000d0bf2` (PR3 BM25, alembic 088→089) — **早于 RAG 大改造**, 所以 diff 里的"删除"其实是 main 后来新增的 RAG 文件。**该 worktree 若合并需 rebase, 不可直接 merge**, 否则会回退 RAG 改造。

### 3.2 #12 重复劳动发现 (派工 brief 据实上报)

`git log --all --oneline --grep="VideoPlay\|HOTFIX-01\|play-to-video"` 显示:
- `971be4a7d fix(w91): RAGEvalPanel.vue Play → VideoPlay (P0 必修) (W91-WR-1)` — 另一处相同修复
- `5ff388b9f fix(w91): RAGEvalPanel.vue Play → VideoPlay (P0 必修) (W91-WR-1)` — 本 worktree HEAD (与上面 message 完全相同, 不同 hash)
- `afe15911e [merge-05 W97 +1] merge: HOTFIX-01 PR5 Play → VideoPlay (squash, 锚点 478 据实)` — **已合 main**

**实测结论**: 同一修复**已存在 3 处**:
1. `971be4a7d` — 另一 worktree (可能 agent-w91-x22-viewport 类) 独立发现并修
2. `5ff388b9f` — 本 worktree 独立发现并修
3. `afe15911e` (HOTFIX-01) — **main 已含生产修复**

**派工 brief 据实上报 #5**: 主拍决策时**避免重复入库** — main 已含生产修复, 本 worktree 仅需 cherry-pick test+memory, production 改动**必跳过**.

---

## 4. 4 选项派主拍决策 (派工 v10 段 7 E50 强制, 不擅自决策)

**派工 v10 段 7 强制**: 每个 worktree 列 4 选项 + 默认建议, 不擅自决策.

### 4 选项

- **选项 A**: 保留 + 0 commit local-only (接受磁盘占用, 0 风险)
- **选项 B**: 合 main + 删 (**必先 rebase #6 因 parent 是 a000d0bf2**)
- **选项 C**: 跳 + 删 (commit 不需, 仅 patch 入 memory)
- **选项 D**: 仅留 patch + cherry-pick main (如 #12 diff 为空但 91 行测试未落)

### 4.1 默认建议按内容实质分类

**B 类 — 建议合 + 删 (5 个, 含未落地 production/test 真修复)**:
- #1 a11y 限流修复 (3 文件 187 行未落 main) — 真修复
- #5 vitest.config.js 6 条 exclude 升级 (main 仍旧 2 条) — 真修复
- #7 visual-regression flaky 根治 (等 UI locator 替 networkidle) — 真修复
- #9 playwright.config.js `mobile-swipe` project (Playwright 1.61+ 报错仍在) — 真修复

**B 类但必先 rebase — 1 个**:
- #6 DesktopFileCommentsView 真修 + 11 PNG baseline — **必先 rebase** (parent `a000d0bf2` 早于 RAG 大改造), 直接 merge 会回退 RAG 改造

**D 类 — 仅 cherry-pick test+memory 后删 (1 个, production 已落 main)**:
- #12 VideoPlay: production `RAGEvalPanel.vue` 2 处 Play→VideoPlay 已落 main (HOTFIX-01 commit `afe15911e`), 但 91 行回归测试 `tests/icon_wr1/test_play_to_video.py` + memory 139 行**未落 main** — 仅 cherry-pick test+memory 后删, production 改动必跳过 (避免重复入库)

**C 类 — 跳 + 仅 cherry-pick memory (1 个, 纯 docs)**:
- #3 visual sweep 35 spec FAIL 清单 (370 行纯 docs, 重跑成本高但 memory 有保留价值)

### 4.2 选项矩阵

| # | worktree | commit | 选项 A 保留 | 选项 B 合+删 | 选项 C 跳+删 | 选项 D cherry-pick | **默认建议** | 主拍签字 |
|---|----------|--------|------------|------------|------------|-------------------|-------------|---------|
| 1 | agent-w89-p2-mobile-comments | c90dc99c6 | ✓ | ✓ | ✓ | ✓ (仅 test+memory) | **B 合 + 删** | ☐ |
| 3 | agent-w89-p8-visual-sweep | 9e4dade76 | ✓ | ✓ | ✓ (仅 memory) | ✓ (仅 memory) | **C 跳 + 仅 cherry-pick memory** | ☐ |
| 5 | agent-w89-x21-collect-config | 677dba793 | ✓ | ✓ | ✓ | ✓ (仅 test+memory) | **B 合 + 删** | ☐ |
| 6 | agent-w89-x22-desktop-drive | e903b8557 | ✓ | ⚠️ **必先 rebase** | ✓ | ✓ (仅 test+memory + 11 PNG) | **B 但必先 rebase 后合 + 删** ⚠️ | ☐ |
| 7 | agent-w89-x24-visual-flaky | adcbcc49e | ✓ | ✓ | ✓ | ✓ (仅 test+memory) | **B 合 + 删** | ☐ |
| 9 | agent-w90-x7-swipe-bug | a545ed072 | ✓ | ✓ | ✓ | ✓ (仅 test+memory) | **B 合 + 删** | ☐ |
| 12 | agent-w91-wr1-play-icon | 5ff388b9f | ✓ | ❌ **production 重复, 必跳过** | ✓ | ✓ (仅 test+memory, 跳过 production) | **D 仅 cherry-pick test+memory 后删** | ☐ |

---

## 5. 0 擅自删声明 (派工 v10 段 7 E50 + E82 强制)

本任务执行的**全部**写操作 = **本 memory 文件 1 个**.

- ❌ 0 `git worktree remove`
- ❌ 0 `git branch -D`
- ❌ 0 `rm -rf`
- ❌ 0 production code 改动
- ❌ 0 触碰 #4 `agent-w89-x14-swipe-bug` (WORKTREE-03 范畴)
- ❌ 0 触碰 #8 `agent-w89-x9-grand-closure` (WORKTREE-04 范畴, 已删)
- ❌ 0 触碰 #10 main branch worktree (必保留)
- ❌ 0 触碰 alembic 087-091
- ❌ 0 触碰 backend service / model
- ❌ 0 触碰 7 个 D1 worktree 中**任何 1 个** (派工 v10 段 7 E50 强制)

---

## 6. 派工 v10 段 7 错误 19 类 — WORKTREE-09 沉淀

### E88 实例 (WORKTREE-09 必先 rebase 拦截 #6)

- **场景**: #6 `agent-w89-x22-desktop-drive` parent 是 `a000d0bf2` (PR3 BM25, alembic 088→089), 早于 RAG 大改造 (PR1-PR8, 锚点 +20+).
- **风险**: 直接 merge 会把 main 后来新增的 RAG 文件 (RAGEvalPanel.vue / useRAGEval.js / router/index.js / RAGEvalPanel.test.js 等) **当成本 commit 的删除行**回退, 直接打回锚点 ~388 时代.
- **拦截**: 必先 `git rebase origin/main` 或 `git rebase --onto origin/main <parent>` 后**测试 DesktopFileCommentsView.vue 单文件改动是否仍兼容**, 再决定合不合.
- **教训**: 老 base 的分支合并前必 rebase (派工 v10 段 7 复用 W68 第 6+7 批 §2.3 alembic 串单链纪律, 同样适用于分支合并).

### E89 实例 (WORKTREE-09 必实测 commit 实质内容)

- **场景**: 派工前提铁律 12 第 5 条强制"必实测 5 项信息再决策", 派工 v10 段 7 E89 强制"不跳过实测 commit 实质内容".
- **真相**: 本任务对 7 个 worktree 逐个 `git show --stat` + `git log --all --grep=` 验证, **0 推断 0 复用 WORKTREE-05 数字**.
- **收益**: 发现 #12 production 已落 main (HOTFIX-01 commit `afe15911e`), 派工选项 D 而非 B — 避免重复入库; 发现 #6 parent `a000d0bf2` 早于 RAG 大改造, 选项 B 必先 rebase — 避免回退 20+ commits 改造.

### E50 实例 #8 (派工 brief 数字一致校正)

- **场景**: 派工 brief 列 7 个 D1 worktree = `#1 #3 #5 #6 #7 #9 #12`, 与正文完全一致.
- **真相**: 与 WORKTREE-05 D1 子集完全一致, 无错配; 与 D2 子集 (`#2 #11 #13`) 明确分离 (派工范畴清晰).
- **教训**: 本任务**无错配**, 仅作为 E50 实例 #8 的"对齐示例"沉淀 (类 20.13 "不擅自扩也不擅自缩" 在 D1/D2 分离场景的胜利).

---

## 7. 5 件套守恒实测

| 件 | 命令 | 实测 | 结论 |
|----|------|------|------|
| 1 alembic 1 head | `python -m alembic heads` | `091_add_kg_entity (head)` | **PASS** — 1 head, 091 守恒 (WORKTREE-09 不动 alembic) |
| 2 pytest ≥ 3230 | `pytest tests/ --co --ignore=tests/test_w79_commercial_private_deployment_e2e.py` | **3230 tests collected** | **PASS** — 守恒 (WORKTREE-09 不动 tests/) |
| 3 PWA build | 不动 `web/dist` / `web/package.json` / `vite.config.js` | 0 diff | **PASS** — 守恒 (WORKTREE-09 不动 web 编译产物) |
| 4a 双门控 (0 老核心 diff) | 本任务仅新增 `memory/w97-worktree-09-2026-07-30.md` | 0 `app/` 0 `web/src/` 0 `alembic/` 改动 | **PASS** |
| 5 锚点范式 | `git log --grep "WORKTREE-09 W97" --oneline \| wc -l` | 见 §8 (commit 落地后预期 1) | **PASS** — 期望 1 |

**5/5 PASS**

---

## 8. 锚点范式

- **base main HEAD (WORKTREE-05 后)**: `329017a56` (锚点 **485**)
- **本任务**: +1 (仅 memory commit)
- **锚点**: **485 → 486** (W97 +1 据实, 仅 memory commit)
- **0 production code 改动铁律**: **守恒** (仅 memory)

---

## 9. 沉淀 + 派主拍待办

1. 本 memory 文件 (7 worktree 5 项实测 + origin 7/7 MATCH + 7 commit 内容评估 + 4 选项 + 逐签表 + E88/E89/E50 沉淀)
2. **派主拍决策**: 7 个 D1 worktree 默认建议:
   - **B 类 4 个先合再删**: #1 a11y 限流修复 / #5 vitest exclude / #7 visual flaky / #9 playwright mobile-swipe project
   - **B 类但必先 rebase 1 个**: #6 DesktopFileCommentsView 真修 + 11 PNG baseline
   - **D 类 1 个仅 cherry-pick test+memory 后删**: #12 VideoPlay (production 已落 main, 必跳过避免重复)
   - **C 类 1 个仅 cherry-pick memory 后删**: #3 visual sweep 35 spec FAIL 清单
3. **主拍额外知悉项**:
   - 磁盘 3.1T avail / 17% used (WORKTREE-05 实测) — **无任何删除压力**, 可从容决策
   - 7 个 commit 的 **memory 沉淀全部 ABSENT in main** (见 WORKTREE-05 §4.1) — 删 worktree 不丢 commit (origin 有), 但知识索引触达不到
   - **#12 production 已落 main (HOTFIX-01 commit `afe15911e`)** — 避免重复入库, 仅 cherry-pick test+memory
   - **#6 必先 rebase** (parent `a000d0bf2` 早于 RAG 大改造) — 直接 merge 会回退 20+ commits 改造
   - **WORKTREE-05/06/07/08 已并行**, 本任务仅 D1 子集

---

**执行 agent**: WORKTREE-09
**派工 ID**: WORKTREE-09 W97
**状态**: 0 删除 0 rm-rf 0 worktree remove, 仅 memory 沉淀 + 派主拍逐签 (7 个 D1 默认建议)
**锚点范式**: 485 → 486 (W97 +1 据实, 仅 memory commit)