# W89-X-12 真 CI 触发模拟 + TEST_TOKEN 部署文档化 (2026-07-30)

> **锚点范式**: W89 第 1 批 X-12 收口 (338 → 339 守恒 +1)
> **派工前提铁律 12 + 类 20 累计 64 实例**: 本任务沉淀 **类 20.64** "真 CI 触发必含: gh auth status + act 模拟 + 真部署文档化"
> **派工 v6 段 5 反馈 #64**: 真 CI 触发必含 3 件套 (gh CLI 检测 + act 模拟 / 真跑 + 真部署文档化)

## 任务背景

W89-P-3 (commit `a765adf2f`) 写了 `.github/workflows/playwright.yml` (a11y + visual 2 job),
W89-P-9 (commit `34560ac09`) 文档化 3 secret (PLAYWRIGHT_TEST_USERNAME / _PASSWORD / _TOKEN) +
`docs/ci-secret-setup.md` + `docs/ci-trigger.md`, 但 **未真触发 CI**。

W89-X-12 收口: 用本机 docker + 真 TEST_TOKEN 模拟 GitHub Actions a11y job, 验证流程跑通。

## 实施结果 (据实)

### gh CLI 状态

```bash
$ which gh
which: no gh in (...)   # 本机 PATH 不含 gh
$ which act
which: no act in (...)   # 本机 PATH 不含 act
```

**gh CLI 未装** — 真 `gh secret set` + `gh workflow run` 留主指挥手动 (仓库设置 Secrets + Actions tab)。
**act 未装** — 模拟 CI 改用本机 docker + 直接 `npx playwright test` 跑 (走与 CI 同一份 config + 同一份 TEST_TOKEN)。

### 真 docker 起服务

```bash
$ docker ps -a --format "table {{.Names}}\t{{.Status}}" | head -10
NAMES                                          STATUS
microbubble-agent-app-1                        Up 2 hours (healthy)
microbubble-agent-db-1                         Up 2 hours
...
```

`microbubble-agent-app-1` healthy, port 8000 已可访问, 无需重启 docker。

### 真 TEST_TOKEN 拿法 (派工 v6 §5 反馈 类 20.57 铁律 3)

```bash
$ curl -X POST http://localhost:8000/api/v1/auth/login \
    -H 'Content-Type: application/json' \
    -d '{"username":"xiaoqi_testbot","password":"testbot_pass_2026"}'

{"access_token":"eyJhbGciOi...PXE_Zdq_1v2hdB1m...","refresh_token":"...","token_type":"bearer","user":{"id":59,"name":"测试小助手","role":"admin","grade":null,"research_area":null,"email":null,"phone":null,"bio":null,"avatar":null,"is_active":true}}
```

**TEST_TOKEN 拿到**, 真 jwt (sub=59, admin role), 与 `docs/ci-secret-setup.md` 模板一致。

### 真跑 a11y (axe-chats.spec.mjs 报告型 spec)

```bash
$ cd E:/agent-w89-x12-ci-trigger/web
$ export TEST_TOKEN=eyJhbGciOi...PXE_Zdq_1v2hdB1m...
$ export BASE_URL=http://localhost
$ npm ci --prefer-offline --no-audit --no-fund   # 20s, 1140 packages
$ npx playwright test -c tests/visual/a11y/playwright.a11y.config.mjs \
    --reporter=list --project=desktop-chrome axe-chats

ok 1 [desktop-chrome] › axe-chats.spec.mjs:22:5 › 01-chat axe 扫描 (2.2s)
ok 2 [desktop-chrome] › axe-chats.spec.mjs:22:5 › 02-drive axe 扫描 (2.1s)
ok 3 [desktop-chrome] › axe-chats.spec.mjs:22:5 › 03-mobile-chat axe 扫描 (2.0s)
ok 4 [desktop-chrome] › axe-chats.spec.mjs:22:5 › 04-task-trash axe 扫描 (2.0s)
ok 5 [desktop-chrome] › axe-chats.spec.mjs:22:5 › 05-file-comments axe 扫描 (2.0s)

5 passed (10.8s)
```

**5 case PASS (TEST_TOKEN 真注入 + axe-core 真扫)** — 模拟 CI 的 a11y job 走通。

实测 violations:
- 01-chat: color-contrast ×7 + scrollable-region-focusable ×1
- 02-drive: aria-required-children ×1 + color-contrast ×2
- 03-mobile-chat: color-contrast ×4
- 04-task-trash: color-contrast ×3
- 05-file-comments: color-contrast ×4

(报告型 spec 不硬门禁, 仅打印, 不破坏 CI pass)

### a11y-baseline.spec.mjs 漂移 (派工 brief 预期)

```bash
$ npx playwright test -c tests/visual/a11y/playwright.a11y.config.mjs \
    --reporter=list --project=desktop-chrome a11y-baseline

5 failed (baseline drift) + 5 passed
```

5 case FAIL 原因: `_snapshots_/01-chat-desktop-chrome.txt` 等 5 个 baseline 是 W87-G-1 (commit `e52d003fd`) 在**匿名态** (无 TEST_TOKEN) 抓的, 报"redirected-to-login: yes / violations: 0"。本次真注入 TEST_TOKEN 后:

```diff
page: 01-chat  route: /chat
-authed: no   redirected-to-login: yes
+authed: yes   redirected-to-login: no
violations: 0
+ color-contrast [serious] ×2
```

→ 登录态差异导致 baseline 漂移, **派工 v6 反馈 #25 "全绿是可疑信号"** 的真实验证:
- 匿名态: violations=0 (因为只扫到 /login 页, axe-core 默认排除 form input, 看似很干净)
- 登录态: violations=2 (color-contrast ×2, 真有可修 a11y bug)

**这是 CI 真跑价值**: 真扫有内容的页面, 而非 SPA fallback 的 /login。

派工 v6 §5 反馈 #25 实测:
> a11y baseline 必须用真登录态抓, 否则 baseline = /login 页 = 永远 0 violations = 假绿

**W89+ 留口**: 跑 `npx playwright test -c tests/visual/a11y/playwright.a11y.config.mjs --update-snapshots` 在 main 重抓 baseline, 把"匿名态假绿"换成"登录态真数据"。

### 自建 e2e (tests/ci_trigger_x/test_simulated.py)

```bash
$ cd E:/agent-w89-x12-ci-trigger
$ SKIP_DB_SETUP=1 python -m pytest tests/ci_trigger_x/ -v

tests/ci_trigger_x/test_simulated.py::test_ci_workflow_exists PASSED     [ 16%]
tests/ci_trigger_x/test_simulated.py::test_workflow_has_2_jobs PASSED   [ 33%]
tests/ci_trigger_x/test_simulated.py::test_secret_setup_doc_exists PASSED [ 50%]
tests/ci_trigger_x/test_simulated.py::test_ci_trigger_doc_exists PASSED [ 66%]
tests/ci_trigger_x/test_simulated.py::test_no_real_token_committed PASSED [ 83%]
tests/ci_trigger_x/test_simulated.py::test_playwright_a11y_passes_with_real_token SKIPPED [100%]

5 passed, 1 skipped in 0.05s
```

5 PASS (静态验证: workflow 存在 + 2 job 设计正确 + 3 secret 文档化 + docs/ci-trigger.md
含 gh workflow run 模板 + commit 不含真 JWT) + 1 SKIP (真跑留给主指挥)。

## 类 20.64 沉淀 (派工 v6 §5 反馈 #64)

### **类 20.64: "真 CI 触发必含: gh auth status + act 模拟 + 真部署文档化"**

| 子项 | 必做 | 据实 (本任务) |
|------|------|---------------|
| `which gh` + `gh auth status` | 必先验证 gh 是否装 + 是否已 auth | ❌ gh 未装, 据实报"留主指挥" |
| `act --version` 或等价模拟 | 装 act 跑或直接 `npx playwright test` 走同一 config | ❌ act 未装, 改用 `npx playwright test` 直接跑 + 真 TEST_TOKEN 注入, **5 case PASS** |
| 真部署文档化 | `docs/ci-secret-setup.md` + `docs/ci-trigger.md` 含 gh secret set + gh workflow run 命令模板 | ✅ P-9 已文档化, 本任务 e2e 验证 |
| 防御性 check | workflow + docs 不含真 JWT (`eyJ` 正则 ≥20 字符) | ✅ `test_no_real_token_committed` PASS |

5 条铁律:

1. **gh CLI 必先 `which gh` 验证** — 派工 brief 不能假设 worker 有 gh, 据实报"未装 / 已装"二选一.
2. **act 模拟必含 `--secret-file`** 或等价 (本任务用 env 直接跑 playwright), 不能"模拟"省略 TEST_TOKEN.
3. **真部署文档化必含完整 4 步**: `gh secret set X` × 3 + `gh workflow run playwright.yml --ref main`.
4. **CI workflow 真跑结果必报**: a11y PASS / FAIL + axe violations 数量 (即使 hard fail 也属成功跑通).
5. **防御性 check 必含**: commit 历史 grep `eyJ[A-Za-z0-9_-]{20,}` 应无, 防 secret 误提交.

## 留主指挥 (本机限制)

1. **手动 gh secret 部署** — 仓库 Settings → Secrets and variables → Actions → New repository secret:
   - `PLAYWRIGHT_TEST_USERNAME` = `xiaoqi_testbot`
   - `PLAYWRIGHT_TEST_PASSWORD` = `testbot_pass_2026`
   - `PLAYWRIGHT_TEST_TOKEN` = `<上一步 curl 拿到的 jwt>` (过期前需 refresh, JWT 30 min)

2. **手动 gh workflow run** — Actions tab → Playwright Tests → Run workflow → branch=main

3. **a11y baseline 重抓** — 等主指挥确认后, 跑 `npx playwright test -c tests/visual/a11y/playwright.a11y.config.mjs --update-snapshots` 在登录态重抓 `_snapshots_/01-chat-*.txt` 等 5 个 baseline, 把假绿换成真数据。

## commit 链

| commit | 内容 | 锚点守恒 |
|--------|------|----------|
| `a765adf2f` | P-3 cherry-pick (.github/workflows/playwright.yml + 5 PASS e2e) | +1 (338→339) |
| `34560ac09` | P-9 cherry-pick (docs/ci-secret-setup.md + ci-trigger.md + 7 PASS e2e) | +1 (339→340) |
| 本任务 | tests/ci_trigger_x/test_simulated.py (5 PASS + 1 SKIP) + memory | +1 (338→339) |

> **注**: W89-P-3 + P-9 + X-12 在 main 平行分支派, 不串到 W89 锚点. X-12 锚点预期 +1 (338→339).

## 派工 v6 段 5 反馈 #64 沉淀

派工 v6 段 5 反馈累计 64 实例 (W87 第 1 批 + 16 + W88 第 1 批 + 4 + W89 第 1 批 + 6 + 历史 + 38):
- W87: 类 20.25-30 + 31 + 32
- W88: 类 20.33-36
- W89: 类 20.57 (P-9) + **类 20.64 (X-12, 本任务)**

W89+ 留口 (主指挥):
- 真 gh secret 部署 + workflow run (本机无 gh CLI)
- a11y baseline 重抓 (登录态 → 真数据)
- act 安装 (可选, winget install nektos/act)

## W89-X-12 守恒验证

- **0 production code 改动铁律 守恒**: 仅 `.github/workflows/playwright.yml` (P-3 cherry-pick)
  + `docs/ci-*.md` (P-9 cherry-pick) + `tests/ci_trigger_x/` (本任务新增, 测试范畴) +
  `memory/` (本任务新增). 不动 `app/` / `web/src/` / `alembic/versions/` / `nginx/` /
  `docker/` / `web/dist/` / `commercial/`.
- **派工 v6 §1.2 真验证**: 5 e2e PASS (静态验证) + 1 SKIP (真跑留主指挥).
- **派工 v6 §5 反馈 #64 沉淀**: 5 条铁律 + 1 据实报告 (gh/act 未装) + 1 防御性 check.

---

**主指挥收口**: 本任务作为 W89-X-12 (真 CI 触发 + TEST_TOKEN 部署) 收口, 派工 brief 估 5 实 5+1 守恒.
W89+ 派工顺序表: W89 第 2 批 / W90+ 调研 / 真 binary 装机收口 / npm audit hint 链调研.