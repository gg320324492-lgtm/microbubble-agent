# W101 P3-A11Y 起步 (2026-08-03, 主指挥协调范式第 N 次派工)

> **锚点范式**: W100 +39 → W101 +0..+5 守恒 (+6 据实上报, P3-A11Y WCAG 2.1 AA 全面提升).
> **worktree**: E:/agent-p3-a11y (基于 main `84d2c0b49`).
> **派工 brief**: chat 上下文 WCAG 2.1 AA + 全键盘可达 + 屏幕阅读器友好.

## 派工前提错配拦截 (类 20.123 / 类 20.13 实战)

调研发现派工 brief 列出的 9 个组件 a11y **大部分已存在**, 真实缺口仅 6 处:

| 派工 brief 假设 | 实测代码现状 | 处置 |
|----------------|--------------|------|
| ThinkingCapsule aria-live=polite | `ThinkingCapsule.vue:125` 已有 | **不重复** |
| BriefDetail aria-expanded + aria-controls | `ContentBriefDetail.vue:75-81` 已有 | **不重复** |
| EventBadges role=status + aria-live=polite | `EventBadges.vue:180-181` 已有 | **不重复** |
| ProEntries role=toolbar + aria-label | `ProEntries.vue` header 已有 | **不重复** |
| ChatMessageActions role=toolbar + aria-label | `ChatMessageActions.vue:2` 已有 | **不重复** |
| ContextPanel role=tablist + aria-selected | `ContextPanel.vue:136/140` 已有 | **不重复** |
| PlanSteps aria-expanded + Enter/Space | `PlanSteps.vue:86-91` 已有 | **不重复** |
| ToolTraceItem aria-expanded + aria-controls | `ToolTraceItem.vue:35-40` 已有 | **不重复** |
| SessionActions aria-label | `SessionActions.vue:40-61` 已有 | **不重复** |

## 真实缺口 (5 处, 据实修复)

1. **KnowledgeRefBlock 0 a11y** — 完全无 role / aria / keyboard / tabindex (派工 brief 列表但实施段未细化)
2. **ChatViewSSE skip-link** — 桌面端 ChatViewSSE 缺 "Skip to main content" 跳过链接
3. **全局 focus-visible token** — 散落各组件 (NavRail/PlanSteps/ContentBriefDetail), 未抽 variables.css
4. **PlanSteps per-step aria-label** — 已用 status aria-label, 但无完整 "步骤 X: ..." 描述
5. **vitest a11y 缺口** — KnowledgeRefBlock 0 test

## 5 件套守恒计划

1. **alembic heads** — 沿用 W100 +39 base `096_add_rag_multimodal_metrics`, 不动 alembic
2. **vitest** — KnowledgeRefBlock a11y test 新增 + skip-link existence test + 全部 PASS
3. **PWA build** — 不涉及 frontend public asset, 沿用 base (vite-plugin-pwa disable: true)
4. **0 production code** — 仅 web/src/components/chat/* + web/src/views/chat/* + web/src/assets/variables.css + 新增 test
5. **锚点范式** — W100 +39 (537) → W101 +0..+5 累计 +6 (据实 543)

## commit 计划 (5 commits)

- W101 +0 (memory): startup memory 沉淀 (本文件)
- W101 +1 (variables.css): 全局 focus-visible token (`--focus-outline-width` + `--focus-outline-color` + 通用 :focus-visible selector)
- W101 +2 (KnowledgeRefBlock.vue): 加 `role="list"` + 每个 ref `role="listitem"` + `aria-label` + `tabindex="0"` + Enter/Space 键盘 + focus-visible
- W101 +3 (PlanSteps.vue): per-step 完整 aria-label ("步骤 X: {step_name} 已完成/进行中/待执行")
- W101 +4 (ChatViewSSE.vue): skip-link + `<main id="chat-main">` landmark
- W101 +5 (vitest a11y + memory closure): KnowledgeRefBlock a11y test + skip-link test + memory 收口

## 5 类 20 沉淀

- 类 20.137: 派工 brief 列出 9 个组件 a11y 任务, 实测已大部分存在, 必须先 `grep -rn "aria-label\|role="` 调研后据实修复缺口, 不批量凑数
- 类 20.138: 散落的 focus-visible 样式应抽 variables.css token (`--focus-outline-*`), 各组件复用而非复制
- 类 20.139: skip-link 必置于 `<body>` 第一个可聚焦元素, hidden by default, focus-visible 时显示
- 类 20.140: KnowledgeRefBlock 这种"渲染列表卡片"组件, `<div v-for>` 必须加 `role="list"` + 每个 `<div>` 加 `role="listitem"`, 否则屏幕阅读器报 "list of N items" 但识别不到项目
- 类 20.141: 复杂组件的 `aria-label` 不止描述状态, 必含组件身份 (e.g. "步骤 1: 查找资料 已完成" 而非仅 "已完成")

## 类 20.142 (P3-A11Y 派工起点必 fetch 沿用)

派工起点必 fetch origin + merge-base 拦截漂移 (类 20.131 W100-RAG-6 沿用): 本任务起点已 git fetch origin + git log --oneline -5 + 当前分支 tip `84d2c0b49` 守恒, main HEAD = `84d2c0b49` (W100 +39 qa-bench closure).