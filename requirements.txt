# Web框架
fastapi>=0.109.0
uvicorn[standard]==0.24.0
python-multipart==0.0.6
websockets==12.0

# 数据库
sqlalchemy==2.0.23
asyncpg==0.29.0
alembic==1.13.0
psycopg2-binary==2.9.9

# Redis
redis==5.0.1
celery==5.3.6

# 对象存储
minio==7.2.0

# AI相关
anthropic>=0.39.0
mcp>=0.9.0

# 语音识别
pilk>=0.2.4
faster-whisper>=1.0.0

# 声纹识别 + VAD
modelscope>=1.14.0
torch>=2.0.0
torchaudio>=2.0.0
addict
# 2026-06-24 v29 Qwen3-Embedding-0.6B: LLM-based embedding wrapper
accelerate>=0.34
# transformers>=4.45 / safetensors>=0.4 已在 torch 依赖链中装好, 无需重复声明
datasets
sortedcontainers       # modelscope/datasets 传递依赖
simplejson             # modelscope 传递依赖（CLAUDE.md 模型依赖安装 提示）
soundfile              # 3D-Speaker ERes2Net pipeline 读取 wav 需要
pysoundfile            # soundfile 的 Python 绑定（部分平台需要）

# 语音合成
# 2026-06-13 升级：6.1.9 的 TrustedClientToken 已过期，Microsoft 返回 403 Forbidden。
# 7.2.8 更新了 internal UA + endpoint 配置修复。>=7.2 锁定 minor 版本兼容 API。
edge-tts>=7.2.8,<8.0.0

# 向量数据库
pgvector==0.2.4
sentence-transformers==5.6.0
# optimum 不装：实测 ONNX 在 GPU 上慢 12-22x（无收益）
# optimum[onnxruntime]>=1.20.0
rank-bm25>=0.2.2
jieba>=0.42.1
neo4j>=5.0.0

# 数据处理
numpy==1.26.2

# 文件解析
PyMuPDF==1.24.0
python-docx==1.1.0
openpyxl==3.1.2
python-pptx==1.0.2

# 多模态知识库 OCR（Phase 7）
Pillow>=10.0.0          # 图片处理（缩放、模式转换）
# pytesseract 可选（备选 OCR 后端，需系统 tesseract 二进制）

# 工具库
python-dotenv==1.0.0
pydantic-settings==2.1.0
pydantic[email]==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
httpx==0.25.2
email-validator==2.1.0

# 测试
pytest==7.4.3
pytest-asyncio>=0.25,<1.0
