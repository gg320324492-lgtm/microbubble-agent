# FRP 服务端配置 - 部署在云服务器
bindAddr = "0.0.0.0"
bindPort = 7000

# 认证
auth.method = "token"
auth.token = "microbubble-frp-2026"

# 日志
log.to = "/var/log/frps.log"
log.level = "info"
log.maxDays = 7

# Dashboard（可选，方便调试）
webServer.addr = "127.0.0.1"
webServer.port = 7500
webServer.user = "admin"
webServer.password = "frp-dashboard-2026"
